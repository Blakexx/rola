# rola-devtools, the development tools every RoLA repository shares (the dev config reader, the machine's locks, the
# public mirror's export, the measurement service), at the public commit this tree is tested with. CI and the dev image's
# bootstrap install it from here; a dev machine links its checkout instead (`python tools/dev.py init`).
rola-devtools @ git+https://github.com/Blakexx/rola-devtools@372af221b57edb558b67140c290e3dbd19c8890e
