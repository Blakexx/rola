# rola-devtools, the development tools every RoLA repository shares (the dev config reader, the machine's locks, the
# public mirror's export, the central cells, the declared build system and its timing system), at the public commit this
# tree is tested with. CI and the dev image's bootstrap install it from here; a dev machine links its checkout instead
# (`python tools/dev.py init`).
rola-devtools @ git+https://github.com/Blakexx/rola-devtools@eebcf9515fd69fc2b2cc8bb097172e018b9a550d
