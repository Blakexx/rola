# Open work

Carried in from the extraction. These become GitHub issues when the repository is
published (execution checklist E12); until then this file is the list.

## The clean-slate line (card C, ratified 2026-08-30) — what is knowingly absent

This section recorded the plan's own starting state right after the clean-slate deletion
took out the old carry kernel and the old stats passes (`docs/internals/DELETIONS.md`, two
rows). Most of the rebuild stages named below have since landed; the row-by-row state is
UPDATED IN PLACE rather than left describing the pre-rebuild tree, per this file's own
no-regression-list rule — a reader here wants what is absent NOW, not an obsolete snapshot.

| # | Absent, and what builds it back |
|---|---|
| **C-a** | **The prefill (carry) forward is BUILT** (the pipelined box-native body, `docs/internals/carry/carry_kernel.md`): `extension().carry_forward` is a real, shipped launch, and `rola.ops.carry.carry_forward` reaches it. What remains absent is the higher-level wiring: `rola.interface.rola_op` (and therefore `rola.layer`'s prefill path) still `raise NotImplementedError("the prefill arm is deleted...")` unconditionally — the op-level kernel exists, the layer/facade path to it does not yet. |
| **C-b** | **The prefill backward.** Still absent; follows C-a's layer wiring. |
| **C-c** | **The liveness pass is BUILT** (`csrc/rola/src/facts/liveness.{cu,cuh}`, `docs/internals/facts/liveness.md`): one geometry-independent pass, graded against `rola.engine.facts.liveness.liveness_words`. |
| **C-d** | **Decode's PAGED path.** Resolved differently than this row originally anticipated: decode's own kernel condenses its write-atom set ON DEVICE (`atom_bits`, `docs/internals/decode/decode.md#atom-bits`) rather than sourcing it from the facts/stats pass, so the paged decode integration tests (`tests/integration/test_decode_paging.py`) were never actually blocked on C-c's liveness pass landing. |
| **C-e** | **The carry arm table carries ONE row.** `tools/manifests/shipped_set.json` declares `[2, 64, 8]` — the flagship shape, one CTA per SM, `DV = 64`. Selection, the generated arm set, the manifests' `shards` block and the post-build check all run against that one row; the other two declared `DV` values (32, 128) are domain members with no built arm yet. |
| **C-f** | **Whether the other families' instantiation axes fold into the carry key.** `shipped_set.json` is the CARRY family's declaration. The producer's solves key on `(LW, IPT, ALPHA15)`, the decode step on `(DV, D, DECAY)`, the intra family on `(D, B, W)` — each its own closed set, none of them the carry key. Whether they become one keyed set or stay separate declarations is unruled, and `tools/lint/drift_guards.py`'s `rule_arm_axes` rule reads carry kernels only until it is. |

## Blocking the release

| # | Item |
|---|---|
| **Namespaces and the AOT build** | **DONE** (commits `5b05ebd`, `17f30aa`, `7dfaa13`, `fc9fa57`). Namespaces are `rola` / `rola::mma` / `rola::paging`; `rola_api.cpp` and `arch_table.cuh` are split out; `setup.py` builds ahead of time with the rule-2 no-PTX assertion (verified on the shipped `.so`: 8 ELF images, **0 PTX images** by `cuobjdump --list-ptx`), the R1/R3 pre-build gates, the post-build ratification, and a generated `_build_config.py`. Re-ratified: 1024 instantiations, 0 unhonored bounds, and tools/verify_rename_transform.py proved **every register and spill byte identical** under the declared rename, with the `ptxas` pin byte-identical. (That one-shot gate was deleted with the tiled consumer: its declared transform named `v3_consumer_forward_kernel` and its pre-rename manifest inputs no longer exist. `git show c7eaeb9:tools/verify_rename_transform.py`.) |
| **W2** | **DONE.** `rola/interface.py` lands the three-layer op stack (private `_consumer_forward` -> `RoLAConsumer(autograd.Function)` -> public `rola_op`) with ONE explicit dispatch predicate, `select_backend`, shared verbatim with `RoLA.select_execution_backend`. The public entry is a single function: the API contract collapsed `rola_func`/`rola_forward` into `rola_op(routes, v, decay)` and moved plan/statistics arguments off the surface entirely (`docs/api.md`). A grad-requiring call runs the WHOLE operation on the differentiable reference -- forward and backward from one graph -- and `RoLAConsumer.backward` is a named tripwire rather than a graph-severing leaf. The O-3 rename landed in the same commit: `naive_rola`, `select_plan`, `plan_and_schedule`, `build_schedule`, `Plan`, `Schedule`, `DecayStream`, `PageArena`, `RouteFactors` (then `RoutingOutput`), the producer slot (then `produce_routing`), `ROLA_SCHEDULE_ABI_VERSION`, and `_consumer_forward` (now private). `tests/unit/test_interface.py` gates the name contract mechanically over the package's AST. **Deferred:** `torch.library` registration + `_fake` meta kernels, filed as W2b. *(Original text: the three-layer op stack, `torch.library` registration + `_fake` meta kernels, and the dropping of `v3` from every public python name.)* | The three-layer op stack (private `_forward` → `torch.autograd.Function` → public `rola_func`/`rola_forward`), `torch.library` registration + `_fake` meta kernels, and the dropping of `v3` from every public python name (`naive_rola`, `select_plan`, `plan_and_schedule`, `_consumer_forward`). One commit, so that no function has two public spellings. A single autograd boundary is also the structural fix for the layer-dispatch autograd gap. |
| **W2a** | **DONE**, and SUPERSEDED by P67 S3b. The stateful surface is now ONE object per sequence: `rola.state()` -> `rola.RoLAState` (a strategy stamp, the keying it binds to, and a paged or dense backing), with `state is not None` the single predicate at the op and at the layer, and `(y, state)` the return of both (docs/internals/state.md). The original answer to W2a -- a per-STACK, layer-indexed native cache with a `transformers` adapter behind an extra -- was deleted with it (docs/internals/DELETIONS.md): a per-layer dict of raw tensors cannot own a page table, and `use_cache` had become a second spelling of the state argument. **A fixed-size recurrent state is still not a KV cache**, which is why no `transformers` abstraction was borrowed in either design; a shim onto the new surface is a wrapper at a model boundary if one is ever wanted.
| **W2b** | **`torch.library` registration + `_fake` meta kernels**, carved out of W2. The op's signature carries structured Python objects, which no `torch.library` schema can express. The obstacle SHRANK with P67 D2 — `Plan` and `Schedule` are gone, and the chunk entry takes a handful of tensors plus the widths — so what remains on the surface is `RouteFactors`/`Topology` and the state object; a flattening layer is still needed and still does not exist. What it buys when it lands: `torch.compile` and meta-device tracing across the op boundary. |
| **W9** | **DONE** (P65 + B1b; merged 2026-08-15/16). The two-pass native backward (Scan A rebuild + Scan B adjoint, zero-snapshot) ships on six scan arms; a grad-bearing `rola_op` call trains through the kernel, and `SavedForward`-era ctx machinery dissolved into the retained `ChunkPlan` reference (P75 stage 2). What remains refused, by design rather than by absence: decode is forward-only (the single-token arm has no reverse pass), and a gradient AND a carried `state=` are mutually exclusive (training is stateless in v1, P65 R-D). Design: `NATIVE_BACKWARD_DESIGN.md`. |
| **The geometry matrix** | **DONE** (`45d7f8b`). tests/tier2_geometry/matrix.py declared every geometry axis with its KIND (GEOMETRY / PROBLEM_SHAPE / SEMANTIC) and every pair with its contract class, precondition, basis, derived-bound name, and non-vacuity probe. The census ran at module IMPORT (so an undeclared geometry fails at COLLECTION), decodes the shipped axes from the mangled names in the ratification manifests, and pins the manifest to `BUILD_CONFIG['manifest_sha256']`. All three meta-tests were verified by construction: the census against a fabricated `BT = 64`, the class-drift check against a `P > 1` launch forced to `P = 1` AND against a class-(a) pair mislabelled (b), the non-vacuity check against an inert probe. Two classes were MEASURED before being declared (`BC` 16-vs-64 is byte-equal on state; `BT` is not on either output), and the design's own `P` sub-case was corrected: zero-initial-state bit-identity on state is a `P = 2` claim only. |
| **Per-arch manifests** | **DONE** (`e4ecd5c`). `tools/manifests/cu13/sm_80.json` + `sm_86.json`, each pinning its own toolchain; `--arch` (repeatable) selects what is probed, checked and written; `setup.py` rule 3 is `path.exists()` and passes `--arch` to the post-build gate. The split is FILING ONLY and it is checked: `manifest_sha256` is taken over the merged canonical record, so N files hash to what one file hashed to (`c2077aa3…` unchanged). `CUDA_BIN` is replaced by `cuda_bin(tool)` resolving torch `CUDA_HOME` → `$CUDA_HOME` → `/usr/local/cuda`, the order `setup.py` uses. `--self-test` grew two arms (unratified arch; split rejoin) and now requires four failures. |
| **The bench harness split** | **DONE** (`78e642a`). Split into `benchmarks/bench/{cells,pairing,instruments,drivers,regression}.py` + a 155-line dispatcher, every body verbatim, 17/17 cells and `topology_manifest.json` byte-identical. `pairing.py` states THE methodology. pytest-benchmark adopted for storage/compare with the judgement kept local: the limit is `median + 3*IQR/1.349`. The spread took two measured corrections, both recorded in the code — within-pass IQR then within-process block IQR were both too small, so the baseline pools block medians across 7 independent processes. *(Original text: bench harness restructure (§6.9):* benchmarks/bench_consumer_new.py is 1385 lines of reusable core plus 11 accreted per-question drivers. Split into `bench/cells.py`, `bench/pairing.py` (the interleaved paired-arm engine — this is THE methodology and must be documented as such), `bench/instruments.py` (ncu counters, gatherstats), and thin drivers. Add the pytest-benchmark regression layer with thresholds **derived from each cell's measured IQR**, never invented percentages. Apply the O-11 provenance normalization at the same time: GPU model + driver + toolchain only, host identifiers stripped. |
| **The document set** | **PARTIALLY DONE.** Landed: the README per §7 (three claims above the fold, each linking a shipped artifact), the GENERATED supported-configurations and activation-capability blocks (`tools/supported.py`, drift-gated by `tools/lint/lint_standards.py::check_supported_table`), and `docs/{testing,ratification,build,bringup}.md`. **Still open:** a `docs/` spec page with REQUIREMENT-ID anchors per the superseded O-10 — every normative clause gets an immutable `[SPEC-…]` ID, tests cite IDs only, and CI checks that every cited ID exists exactly once and that citing tests pin a content hash of their clause, so a silent normative edit fails on purpose. `planner.md` LANDED, and its scope collapsed with the mechanism: `BC` is a closed form over launch-static inputs, so there are no cost constants to cite cells for — what the doc states instead is each ceiling's derivation and its falsifier. `api.md` LANDED with the API contract: the reduction-theorem surface (`rola_op`, the `RouteFactors` bundle, the producer and decay-source slots, the quarantined expert namespace). `evidence.md` and `bringup.md` shipped as `testing.md` / `bringup.md`. |
| **W7** | **DONE** (`261ceeb`). `ruff check .` is **0**. The 70 `F821` were not timeit strings: they were `del layer, x, v, …` at the end of each VRAM-freeing loop body, unbinding names that closures in the same body read — rebinding to `None` drops the same references without unbinding. One genuinely dead rebinding in `rola/routing/reference.py`, seven unchecked test locals, two generators, one lambda. Three rules suppressed with the reason stated in `pyproject.toml` (TC001 joins TC002/TC003; RUF022 off for `rola/__init__.py`, whose `__all__` is grouped by concept). *(Original text: lint is not clean, and never was.* `ruff check .` under the carried configuration: **149 errors** in this repo, **136** on the same files in the fork. Overwhelmingly `F821` inside benchmark `timeit` setup strings (70), `I001` import order (30), `RUF022`/`UP037`/`F841`/`F401`. Pre-existing debt, inherited wholesale by the move; a real cleanup is its own unit and must not ride inside a rename. The configuration is now committed so the standard exists to be met. |
| **The activation registry** | **DONE**, in the registry form of the 2026-08-01 amendment. `rola/routing/activations.py` holds ONE table, `ACTIVATION_REGISTRY: tag -> {normalized, exact_zeros, support_differentiable, kerneled}`. Users write plain tagged constructors (`softmax()`, `entmax(alpha)`) and never see property metadata; every validator reads COLUMNS (mass decay requires a `normalized` write side; union routing's intersection guarantee requires `exact_zeros`), never a tag list, and `tests/unit/test_activation_registry.py` enforces that structurally by walking the package AST for activation-class references outside the exempt lowering modules. An unknown tag is a hard panic at config construction; admission to `IndependentRouting` is a registry lookup, not an isinstance list, so one row makes a variant participate in every rule at once. The `kerneled` column drives dispatch: `rola_op` routes an unratified activation to the reference path carrying an explicit `not_kerneled` flag (`rola/routing/types.py`), and `_consumer_forward` refuses one outright so the routing is a guarantee, not a convention. The docs' capability matrix is GENERATED (`capability_table()`). *(Original text: a `LevelActivation` protocol carrying properties as data.)* | (design §4.6, user amendment 2026-08-01). A `LevelActivation` protocol carrying the forward map AND its properties as data (`normalized`, `exact_zeros`, `support_differentiable`); per-level AND per-duty (read/write) configuration, which turns level-split into pure configuration; validation of combinations against the declared properties, loud rather than silent. v0.1 ships two ratified instances (entmax α∈{1.5,2}, dense/softmax). The ratified kernel surface is unchanged — an unratified activation dispatches to the reference path carrying an explicit `not-kerneled` flag, which is a labeled research surface and not a kernel fallback. Lands with the three-layer public surface above, because it is the same public shape. |

## Measurement, resuming in this repository

| # | Item |
|---|---|
| **M-1** | **DONE (2026-08-01). RULING A: `STAGE_COLUMN_WORK` IS RETIRED, ON A MEASUREMENT.** The term priced `2 · gram_cols · 85 / ring_slots` per candidate tile and was load-bearing (zeroing it flipped 4 of the 8 measured argmins), so it could not be deleted on the source read alone. `gram_cols ≤ BC` with equality iff ONE level is spanned, and all four calibration cells span one — so no row that ever bound the constant could see its variable. Fifteen isolating cells were added to the frozen registry (`iso-gc-*`, `iso-tile-*`, 60/60 oracle arms green) and profiled with the model's own instrument (`ncu --clock-control base`, `smsp__inst_executed.sum`, one launch/arm, RTX 3080 Ti). **`iso-gc-w32x16x8-BC64` (gc=16), `iso-gc-w64x16x4-BC64` (gc=20) and `iso-gc-w64x32x2-BC64` (gc=34) retire BYTE-IDENTICAL instruction counts (10,062,336):** the per-staged-column price is exactly 0.000, not 85. What the counts do separate is the FIXED-level count, at 74.6/tile/level — measured, decision-inert, recorded, not modelled. The work the term carried is a per-candidate-tile CONSTANT: `TILE_FIXED_WORK` re-measured **120 → 3510.35** by two independent isolating solves (3507.0 and 3513.7, 0.19 % apart), each separating a per-COHORT cost from a per-TILE one with a three-point `T` sweep first. Result: **8/8 argmins hold** (and `TILE_FIXED_WORK` is now IDENTIFIED from below, where it used to be inert 0→1e6), and the absolute-total recording goes from ±83.9 % / mean 41.9 % / mixed signs to **+44.8 % / mean 17.4 % / signs consistent (+)**, with the `BC=16` rows moving from −26…−35 % to +2.9…+4.6 %. The ring axis is now UNPRICED in every currency, so `select_plan_axes` STATES the depth instead of arbitrating equal keys; the plumbing survives for §2.3's item buffers, whose ring — if it is ever built — gets a price measured on the kernel that has one. **Two named successors, both measurements, neither a re-fit:** (a) the dense k-quantum is over-priced at 1899 against 1033/1155/1316 measured at `D`=1/2/3 and grows ~141 per level, now the leading residual; (b) at `atscale-L2048` 39 % of candidate pairs gather nothing and skip per-tile work the model still charges. |
| **M-2** | Deferred kernel-performance queue. **The two forward-interior items are CLOSED on measurement, with no code — so the forward kernel interior is FREEZE-READY.** *Single-copy `W`* is a **no-op against this tree**: there is exactly ONE `W`-side buffer (`WT`, `[BC][kWtPitch]` bf16 leaf-major), serving GEMM2 as B (leaf-major already *is* `[k][n]`, fetched by `wt_frag_b`'s `ldmatrix.x2.trans`) and the fold as A (`wt_frag_a`'s `ldmatrix.x4`, the same bytes read under the other atom at zero cost) — the duplicate the item was accepted to delete does not exist. The bf16 re-encoding named here as the only buildable neighbour is NOT open work: `WT` has been bf16 since the endgame, so there is nothing left to re-encode. *Windowed-metadata / async pipeline* was BUILT and MEASURED (P39, `workflows/perf/p39_prefetch.md`), and the closing sentence of the original entry is the finding that survived: the amplitude `cp.async` prefetch adds NO steady-state barrier and does cut `long_scoreboard` (-46% dense, -26% at scale) with DRAM traffic down 43% on the dense cell — but the kernel's dominant stall is still the BARRIER, and at scale the mechanism's own cost lands there (+22%). It did not buy the occupancy freedom it was built for: the wide window's residency penalty is unchanged. Full measurement, per-cell tables and reopen conditions: `workflows/20260801-extraction/extraction.md`, "M-2 kernel-interior package". **M-2's forward-interior findings are the TILED consumer's**, and that kernel retired in P67 D2 — they are kept as the measured record of a kernel that shipped, not as open work against the chunk arm, whose interior is its own queue. **Still open in M-2:** the dense-saturation closed form; stage 4 closed-world rules 2–4; stage 5 headline benches including the latency-vs-capacity frontier cells. **Named but NOT started, and needing a ruling before it is:** the forward's dominant stall is `__syncthreads` at 8 warps/CTA, so barrier reduction (starting with the per-candidate-tile barrier compaction added) or warp specialization is the measured next target — a forward-interior redesign, not a freeze-adjacent micro-item. |
| **M-3** | Level-split implementation — the campaign's third ablation arm. |

## M-4 — Decode kernel + decode bench cells (filed 2026-08-01, user question)

**THE KERNEL HALF IS DONE.** A dedicated `T = 1` decode path ships: bitmap support
expansion + token-stationary gather-GEMV, fold and walk in ONE kernel
(`csrc/rola/src/decode/decode.cu`), behind the same state facade as the chunk arm
(`decode_forward(routes, v, state)`), with a PAGED backing whose address is the 16-leaf
atom and the step's own per-batch-head residency verdict, reached off the write map the
walk builds anyway, that keeps a steady step off any admission preamble at all
(`docs/internals/decode/decode.md`). Gated by
`tests/oracle/test_decode_vs_oracle.py`, `test_decode_continuation.py` (6),
`tests/integration/test_decode_paging.py` (24), `test_decode_growth_trigger.py` (33),
`test_decode_bc_blindness.py` (4) and `tests/integration/test_decode_dispatch.py` (18).
**Still open, and it is the measurement half this item also asked for:** decode BENCH
cells and latency-regression rows — every shipped bench cell is still prefill, so any
serving/decode-bandwidth claim remains derivation-only. The steady step's own profile is
recorded (`decode/decode.md` §4b) and is host-wait-bound, which is what a decode bench would
have to be built to measure honestly.

*Original text, for the record:* No V3 decode kernel exists (V2's fused_recurrent_rola had no successor).
Decode today = the layer's cache path through general machinery (correct —
91 layer tests incl. cache round-trips — but never benchmarked or gated; all
17 bench cells are prefill). Work: a dedicated small decode kernel (T=1:
route, gather support, skinny read, deposit, clock tick — shares
producers/state layout with the consumer, none of its tile/wave machinery),
plus decode bench cells + latency-regression rows + tier-1/tier-2 coverage
(decode vs oracle single-step; decode-then-chunk geometry class). Load-
bearing for: any MEASURED serving/decode-bandwidth claim (§6's decode ledger
is currently derivation-only) and the lifelong demos. Sequencing: post-freeze
era, natural sibling of the backward build (shares the freeze's stability
premise). Sized: days under batch discipline, not weeks.

## P67 named future work (filed 2026-08-14, the paging integration's close-out)

Designed, contract carried IN THE REPOSITORY, not built and not scheduled. Each has a
doc section rather than a line here, because the contract is what a future builder needs:

| # | Item | The contract |
|---|---|---|
| **P67-a** | **A device-side ADMISSION. DONE** (K21/K23). `rola.state(pool_slack=...)` reserves a fraction of each batch-head's residency as pre-committed, pre-zeroed ordinary slots; a growth step claims into them itself, replicated and canonical across its CTAs with no atomic, via a shadow table row folded into the real table by the batch-head's last-arriving CTA. Exhaustion degrades to exactly the pool-free host round trip. `pool_headroom`/`refill_pool` are the host's off-path knobs for restocking it. | [`internals/paging/paging.md` §the-slack-pool](internals/paging/paging.md#the-slack-pool) |
| **P67-b** | **Lifting the 4 GiB state-plane bound.** The carry family addresses state by a 32-bit byte displacement from a pinned origin. The dense backing hits the bound first and the paged backing is the answer to it; the two need different lifts (one per-`bh` 64-bit base vs arena sharding). | [`internals/carry/carry_kernel.md`](internals/carry/carry_kernel.md) |
| **P67-c** | **Copy-on-write state branching.** `strategy="cow"` is refused by name today. The semantics are SETTLED (strategy on the state as a stamp, plan-overridable; fresh-slot writes; every returned state stays valid and resubmitting one IS branching; refcounts for GC and the never-write-a-shared-slot invariant; a strategy change is a full deep copy in both directions), as is the known cost and its known fix (the per-step table copy wants a two-level page table). | [`internals/state.md` §9.1](internals/state.md#cow) |
| **P67-e** | **Retire the legacy V3 tiled consumer. DONE** (D2-a `d45f92f` … D2-d, this row's own phase). The dispatch repointed onto the built chunk matrix, ~6,900 test LOC re-anchored or retired, then 53 files and 11,615 lines deleted whole — csrc, the planner package, the tiled measurement apparatus — with the protected surface proven unmoved (310 non-consumer SASS bodies: 0 missing, 0 extra, 0 differing) and the docs made truthful. Every capability removed has a row in [`internals/DELETIONS.md`](internals/DELETIONS.md) with its evidence and revival SHA (`c7eaeb9`). | [`internals/DELETIONS.md`](internals/DELETIONS.md) |
| **P67-f** | **Widen the chunk matrix to the shapes the tiled consumer used to carry.** The retirement above NARROWED the shipping kernel envelope to the built matrix (`docs/api.md` §1.1): `d_v = 32`, `raw`/per-state normalization, jagged topologies (including the flagship `widths = (8, 16)`) and an unbuilt `(D, B)` are now REFUSED (P71-B's fast-fail replaced the oracle dispatch this row was first written against). Each is a separate piece of work with a separate cost — a `d_v` axis is an instantiation, a jagged topology is a kernel-shape question, `raw` is a readout variant — and each should be justified by a measurement showing the shape matters at scale rather than by the fact that it used to exist. | [`api.md` §1.1](api.md#11-the-built-envelope-is-the-surface) |

## K31 integration rung — named future work (filed 2026-08-20)

| # | Item | Where |
|---|---|---|
| **K31-i-a** | **Skip-induced wave drift destroys the carry's same-wave L2 sharing.** Every owner CTA reads the SAME amplitude planes, and the design pays for that by having all owners resident on the same window at the same time: 512 re-reads of one token's runs become one DRAM fetch and 511 L2 hits. Measured dense, that is a 96.7% L2 hit rate at 23 GB/s — 2.5% of DRAM peak. Under CLUSTERED (cohort) routing each owner is live in only 29–46% of windows, so owners that take the whole-window skip race ahead along the token axis while busy owners lag; the CTAs de-phase, the shared working set dissolves, and the receipt reads L2 50.1%, DRAM 3.8 GB (4.2× MORE traffic for 3.4× LESS work, host-counted), tensor pipe 6.5%, long-scoreboard 58.6%. The cell is 36% SLOWER than the scattered one it strictly dominates in work. The fixed global window grid removed the per-CTA super-chunk drift; it does not remove skip-induced drift, and the architecture doc does not name this. **The fix is phase coherence, not work reduction** — candidates: a grid-wide per-window barrier (cooperative launch); a cooperative per-window liveness scan whose result is shared rather than recomputed per owner; or a skip that advances the owner without advancing its stream position. This sits directly on K33's path, since bucketing assumes the cohort case is the good one. | `workflows/k31-integration/FINDINGS.md`, [`internals/carry/carry_kernel.md`](internals/carry/carry_kernel.md) **MOSTLY DISSOLVED BY K31-i-d, MEASURED 2026-08-20 (reported, not chased).** The collapse's COST was dominated by the per-(owner, window, token) amplitude re-reads the dissolved L2 window multiplied; with the ballot table wired the clustered cells went from the carry's WORST case to its BEST -- combined prefill `cohort=64` `k_tok=4` 12.3 -> 3.4 ms and `k_tok=16` 14.0 -> 4.9 ms, same session, same card, device stamp verified on both binaries. The DE-PHASING itself is untouched and no phase-coherence machinery was built; what remains of this row is the drift, not its old price. Re-measure before designing a fix. |
| **K31-i-b** | **The window lever did not end the intra's bandwidth-boundedness; the output tile is the one that can.** The architecture doc sized `W` from the intra's ideal `W/4` flop/byte intensity and expected `W >= ~380` to clear sm_86's 94 flop/byte balance. Measured at `W = 512`: DRAM 500–525 GB/s (~56% of peak) and tensor pipe 17.3%, against 51%/12.5% at `W = 256`. The move helped by ~40% on the tensor pipe and did not finish the job, because the tile RE-READ factor grows with `W` too — 2.5× at 256, 3.5× at 384, 4.5× at 512 — and measured DRAM read is 4.8× the ideal, which is the re-read factor and nothing else. The `C = 128` output tile cuts re-read rather than raising ideal intensity, so it is now the lever that matters. Gated on its register census. | [`internals/intra/intra_kernel.md`](internals/intra/intra_kernel.md#where-the-time-goes-sm_86-measured) |
| **K31-i-c** | **CLOSED FORWARD, OPEN REVERSE (2026-08-20).** The level width is an intra instantiation axis and both families carry `(16,16,16)` `D = 3` and `(8,8,8,8)` `D = 4` at `BC = 128`, so the forward `rola.ops.prefill` has a depth-three and a depth-four row and both are gated against `naive_rola`. The census that gated it: zero spill and zero local ops at every new arm, and the deep carry arms are LIGHTER than the flat ones (149-167 registers against 163-192). What remains is the REVERSE pass: both reverse families are built at depth two, `prefill_autograd` refuses depth by name, and the obstruction is two `static_assert`s on the digit reduction's row and warp maps (`internals/carry/backward_kernel.md`). The `(64, 64)` arm is still an inter-only conformance cell -- its `B = 64` has no intra partner. | [`internals/intra/intra_kernel.md`](internals/intra/intra_kernel.md), [`internals/carry/box.md`](internals/carry/box.md) |
| **The facts plan takes its spans** | **CLOSED (2026-08-20).** The facts plan now TAKES its per-level spans (`SpanPlan<D, B, s_0..s_3>`; `LevelPlan<D, B, BC>` is the lex policy as an alias) and `chunk_facts_tables` is templated on the plan type, so the lattice's spans `s_l = (8, 16)` are expressible and are built (`launch_span_facts_arm`). The carry's liveness pass consumes the table: a WARP's thirty-two tokens are one ballot word, so the conjunction over levels is `D` word ANDs per side for the whole warp, where the amplitude scan it replaces read `2 * sum_l s_l` bf16 per TOKEN per owner. The rename it was blocked on cost 48 mangled names and moved NO body (`tools/sass_bodies.py`, master vs the new build: 48 renames, all byte-identical; 0 shared entries moved). | csrc/rola/src/facts/facts_kernel.cuh (deleted with the family, card C), `csrc/rola/src/carry/carry_kernel.cuh` |
