# `DELETIONS.md` — the ledger

Every removal of code from this tree gets a row here: what went, the commit that
removed it, and one line on why it was safe. Deletions land in **deletion-only
commits** — never mixed with a move, a rename or a rewrite — so that this ledger
and `git log` say the same thing, and so a reviewer can read a deletion without
reading a refactor. The SHA is stamped in the immediately following commit, since
a commit cannot name itself.

The ledger exists because a codebase that cannot delete accumulates plausible
code nobody dares touch. Recording the deletion is what makes deleting cheap.

**A row is written once and left alone.** Rows below cite files, sections and
mirror docs that later deletions removed -- `consumer.md`, `smem_layout.md`,
`host_api.md` and the rest of the tiled consumer's mirror tree among them (P67
D2). Those citations are not repaired: a row describes the tree AT ITS COMMIT, and
editing it to match a later tree would destroy the only record of what was true
when the decision was made. Each row carries the SHA that revives what it names.

## What must be recorded

1. Code removed from `csrc/`, `rola/`, `tools/`, `benchmarks/` or `tests/`.
2. A **capability** removed — an axis, a mode, a kernel arm — even if the code
   was already unreachable.

## What must NOT be deleted, and is recorded here instead

Implemented, tested, oracle-gated machinery that the planner does not currently
select is **dormant by plan**, not dead. Deleting it deletes a measured-and-kept
capability, and it is exactly what a stranger reading the tree will mistake for
dead code. The [dormant register](#dormant) below is the answer to that mistake.

---

## Deletions

| Date | Commit | What | Why it was safe |
|---|---|---|---|
| 2026-09-16 | (this commit) | **THE DUAL-RUN PROTOCOL'S TOOL.** `tools/oracle_dual_run.py` (723 lines: the OLD side as a throwaway worktree of `--against`, `bind_library` proving which tree each side imported, a matrix generated in the file for both sides, `emit_producer`/`emit_oracle`, `compare` under bit-identity or the producer contract, `--mutate` for non-vacuity, `MINIMUM_COMPARISONS`). | **EVERY CLAIM IT MADE IS A TARGET NOW.** The DIFF NODE (`rola_devtools.diff`, ruled 2026-09-15) is two sides over shared cell nodes under a named rule: the surfaces are `tests/oracle/sides.py` (the same quantities, the empty tensor for "no gradient"), the matrix is the central `producer.json` cells and the oracle-tier carry cells (`declare.py`'s `SURFACES`), the old side is a second checkout rola-bench composes (`declare.py:jewels`), the binding proof is `binds="rola"` and the same-function-same-file refusal, the count is `minimum`, and `expect="different"` is `--mutate`. What it also does that the tool could not: the carry kernel against the fp64 reference INSIDE one checkout (`rola/diff/carry-vs-oracle`, per-slot under the output kinds' clauses), stored per cell. |
| 2026-09-16 | (this commit) | **THE `pipe_timeline` RECORD, and the tool's own storing of it.** `tools/pipe_timeline.py`'s write to the `pipe_timeline` location (the series, the summary and the scale of one capture) and the half of `--no-record` that suppressed it | REPLACED by the declared `timeline` target: an instrument is a target of `declare.py` and a store target beside it writes the record, so the tool writing its own put the same measurement in two places under two keys. The tool still prints its reports, writes `--json` and the HTML chart, and still stores a CALIBRATION at `pipe_timeline.scale`, which it reads back as every later run's scale. The deleted location's records were removed with the other pre-declared ones (rola-results 68430de). |
| 2026-09-15 | (this commit) | **THE FLA CROSS-CHECK, the oracle's one outside anchor.** `tests/oracle/test_oracle_vs_fla.py` (the renaming onto `fla.ops.gla.naive`'s `naive_recurrent_gla`, its four arms and their measurements), the `environment.fla_crosscheck` dev-config key, and the two output kinds it graded (`tests/oracle/tolerances.py`'s `FLA_OUTPUT`, `FLA_STATE`, `FLOAT32_FLOOR`, and `Output.measured`, which existed for them alone) | RULED by Blake, 2026-09-15: "Delete the fla anchor", on the standing objection to parts of the build that are on or off. It was opt-in (flash-linear-attention is not a standing dependency), and a default-off gate reports a skip where it should report a failure -- it sat unrunnable for ten days in August when the `raw` normalization died under it. What it checked, that the oracle's recurrence IS the gated linear-attention loop, is now held by review of the eleven lines it is written in and by the mutants its own tier plants; the renaming it recorded and its measurements (worst 2.98e-08 at adoption, fla 0.4.2) are in this repository's history at `293c759`. |
| 2026-09-15 | (this commit) | **THE COMPARISON AND THE MEASUREMENT SERVICE'S REGISTRY.** `tools/compare.py` (`rola_arm`, `foreign_arm`, `point_of`, `compare`), its mirror page `docs/internals/tools/compare.md`, `benchmarks/registry.py` (`RolaBuild`, `Clock`, `Tool`, `Timeline`, `Subject`, `registry`), `tests/unit/test_measurement_registry.py`, and `tests/unit/test_bench_provider.py`'s two comparison tests | REPLACED, by Blake's ruling of 2026-09-15 (a declared build system: targets declared by files, composed by a root). The repository root's `declare.py` declares what the registry registered -- the build, the clock reader, every instrument and every bench subject as a timing entry -- as targets of rola-devtools' `rola_devtools.build`, run by `benchmarks/executors.py`; an interleaved comparison is a timing session (`rola_devtools.timing`'s `measure_timing`) over entries of any checkouts a root registers, which rola-bench's root composes. `tests/unit/test_declare.py` holds the declarations; the method's refusals are rola-devtools' `tests/test_timing.py` and `tests/test_declared_build.py`. |
| 2026-09-15 | (this commit) | **THE CELL REGISTRY'S DATA AND DRAWS, AND THE MEASUREMENT GRAPH, MOVED TO ROLA-DEVTOOLS.** `benchmarks/cells/carry_cells.json`, `layer_cells.json`, `regimes.py` (the six regime axes and their proofs), `registry.py` (`CELLS`, `GROUPS`, `registry`), the draws of `benchmarks/cells/__init__.py` (`simplex`, `clustered`, `_degenerate`, `_corner`, `realize`, `CellSpec`, `carry_cell`, `by_name` over the file) and of `layer.py` (`LayerCellSpec`, `layer_cell`, `layer_cells`); `benchmarks/graph.py` (`Instrument`, `Timeline`, `Arm`, `Memory`, `graph`, `hold`); `bench.subjects.STATE_ARMS` and the `@state` arm dial; `tests/unit/test_cell_regimes.py`, `tests/unit/test_measurement_graph.py` | A MOVE, not a loss (Blake, 2026-09-15: one central registry, the inverted measurement system). Every cell, its draw and its regime proof are rola-devtools' `rola_devtools.cells` since `5d3d9a5`, each of the 73 inputs drawn bit-identically there before the move, with the regime tests beside them; `benchmarks/cells/` is now rola's reading of those cells (descriptor, launch, `carry_call`, the state a cell's `state` and `backing` bind) and `layer.py` RoLA's constructions, named as the layer cells were. The graph's units are `benchmarks/registry.py`'s registrations with `rola_devtools.measure`, which measures memory itself. The `@state` dial left because the state is the cell's: `carried` binds its entry, `paged` its slot table (`readonly` had no cell and goes). `flagship-alt-k4-w4` goes as a cell: its input is `flagship-alt-k4`'s and no binary carries (2, 64, 4). Removed in the commit that moved their callers, not a deletion-only one: the gate's dead-code ratchet refuses the unreferenced files. The carry oracle, prefill oracle and paging tests fail and pass exactly as before (the same standing reds of unbuilt arms, and the paging file's standing timeout), less the w4 cell's one row |
| 2026-09-15 | (this commit) | **THE MACHINE'S LOCKS, MOVED TO ROLA-DEVTOOLS.** `tools/gpu_lock.py` (`gpu_lock`, `GPU_LOCK_DEFAULT`, `GPU_SHARED_SLOTS`), `tools/host_budget.py` (`acquire`, `file_lock`, `lower_priority`, `trace`, `HOST_BUDGET_SLOTS` and its CLI), `tools/clock_lock.py` (`load`, `within`, `engage`, `TOLERANCE`), their mirror pages `docs/internals/tools/gpu_lock.md` and `host_budget.md`, `tests/unit/test_locks_accounting.py`, and the three lock-held markers in `tools/dev_config.py`'s `HANDOFFS` | A MOVE, not a loss: the same mechanisms, their tests (`tests/test_locks.py`, with the GPU lock's priority proof) and their reentrant markers are rola-devtools' `rola_devtools.locks.{gpu,host,clock}` since its `91f3ea1`, and `089bf7f` switched every caller here to them (the module-level `GPU_LOCK_DEFAULT` and `HOST_BUDGET_SLOTS` became settings read when a lock is taken). Nothing in the tree imports the deleted files; the gate's lint, `docrefs` and the lock tests ran without them |
| 2026-09-14 | `48fb59d` | **UNCALLED DEFINITIONS OUTSIDE THE ORACLE TIER.** `rola/routing/topology.py`'s `resolve_b_spec` and `canonical_radix_strides`; `tools/gen_shards.py`'s `carry_members_sha256` and the `CARRY_ARM_DOMAINS` alias; `rola/ops/carry.py`'s `MAX_LEVELS`; `benchmarks/bench/carry_model.py`'s `L2_LATENCY` and `SMEM_BW`; `tools/lint/r9_enforcement.py`'s `ELSE_RE`; `tools/compose_ledger.py`'s `PHASE_NAMES`; `tests/integration/stream_discipline.py`'s `watch_extension`; `tests/unit/test_exchange_law.py`'s `_canonical`. | **EACH NAME OCCURS ONCE IN THE TREE, AT ITS DEFINITION** (`git grep -w` over every tracked file, setup.py and the docs included), from vulture's last inventory read before the lint was deleted (c01e013). The oracle tier's uncalled helpers (`fixtures.py`'s `require_arm`, `ref_levels`, `state_in_plane`; `families.py`'s `instantiation_values`, `runs_on_kernel`; `instantiation.py`'s `AXIS_BY_NAME`; `generators.py`'s `build_case`; `oracle_fixtures.py`'s `_output_charge`) and `stream_discipline.py`'s `assert_agrees_under_launch_blocking` are NOT here: build.md, testing.md and the oracle-tests skill describe them as the tier's mechanisms, so they wait for Blake. Revival = git (this row's commit, parent). |
| 2026-09-14 | `6772313` | **THE CONFORMANCE FAMILIES, whole.** `tests/oracle/families.py` (`Family`, `REGIME_AXES`, `FAMILIES`, `FAMILY_BY_NAME`), `tests/oracle/generators.py` (`Case`, `build_case`, the regime `CHECKERS`, `CORNER_CHECKS`, `assert_nonvacuous`, `assert_swept`, `relative`), `tests/oracle/instantiation.py` (`CHUNK_ARMS`, `BUILT_TOPOLOGIES`, `CHUNK_TOKENS`), `tests/oracle/test_family_meta.py`, `tests/oracle/test_producer_families.py`. The stratified sweep over `BUILT_TOPOLOGIES` and the imbalanced-heads family are not carried over: the registry's topologies are its cells, and a registry cell is one stream. The chunk-boundary corner became the window-flip cell (the carry kernel's seam is its window, not a 32-token chunk). | **FOLDED INTO THE CELL REGISTRY** (Blake, 2026-09-14: the carry kernel's oracle tests taking their inputs from the registry "is great"): every carry cell states its coordinates on `benchmarks.cells.regimes.REGIME_AXES` and `realize` proves them; the named corners are cells (`corner-*`), frozen by `tests/unit/test_cell_regimes.py`, which also proves each check's teeth and that the registry covers every regime value; the producer's gain sweep is the layer registry's `producer-sweep` group (`tests/integration/test_producer_sweep.py`). No kernel ran the families since their runner left with the chunk consumer (675d6b6). Revival = git (this row's commit, parent). |
| 2026-09-14 | `c1e8660` | **THE GLOBAL AND PER-SEGMENT ORACLE GRADERS.** `tests/oracle/oracle_fixtures.py`'s `_output_charge`, `_segments` and `_OUTPUT_SEGMENTS`; `tests/oracle/fixtures.py`'s `relative_per_token`; `tests/oracle/test_decode_vs_oracle.py`'s `DECODE_RTOL` alias; `tests/oracle/test_intra_kernel.py`'s `_relative`; the global logit floor as the projection gate's assertion (`_logit_floor` stays, the amplitude envelope reads it). They go with the rule that replaces them, in the commit that moves their callers (a ratchet refuses an uncalled definition). | **EVERY CALLER NOW GRADES PER SLOT** (Blake, 2026-09-14: "each oracle vs real should be checking per slot"): `tests.oracle.fixtures.assert_slots_close` holds each slot to `BF16_RTOL` times its envelope, and each gate's planted errors fail under it. `git grep` finds none of the names outside docs/testing.md, which the docs pass rewrites. Revival = git (this row's commit, parent). |
| 2026-09-14 | `47ad119` | **THE ORACLE TIER'S UNCALLED HELPERS.** `tests/oracle/fixtures.py`'s `Cell.ref_levels`, `Cell.state_in_plane` and the `g_bf`/`v_bf` planes only they read; `families.py`'s `Family.instantiation_values`, `runs_on_kernel` and `refusal`; `instantiation.py`'s census axes (`AxisDeclaration`, `AXIS_DECLARATIONS`, `AXIS_BY_NAME`, `instantiation_of`), its kernel-envelope mirror (`kernel_refusal`, `REFUSAL_CLAUSES`, `BUILT_DV`); `tests/integration/stream_discipline.py`'s `assert_agrees_under_launch_blocking` and its rows in the module table and `testing.md`. | **NOTHING CALLED THEM (Blake, 2026-09-14: "we should delete anything that is unused and has no clear purpose, and refresh docs, if we need it again, we will just make it again").** The census join that read the axes and the envelope mirror was deleted with the chunk consumer (675d6b6); the oracle reads a registry cell's rounded operands through `RealizedCell.doubles()`, not `ref_levels`; no stream-crossing path ran the launch-blocking comparison. The oracle-tests skill now describes registry cells. `generators.py` (`Case`, the regime checkers, `assert_nonvacuous`) is NOT here: it lost its kernel runner in the same deletion and waits for Blake. Revival = git (this row's commit, parent). |
| 2026-09-14 | `c01e013` | **REVERSED the next commit** (Blake, 2026-09-14: "shouldnt we keep work_codes, so we dont add anymore in the future? and i want to ahve some system to get rid of unused code, even if imperfect"; the three return and gate on NEW findings). **THREE REPORT-ONLY LINTS.** `tools/lint/run_vulture.sh` with `vulture_whitelist.py` and its fixture (Python dead code, 51 findings), `tools/lint/constant_parameters.py` with its two fixtures (a knob only ever given one value, 144 findings), `tools/lint/work_codes.py` (campaign work codes in prose, 611 findings); their pre-commit hooks, their CI lines, their rows in `lint.md`, and `vulture` from the `dev` extra (its pins in `requirements.in` and `requirements.lock` leave in the next environment commit, which needs a passing container check). | **A CHECK THAT CANNOT FAIL IS NOT A GATE (Blake, 2026-09-14: "ok to delete").** Each printed an inventory and exited 0 on every commit and in CI; none was ever flipped, and the inventories grew. (This row said the work-code law kept a gate in `lint_standards.py`; it did not -- `check_no_campaign_work_codes` is defined there and only `work_codes.py` ran it.) `drift_guards.py` and `r9_enforcement.py` are NOT in this row: KERNEL_STANDARDS' R9 addendum requires a device switch on a geometry field to be a lint finding and R19 names the drift guards, so whether they gate or go is Blake's. Revival = git (this row's commit, parent). |
| 2026-09-14 | `ee845e6` | **THE PROBE ORCHESTRATOR.** `tools/probe_cells.py` (the two-binary A/B over registered benches: its worker, interleaved rounds, ncu pass and `probe_cells` record, with `rola_file_in` and `portable_error`), `tests/unit/test_record_paths.py` (which tested those two), the `rola-probe` skill, and the passages naming it in the benchmarks README, the cells module, `ESSENTIALS.md`, `sm_clock.md`, `gpu_lock.md`, `phase_ledger.md`, `measurement.md`, `setup.md` (its container step now runs `tools/compare.py`), `testing.md`, `gpu_lock.py`, `sanitize_oracle.py`, `conftest.py` and the brief skill. | **`tools/compare.py` REPLACED IT (Blake, 2026-09-14, P82 step 20).** A comparison of checkouts is arms of rola's runner on a point's cells, interleaved by rola-devtools' driver under the GPU and clock locks, and the suite times through it; the probe was a second definition of that run. Its one live use, the untimed launch the profilers wrap, moved to rola's runner first (`bench.provider`'s `oneshot_argv`, 982c4ca), checked by pipe_counters reading the stored HMMA count. The row checks the two helpers made (a `rola` imported from outside the checkout, a message carrying machine paths) live on in the runner's arm build and in `rola_results`. The store keeps the one `probe_cells` session. Revival = git (this row's commit, parent). |
| 2026-09-14 | `8e44830` | **THE BUILD LEDGER AND THE DASHBOARD.** `tools/build_ledger.py` (one run of every measurement a build is judged by, diffed against the previous build's report), `tools/dashboard.py` and `tools/dashboard_page.html` (a static page over the `build_ledger`, `compose_ledger`, `pipe_timeline` and `probe_cells` stores), their mirror pages, `compose_ledger.py`'s call to render the dashboard, and the passages naming them. | **THE SUITE REPLACED THE LEDGER, AND THE DASHBOARD READ ONLY WHAT THE LEDGER ERA STORED (Blake, 2026-09-14, P82 step 20).** rola-bench's measurement suite runs the same instruments (sass, registers, phases, counters, census, timeline) per build, keyed and stored under `suite/<module>`, with the regression verdict; the ledger's steps were a second definition of that run. The dashboard never read `suite/*`, and the store it read holds one smoke probe session, so it rendered nearly nothing. KERNEL_STANDARDS section 22's addendum names the suite as the decision tree's trunk. A dashboard returns as a view over rola-results (P82 18a). Revival = git (this row's commit, parent). |
| 2026-09-14 | `4c55b6c` | **TWO UNCALLED TOOLS.** `tools/field_coverage.py` (the sweep of the carry geometry's generated sub-box set over both `warps_per_cta` values) and `tools/experiments/dram_by_activity.py` (the DRAM counters with the activity byte controlled), its mirror page `docs/internals/tools/dram_by_activity.md` and the directory; the passages in `geom.md`, `measurement.md`'s store table and the probe skill that named them. | **NO CALLER, NO HOOK, NO CI JOB (Blake, 2026-09-14, P82 step 20).** The sub-box set's enumeration, de-duplication, count and arch-law assert are `tests/unit/test_carry_geometry.py`'s, which run in the battery; the sweep restated them by hand. The DRAM harness reproduced one finding (read-only atoms at 0.657x DRAM, journal G1) on the kernel P81 rebuilt, and nothing ran it after; its stored rows stay in the store. Revival = git (this row's commit, parent). |
| 2026-09-14 | `936322e` | **THREE UNRUNNABLE BENCHES.** `benchmarks/bench_scaling.py` (the whole layer against flash attention over `L`), `benchmarks/bench_layer.py` (the whole layer at the Stage 0 matched cell, per plane) and `benchmarks/bench_paging.py` (resident pages against `N`), and the README passage naming them. | **EVERY ONE REFUSED BEFORE IT TIMED ANYTHING (Blake, 2026-09-14, P82 step 20: "ok to delete").** The first two call `RoLA.forward` over a prefill, and `rola_op` refuses every prefill (`rola/interface.py`); the paging bench builds its arena through `rola.engine.facts.call.arm_of`, which asks the extension for the deleted `chunk_arms` binding. Their docstrings describe the retired consumer (planner planes, `v3_consumer.cu`). The whole-layer comparison is rebuilt as layer graphs on the bench's build system (P82 steps 18a and 18), and residency is measured again when the carry takes a paged plane (P80). Revival = git (this row's commit, parent). |
| 2026-09-14 | `7ccb492` | **THE PARKED CELL RECORDS AND THE LAYER MANIFEST.** `benchmarks/cells/topology_manifest.json` (the tiled consumer's cell statistics) and `latency_baseline.json` (its per-arm medians, RTX 3080 Ti, CUDA 12.4), with `benchmarks/cells/README.md`, which described only them; `benchmarks/cells/layer_manifest.json` and `layer.py`'s `check_manifest`, `build_manifest`, `write_manifest`, `cell_manifest` and `realized` (the layer cells' integer support statistics and their comparison), and the README passages naming them. | **NOTHING READ THEM (Blake, 2026-09-14: "that should be deleted then").** The two parked files described launches of a kernel deleted on 2026-08-20; no gate read them and their own README forbade comparing a current number against them, and the record of what was measured belongs in the store. `check_manifest` was a gate nothing called, so the layer cells' statistics had not been checked since the layer's prefill was unwired; a realized-support statistic returns with the layer rows that record it (P82 step 18). Revival = git (this row's commit, parent). |
| 2026-09-14 | `d1e36c7` | **`benchmarks.cells.registry.runnable()` and its unit case.** | **ROLA'S RUNNER ANSWERS IT, PER CELL.** `bench.provider:arms` refuses a binary lacking a shipped arm and a carry cell whose arm the binary does not carry, by name, and offers a subject only where the binary carries its intra or decode kernel too -- the families `runnable()` never checked; rola-bench's suite asks the runner through the driver's `accepts` (rola-bench afb2a50), and `tests/unit/test_bench_provider.py` plants the same refusals. Revival = git (this row's commit, parent). |
| 2026-09-14 | `0104fa0` | **THE IN-PROCESS BENCH METHOD, whole.** `benchmarks/bench/driver.py` (one driver's body and its `bench_driver` record), `bench/pairing.py` (the in-process interleaved A/B engine and `measure_arm`), `bench/discipline.py` (`disciplined`: the lock, the settle, the tree and extension identity, the tier), `bench/stats.py` (the paired signed-rank verdict and the persistence rule), `bench/regression.py` (the three-gate `classify`, `threshold_ms`, the instrument refusal, `settle_clocks`), the five unit drivers `benchmarks/unit/bench_{carry_forward,decode_step,entmax,intra_forward,liveness}.py`, and their gate `tests/unit/test_perf_ledger.py`. | **TWO MEASUREMENT METHODS WERE ONE TOO MANY, AND THE OTHER ONE CROSSES PROCESSES AND LIBRARIES.** `tools/compare.py` times the same subjects (`bench/provider.py` builds each one from `bench/subjects.py` over the same registry) through rola-devtools' interleaving driver: one worker per environment, a warmup floor, a fresh random order per rep, one stopwatch per comparison refused otherwise, the GPU lock and the host clock lock (which supersedes settling) held by the tool, the device stamp and the checkout's own `import rola` proven per arm. The statistics moved whole to `rola_devtools.verdict` with their planted-violation tests; which samples are a baseline is `python -m rola_results verdict`, a query over stored sessions. `benchmarks/cells/registry.py` and `subjects.Launch` moved out first (`bee9485`), so nothing that stays imported these files. The `bench_driver` records stay in the store. Revival = git (this row's commit, parent). |
| 2026-09-14 | `f0d97f8` | **THE `attention_reference` SUBJECT.** `benchmarks/bench/subjects.py`'s `attention_reference` and its roster entry. | **THE REFERENCE MOVED TO THE BENCHMARK REPOSITORY, WHERE COMPARISONS WITH OTHER LIBRARIES LIVE.** rola-bench's `rola_bench/measure/attention.py` is the same measurement (one head of width `dv` over the cell's tokens, bf16, causal, torch's forced flash backend) as a provider of the interleaving driver, and `tools/compare.py` runs it as a foreign arm beside rola's arms at one cell, under one lock and one stopwatch; rola's bench roster names only rola's kernels. Revival = git (this row's commit, parent). |
| 2026-09-14 | `f59571a` | **THE MIRROR TOOL'S COPY IN THIS TREE.** `.github/mirror/mirror.py` (the export and its tripwire), `.github/mirror/README.md` and `tests/unit/test_mirror.py`. | **IT MOVED, WHOLE, TO ROLA-DEVTOOLS.** Five repositories carried identical copies; `rola_devtools.mirror` is the one, with the same declarations format and tripwire and its tests beside it. This tree keeps `.github/mirror/declarations.json`; the commit gate runs `python -m rola_devtools.mirror --check` from the base venv, where `tools/dev.py init` links `workspace.devtools`, and CI and the mirror workflow install rola-devtools at a pinned commit. Revival = git (this row's commit, parent). |
| 2026-09-14 | `185dfe3` | **THE `flash-attn` PACKAGE AS THE ATTENTION REFERENCE.** `pyproject.toml`'s `bench` extra entry `flash-attn` and its comment (one library on every host, never torch's vendored backend); `benchmarks/bench/subjects.py::attention_reference`'s `flash_attn_func` call and its import refusal; `docs/measurement.md`'s sentence refusing torch's backend as a stand-in. The lock's `flash-attn` 2.7.4.post1 cu12 wheel pin leaves with the cu13 lock in the next commit. | **THE SAME KERNEL, WITHOUT A PACKAGE THAT CANNOT BE INSTALLED.** torch 2.14 compiles Dao's FlashAttention-2 kernels from flash-attention@14c3779 (2026-07-27, 543 commits past v2.8.3 and none behind), and `attention_reference` now calls them through `scaled_dot_product_attention` with the flash backend forced: torch refuses a call flash cannot take instead of timing another backend (checked: an fp32 call is refused), and the row names torch's version and the active implementation. No flash-attn release wheel matches torch 2.14 with CUDA 13, and compiling one from source exhausted the host's memory. FA3 (Hopper) and FA4 (Blackwell) register into the same call through `torch.nn.attention.activate_flash_attention_impl`, so every host still measures one baseline. Revival = git (this row's commit, parent). |
| 2026-09-13 | `60e540b` | **R1c: `IndependentRouting.tied` and `IndependentRouting.shares_solve`, whole.** `rola/routing/types.py`'s `tied` field, its `bool` validation, `at()`'s `tied=self.tied` argument, the `shares_solve` property, and the two-exact-zero-sides check's `shares_solve` escape (now an unconditional refusal naming `TiedRouting`/`UnionRouting`); `TiedRouting`'s own now-redundant `tied`/`shares_solve` properties (always `True`, so every generic call site collapsed to `isinstance(level, TiedRouting)`); `ResolvedRouting.untied_levels`'s `not level.tied` branch and `.shared_solve_levels`'s `isinstance(level, (IndependentRouting, TiedRouting)) and level.shares_solve` branch; `rola/routing/reference.py`'s `elif level.tied` asymmetric read-off-write-logits branch; `rola/routing/entmax/production.py`'s ~8 `level.tied`/`level.shares_solve` sites across both autograd `Function`s' forward/backward and the torch fallback lowering; `docs/api.md`'s `tied=False`/`tied=True` spellings. | **THE VOCABULARY IS EXACTLY THREE TYPES NOW: `IndependentRouting \| TiedRouting \| UnionRouting`.** `IndependentRouting.tied` carried two semantics: (a) `read == write` making one solve exact -- that IS `TiedRouting`, landed as its own first-class type in R1a/R1b, so the flag was a second spelling of a case a type already covers; (b) the asymmetric case -- one logit slice, two different activations solved on it -- tested (`test_tied_asymmetric_routing_does_not_share_a_solve`) but used by no shipped configuration, and deleted outright rather than migrated. `IndependentRouting` is now always untied (two logit slices, two solves): constructing it with a `tied` keyword is a `TypeError` (no compatibility shim), and every `level.shares_solve`/`level.tied` dispatch that used to distinguish independent-shared from tied-shared collapsed to a single `isinstance(level, TiedRouting)` check since only `TiedRouting` can share a solve now. Tests: cases constructing `IndependentRouting(tied=True, read=write)` became `TiedRouting`; the asymmetric cases were deleted with a one-line note (`test_routing_types.py`, `test_production_levels.py`); the equivalence tests that specifically compared `TiedRouting` against `IndependentRouting(tied=True)` (`test_tied_routing_packs_identically_to_shared_solve_independent_routing`, `test_tied_routing_is_exactly_shared_solve_independent_routing`, `test_cuda_tied_routing_is_exactly_shared_solve_independent_routing`) were rewritten to assert `TiedRouting`'s own properties directly, since the second construction to compare against no longer exists. Revival = git (this row's commit, parent). |
| 2026-09-13 | `892312a` | **THE cu124 TOOLCHAIN.** `tools/toolchains/cu124.json` (CUDA 12.4's assembler, V12.4.99, cuda_12.4.r12.4/compiler.33961263_0) and its ratification, `tools/manifests/cu124/sm_80.json` and `sm_86.json` (349 instantiations each, ratified before the pool line's last kernel deletions); README's two cu124 support rows, regenerated. | **cu13 REPLACES IT, MEASURED.** fb86196 ratified CUDA 13.0 over the same source; the census of every candidate showed no entry gaining spill or in-loop locals, CUDA 13.0 times at parity with 12.4 on identical source (P82 step 3), and the one entmax cost of the move is the CCCL-3 port's, accepted and carded (K54). torch's CUDA 12 wheels stop at the 12.x line the environment leaves, and fla 0.5.2 needs torch >= 2.7, which cu124 wheels never shipped. A build under the 12.4 toolkit now refuses at setup, naming the declared toolchain. The dev container and `requirements.lock` still describe 12.4 until P82 step 5 rebuilds them. Revival = git (this row's commit, parent). |
| 2026-09-13 | `ac88752` | **THE `[fla]` EXTRA AND THE `[transformers]` ONE THAT NEVER EXISTED.** `pyproject.toml`'s `fla = ["fla-core"]` optional dependency and its comment; `README.md`'s install-modes row advertising `rola[transformers]` / `rola[fla]`; `docs/build.md`'s sentence placing optional interop behind those extras. | **NOTHING INSTALLED THROUGH THEM WAS USED.** `[fla]` served `use_short_conv=True`, the fused short-conv path the conv hooks carried, and those hooks were deleted on 2026-08-29 (the row below); no module under `rola/` imports `fla`. `[transformers]` was never declared in `pyproject.toml`, so the README row named an install that failed. FLA's RoLA layer and HF model live in the fla fork's `rola` branch (P82 step 5b), which depends on `rola`, never the reverse. `requirements.in`/`requirements.lock` still pin `fla-core==0.4.2` for the dev image until the cu13 lock replaces them (P82 step 5). Revival = git (this row's commit, parent). |
| 2026-09-12 | `ec72e5a` | **THE MEASUREMENTS RECORD'S OWN WRITERS AND INDEX.** `benchmarks/bench/store.py` (the K38 run JSONL writer: `build_run`, `build_result`, `write_run`, `results_dir`, `git_state`, `REQUIRED_RUN_FIELDS`), `benchmarks/bench/ledger.py` (the perf ledger: `make_row`, `make_legacy_row`, `fingerprint`, `append`, `load`, `baseline_series`) with its schema and fingerprint tests in `tests/unit/test_perf_ledger.py`, the driver's ledger append (`_row`, `--no-ledger`) and `Discipline.row`, `tools/rola_db.py` (the sqlite index and its `last`/`history`/`compare`/`baseline`/`regressions`/`sql` queries), `bench.hw_profile.machine_tag` with `environment.machine_salt`, `store.db`, `dev_config.store()`, `tools/dev.py`'s `STORE_FILES`, `ensure_store`, `store import` and `_record_dir`, `tools/pipe_timeline.py`'s `scale_dir`, and the dashboard's join of rows from before the session id (`SESSION_GAP_S`, `seconds`). | **Every writer stores through `rola_results` since `8162043`** (Blake, 2026-09-12: "every tool stores through rola_results, each in its own location"; the old timing history goes to the owner's scratch archive, not converted), and the A/B samples the ledger kept are in the driver's stored result since `845ffa0`. The regression judgement (`bench.regression.classify`) and the statistics survive, tested on plain rows (`e547c5d`); nothing calls `classify` outside its tests, which is now the open question of a reader over `bench_driver` records rather than of storage. The index's queries have no replacement yet beyond `python -m rola_results show` and the dashboard. `git show 845ffa0` has all of it. |
| 2026-09-12 | `b729e70` | **`prefill_op_chunked`**, the bench subject that priced a cell's sequence as four carried calls (`benchmarks/bench/subjects.py`: the function and its roster entry). | **`prefill_op` at `calls=4` is the same measurement since `28d5dae`**: one body builds both counts, and on `nl64k-alt-k4` the four-call arm hands `rola.ops.prefill` the operands and plane bindings `prefill_op_chunked` did, captured call by call and compared exactly. Its rows stay readable under their own name; a tree from before the count is timed through the suite's lane for it (rola-bench `PRE_COUNT_BENCHES`). `git show 28d5dae:benchmarks/bench/subjects.py` has it. |
| 2026-09-12 | `515063c` | **THE MEASUREMENTS IN THIS REPOSITORY.** `benchmarks/results/` (its README and the 762 committed record files, `<hw-profile>/<yyyy-mm>/<run_id>.jsonl`, with the card's calibration and pipe-timeline scale rows), `perf/ledger.jsonl`, `perf/legacy_measurements.json`, and `.gitattributes` (its only lines were the perf ledger's union merge). | **They moved, byte for byte, into the measurements store** (`store.root`, its own git repository, commit `a6ba88a` there; the writers point there since `d0797ef`), which also holds every row the other worktrees had and this one lacked; the store's `.gitattributes` carries the union merge. Every file deleted here was verified present in the store before the deletion. `git show d0797ef:<path>` revives any of them. |
| 2026-09-12 | `ae55b5c` | **`docker/verify_sccache.py`**, the image build's download-and-verify helper for the pinned sccache. | **The image installs its pins through `tools/dev.py init --image` since `def0c3f`**, from `tools/sccache_pin.json` and `tools/mold_pin.json` with the same tarball and binary sha256 checks, the same command a bare host runs; nothing copies or runs the helper. `git show def0c3f^:docker/verify_sccache.py` revives it. |
| 2026-09-12 | `a750b7a` | **THE SCATTERED SETUP SCRIPTS.** `tools/build/new_worktree.sh` (the pointer-venv worktree, driven by `ROLA_BASE_VENV`, `ROLA_WORKTREES_DIR`, `ROLA_WORKTREE_BUILD`), `tools/setup_clock_lock.py` (the one-time clock-lock host setup, `ROLA_CLOCK_CONFIG`) and `tools/build/build_lock.sh` (iteration/gate build slots on `/tmp/rola_build_slot_<i>.lock`, `ROLA_BUILD_SLOTS`, `ROLA_NO_NICE`). | **Each is `python tools/dev.py` now, and one was a defect.** `dev.py worktree` and `dev.py clock` carry the same sequence and refusals, reading the dev config (`0408e73`). `build_lock.sh` kept its OWN slot pool beside the host budget `setup.py` draws from, so a census compile under it and a build under `setup.py` never blocked each other; its two callers, `tests/unit/test_arm_switch.py` and `test_structure_switch.py`, run under `python tools/host_budget.py --` since `0408e73`. `git show 0408e73^:tools/build/new_worktree.sh` revives any of the three. |
| 2026-09-12 | `c94f8c9` | **THREE ORPHANED `tools/` SCRIPTS.** `tools/chunk_sass_gate.py` (the SASS gate for the deleted `chunk` kernel family: `cuobjdump -sass` extraction plus the `LDL`/`STL`/`ATOM`/`IMAD.WIDE`/`I2F`/`F2I`/`MUFU` regex checks, keyed to `chunk_arms.inc`/`chunk.cu` namespaces that no longer exist). `tools/run_subjects.py` (zero callers anywhere in the tree; a thin CLI wrapper over cell selection and subject execution). `tools/ledger_import.py` (a one-time migration of the banked markdown-era ledger rows at `probe_version: 0`, already run; its provenance is recorded in `docs/measurement.md#legacy-rows`). | **NO LIVE SUBJECT, NO CALLER, ALREADY RUN.** `chunk_sass_gate.py`'s subject (`csrc/rola/src/chunk/`) is gone (P67 D2-c, row above); the current families (carry/intra/decode) are gated by `tools/sass_gate.py`, which is not a drop-in replacement of the old instruction set (it checks local memory, convergence subroutines, predicated HMMA and instructions-per-HMMA, not the old `ATOM`/`IMAD.WIDE`/`I2F`/`F2I`/`MUFU` set) — a narrower gate over a narrower kernel family, not a renamed one. `run_subjects.py` was a wrapper over `tools/probe_cells.py`'s cell/subject machinery with no caller left pointing at it (`grep -rn "run_subjects"` across `.py`/`.md`/`.sh`/`.yml` returns only its own file). `ledger_import.py` imported the legacy rows once; the rows it wrote are permanent ledger data, not a live dependency on the script. Revival = git (this row's commit, parent). |
| 2026-09-04 | `beb7001` | **THE REVERSE PASS AS IT STOOD: the intra backward kernel and the prefill autograd node.** `csrc/rola/src/intra/intra_backward.cu`, `intra_backward_kernel.cuh`, their bindings (`intra_backward`, `intra_backward_arms`, `intra_backward_stamp`), `rola.ops.intra.intra_backward`/`backward_arms`/`backward_stamp`, `rola.ops.prefill._Prefill`/`prefill_autograd`, `tests/oracle/test_intra_backward.py`, `tests/oracle/test_prefill_backward.py`, `tests/integration/test_backward_ctx.py`, `tests/oracle/test_carry_backward_vs_reference.py` (which named a reverse-scan stamp no binary produces), `docs/internals/intra/intra_backward*.md`. | The forward is rebuilt at the position carve (`carry_kernel.md#readout`) and the intra is to be rebuilt on the same form; a reverse pass written against the old orientation and the old fan-in would be a second seam to keep in step. What remains is what the new reverse pass is built TO: the fp64 backward reference (`tests/oracle/backward_reference.py`, gated by `test_prefill_backward_reference.py`) and the carry backward seam (`rola.ops.carry.carry_backward`, which refuses until it exists). |
| 2026-09-04 | `eab4056` (mixed with the position-carve rewrite, against this ledger's deletion-only rule; recorded here so the ledger and the log agree) | **THE UNION READOUT BODY AND ITS TENANTS.** `readout_phase`, `readout_produce`, `readout_mma`, `readout_deposit`, `readout_fan_in`, the claimed cover slot A (`readout_stage`, `cover_read_batch_zero`, `stage_unit_factors`, `claim_unit`, `Smem::claim`), the fan-in panel (`Smem::panel`, `kPanel*`, `panel_tiles`), the raw read ring (`Smem::read_raw_slot`, `kReadSlots`, `kRawSlotBytes`, `raw_stride`), the read-side lists, the transit's pull and store-back (`out_leg_pull`, `return_leg_store`, `Transit::sa`/`ma`), `Form::kUnion` and `CarryParams::readout_form`; then (the commit after) the form slot of the schedule ABI, `Form`, `rola.ops.carry.FORMS` and `CarrySchedule.readout`. | The position-carved readout (`carry_kernel.md#readout`) is the union point of the family at the warp-box grain and the sparse form at a finer one, one body: it measured faster than the union body at every density (dense 0.697 ms against the union body's 0.863) and the reduction count it issues is the union body's floor. The state stays in the region through PHASE 1, so nothing pulls or stores it back. |
| 2026-08-30 | `7b58698` | **`fan_in_add_shared`**, the shared-memory member of `fan_in_add` in `csrc/rola/src/common/ops.cuh`, and its `ops.md` section. | **The architecture has no such atom, and the constitution's no-fallback clause forbids the one ptxas substitutes.** `red.shared.add.f32` assembles on this target to `ATOMS.CAST.SPIN` -- a compare-and-swap RETRY LOOP -- measured by the carry readout's own instruction ledger at 4.80 M issues costing 4.80 M `FADD`, ~4.8 M `LDS` and the branch pair around them, roughly seven and a half instructions apiece and 35% of that body's whole instruction count. Its one consumer, the readout's fan-in, reduces its warps' panels with plain disjoint stores and a fixed-order sum instead (`carry_kernel.md#fan-in`), which needs no atom at all. An architecture that grows a native shared float add can have the primitive back; nothing here should get it by emulation. Revival = git (this row's commit, parent). |
| 2026-08-30 | `836d5fe` | **THE SPLIT `mbarrier` EDGE.** `EdgeState`, `edge_init`, `edge_arrive` and `edge_wait` in `csrc/rola/src/common/ops.cuh`, their `ops.md#edge` section, and the three shared-memory barrier words the carry box's ledger allocated for them. | **Its one consumer stopped needing it, and the measurement is on the record.** The carry frame's three edges were its only callers; they are NAMED BARRIERS now (`carry_kernel.md#edges`), because a named barrier cannot SPLIT for equal participation and does not have to: the cover work runs before the crossing and buys the same cover, while `mbarrier.test_wait` spins where `bar.sync` sleeps (K1 measured that spin at 21% of issue slots on the stream edges it then left). A producer/consumer edge with UNEQUAL participation would want the split back, and this row is where to get it. Revival = git (this row's commit, parent). |
| 2026-08-30 | `4a572cd` | **THE TESTS AND BENCHES OF DELETED FORMS.** `tests/oracle/test_block_bitmap.py` (the block-grain liveness reduction's oracle: its kernel and its `chunk_block_bits` binding went with the stats passes). `tests/unit/test_exchange_layout.py` (the retired carry body's transit layout, proved through the generator's Python mirrors of that body's box plan -- mirrors the same deletion removed). `benchmarks/bench_prefill.py` and `benchmarks/bench_prefill_backward.py` (the combined prefill's measured tables, written against the deleted carry bindings and the axes the surface no longer takes). `perf/certrewire/ab.py` (a paired A/B for a seam question that was settled, whose two binaries no longer exist). | **The subjects are gone, so there is nothing to re-key onto.** The liveness pass carries its own contract and kernel tests (`tests/unit/test_liveness_contract.py`, `test_liveness_pass.py`), which is the block bitmap's successor coverage; the transit's proof returns with the transit (C3). The two prefill benches are replaced by the registry's cells and the registered benches the probe orchestrates, which is the one measurement definition this stage consolidates onto. Revival = git (this row's commit, parent). |
| 2026-08-30 | `ef567f9` | **THE CUDA CARRY FAMILY, whole.** `csrc/rola/src/carry/`: `carry_kernel.cuh` (the carved body -- schedule, fetch/expansion, cooperative staging, fold, exchange, readout, fan-in ring), `carry_split_kernel.cuh` (the channel-split body), `box.cuh` (the `(k, m)` box lattice: `BoxPlan`, `ModeOrder`, `StreamSpans`, the leaf bit permutation), `carry_rows.cuh`, `carry_arm.cuh` + `carry_arm_abi.cuh` (the torch-free per-arm ABI), `carry_api.cuh`, `carry.cu` (the dispatch and its "unbuilt carry arm" refusal), `backward.cu` + `backward_kernel.cuh` (the two-scan reverse pass). `csrc/rola/src/instantiations/carry_arm_{0,1,2,3,4,5,6,8,9,12}.cu` and `carry_arms.inc`. `rola_api.cpp`'s `carry_forward_inter`, `carry_build_stamp`, `carry_owner_rows`, `carry_arms`, `carry_backward_inter`, `carry_backward_stamp`. `tools/gen_shards.py`'s thirteen `CARRY_ARMS` rows, its Python mirror of `box.cuh`, its carry TU/`.inc` emitters and `SPAN_FACTS_ARMS` (12 instantiations derived from the carry rows). `docs/internals/carry/*` (eleven pages). | **RULED, NOT OBSOLETE** (card C, `development/queue/C_CLEAN_SLATE.md`, ratified 2026-08-30): the family is REBUILT ground up in C3 as the lean final form, on foundation features shipped and tested on their own first. This is the one deletion in this ledger whose subject is not dead but SUPERSEDED BY A PLAN, so the usual "why it was safe" is a pointer instead: the bodies are readable and runnable at the `k35-final` tip, which C3 keeps as the PARITY REFERENCE every rebuilt stage is graded against, and every C3 stage brief names which past kernel it draws its mechanism from. Nothing outside the family consumed it: `carry_geometry`/`carry_sub_boxes` (the addressing block) were moved to `common/` one commit earlier and are untouched, and decode's only dependency on `box.cuh` was the page granule, which now has ONE definition in `common/geom.cuh`. Proof the rest of the binary did not move: `tools/sass_bodies.py` reports every surviving entry byte-identical to the pre-C0 build of the same tip. The Python contract (`rola/ops/carry.py`, the engine DAGs, the layer, every test and bench) is deliberately NOT deleted and goes red at the `_C` boundary -- that red is the measure of what does not exist yet. Revival = git (this row's commit, parent). |
| 2026-08-30 | `6caea05` | **THE STATS PASSES, whole, and the sharding machinery that existed for them.** `csrc/rola/src/facts/`: `facts_kernel.cuh` (the union-table pass, the atom-grain activity reduction, the block-grain liveness epilogue), `facts_launch.cuh`, `facts_ops.cuh`, `facts_api.cuh`, `facts.cu`. `csrc/rola/src/instantiations/` entire (`facts_s0.cu`, `facts_arms.inc`, `README.md`) -- the directory itself. `rola_api.cpp`'s `chunk_facts`, `chunk_block_bits`, `chunk_arms`. `tools/gen_shards.py`'s `FACTS_ARMS`, lex-policy mirror, table/atom-arm projections, `chunk_rows`/`chunk_shards`/`chunk_canonical`, shard and `.inc` emitters, README template and formatter. `tools/fatbin_entries.py` whole, and `setup.py`'s `_post_build_fatbin_check`. `tools/ratify.py`'s facts mangled-name decoder (`parse_shape`, `PLAN_TPL`, `FACTS_TPL`), `shard_paths`, and the census's facts-only grouping. `tools/lint/lint_standards.py`'s `check_shard_partition` shard-membership half and `check_arch_coverage` per-topology half. 103 entries per arch from `tools/manifests/sm_{80,86}.json` (450 -> 347) and the `shards` block. `docs/internals/facts/*` (five pages). | **RULED** (card C: "carry and friends (the stats passes)"). The facts family is the carry family's fact producer and nothing else's: it emits the union table the carry kernel's ballots read and the activity bitmaps its state I/O keys on. C2 rebuilds it as the LIVENESS PASS -- a foundation feature with host folds, shipped and tested without a kernel body -- and C1 adjusts the Python contract where its outputs replace the atom/block bitmaps. The tooling in this row is not collateral: every one of `fatbin_entries.py`'s sub-checks decodes a FACTS mangled name and asks a question about the SHARD (which unit owns an instantiation, whether a launcher is unresolved), and with no sharded family it verdicts on nothing; it returns with G4's per-arm units. `arch_caps.cuh` was NOT part of this -- `common/design.cuh` and `common/ops.cuh` include it, so decode and intra depend on it, and it moved to `common/` one commit earlier. THE COST, STATED: decode's PAGED path consumed `atom_bits` from this pass, so `rola/engine/dags/decode_dag.py`'s workspace and the integration tests over it go red until C2. Decode's own ORACLE tier does not touch the facts pass and is unchanged and green, and every decode and intra kernel in the binary is byte-identical (`tools/sass_bodies.py`, 347/347 survivors). Revival = git (this row's commit, parent). |
| 2026-08-29 | `f890ad1` | **THE CONV HOOKS, whole: `value_conv`/`route_conv` and the cache rings they carried across a continuation.** `rola/layer.py`: the `value_conv`/`route_conv` constructor parameters and stored attributes, the `_unpack_continuation` pair-return (`(state, conv_rings)`), the `conv_states` list and both conv call sites in `forward`, and the `value_conv`-conditioned dtype round-trip on `v`. `docs/api.md` §4.1's `value_conv` dtype exception paragraph and §1.2/§4's `(state, conv_rings)` examples. `docs/internals/state.md` §7's ring text and §1's `short-convolution rings` list entry. `README.md`'s layer example and state section. `tests/unit/test_api_contract.py::test_the_layer_takes_the_slot_and_not_the_slot_spelled_out`'s `value_conv`/`route_conv` rows. `tests/integration/test_decode_dispatch.py`'s `state=(state, conv_rings)` call sites. | **NEVER REVIEWED AS A MECHANISM, AND NOT PAPER-1 SCOPE** (K45 card). Both slots took an arbitrary caller module (e.g. `fla.modules.ShortConvolution`) and threaded its cache through the layer's continuation pair sight-unseen -- no oracle cell, no gate, no design review ever touched what ran inside them. `LayerContinuation` (this commit) replaces the pair with a frozen container holding the op's `state` alone; a convolution hook is future work and re-enters as a field on that container, never as a second ad hoc tuple slot. Nothing downstream depended on the rings: no shipped model configuration in this tree ever passed a `value_conv`/`route_conv` module, so every call site the deletion touches was carrying `None` in both positions already. Revival = git (this row's commit, parent). |
| 2026-08-21 | K34 M1/M2 | **THE CARRY SCHEDULE'S CTA-WIDE COMPACTION, and the read side's owner-grain scan.** `csrc/rola/src/carry/carry_kernel.cuh` — the per-(scan iteration, word) vote registers (`voteR[kScanIters][kWarps]`, `voteW[kScanIters]`), the packed live-count words and the `sscan` SMEM tenant they were published through, the CTA-wide exclusive prefix every thread re-derived from them, the per-word `bit_count`/prefix/predicated-store walk, and the TWO rendezvous that separated the three. `CarryGeom::kScanIters` and `scan_bytes` with them. | **A WINDOW IS AT MOST `kBallotLanes` BALLOT WORDS, SO ONE WORD PER LANE REPLACES ALL OF IT.** A row becomes one `LDG.32` per warp, which is cheap enough that every warp holds BOTH sides' owner-grain ballots — and once it does, the lists, the counts and the whole-window skip are warp-private computations of CTA-IDENTICAL inputs, so the warps agree by construction and the schedule's two barriers have nothing left to publish. Measured at the graded cell, per (owner, window): 10,070 → 8,475 instructions (−399 per warp per window, against the +340 the four-list schedule was measured to cost), store bank conflicts 556 → 23, registers 217 → 210, and the scatter's trip count becomes the window's LIVENESS instead of its width. The CTA-cooperative row load that the same shape suggests is REFUSED with its own measurement (`carry_kernel.md`). Revival = git (this row's commit, parent). |
| 2026-08-21 | K34 M2 | **THE CARRY'S SINGLE-BODY CARVE, and the read side's panel produce path.** `csrc/rola/src/carry/carry_kernel.cuh` — the `produce` lambda's `Wt` template argument and its read branch (the gain-free store path and the `have` zeroing of the carve level's run), and `BoxPlan::kCarveLevel` as a fixed `D - 1`. | **THE DECLARATION CHOOSES THE BODY NOW, so there are two kernels and neither has to serve both jobs.** `carry_split_kernel.cuh` carries the channel-split body for a declaration with no sparse level on the carve axis (its SASS is byte-identical to 7825d31's on all five topologies), and `carry_kernel.cuh` carries the leaf carve for one that has; the carve level is a template parameter named by the declaration rather than a constant. The read side's produce is the split body's alone. Revival = git (this row's commit, parent). |
| 2026-08-21 | `8db2192` | **THE INTRA REVERSE'S REGISTER OPERAND SYNTHESIS.** `csrc/rola/src/intra/intra_backward_kernel.cuh` — `transpose_acc` (the `[16 x 64]` accumulator's in-register transpose as four A fragments, four `movmatrix` plus four packs) and `pack_acc` (the same accumulator as A fragments in its own orientation), with the file's `transpose_frag_b16` import; the `at[4][4]` fragment array `d_v` held them in; and the mirror doc's "Where the transposes are" section. | **THE OBJECT IS PUBLISHED NOW, SO BOTH OPERAND MAPS COME OUT OF SHARED MEMORY.** The two functions existed because pass 2's A operand was the warp's own accumulator and the MMA wanted it in a different fragment map per m-tile, so the SAME 32 register values were re-derived sixteen times each — 1,056 instructions per (warp, active tile pair), 8.2% of the kernel. `A` and each `dG_l` are now published once per level as a bf16 image at exactly the rounding these functions applied, and `ldmatrix` / `ldmatrix.trans` deliver the two maps from it with zero bank conflicts in either flavour (measured, `prototypes/k31_intraprobe` PART 2). `common/ops.cuh::transpose_frag_b16` is UNTOUCHED and still used by the carry's reverse (`carry/backward_kernel.cuh:811`), so the `reg_transpose_b16` architecture refusal it carries still fires. GATED on the fp64 oracle and gradcheck rather than on `torch.equal`: the re-carve legitimately reassociates `d_pw_l` and `d_v`, and the intra reverse carries no bit-identity obligation. Revival = git (this row's commit, parent). |
| 2026-08-20 | 675d6b6 | **THE SHIPPED PREFILL CHUNK CONSUMER, whole.** `csrc/rola/src/chunk/chunk_kernel.cuh` — `compacted_kernel<C, D, B, BC, STATE>` and its device world (`ChunkParams`, `Geom`, `reg_floor`/`kCtaTarget`, the `Ascent` walk, `form_impl`, the `Producer` Kronecker component, the K-window and form-ring constants); `chunk_launch.cuh` (`launch_chunk_arm`, `query_chunk_census_arm`); `chunk.cu` (the dispatch TU: `forward_raw`, `forward`, `divide_pass` + `divide_kernel`, `build_stamp` + `stamp_kernel`, the consumer half of `arms()`); `chunk_api.cuh`; the consumer's operation catalogue `ops.cuh` and selection layer `design.cuh`; the `compacted_kernel` instantiation shards (`chunk_s0..s2.cu`, `chunk_arms.inc`, regenerated by `tools/gen_shards.py` as the facts-only `facts_s0.cu`/`facts_arms.inc`); the pybind entries `chunk_forward_raw`, `chunk_forward`, `chunk_divide_pass`, `chunk_build_stamp`; mirror docs `docs/internals/chunk/{chunk_kernel,chunk_launch,chunk,chunk_api,ops,design}.md` (the facts-pass, geometry-law, leaf-order-law, outer-radix, template-level and block-bitmap sections moved to `docs/internals/facts/facts_kernel.md` in the preceding extraction commits). | **K31 redesign replaces; baseline = tag `baseline/pre-k31`** (Blake ruling 2026-08-19: death by deletion — the box-native K31 kernel is built ground-up, never bolted onto this skeleton, and an in-tree baseline would be the bolt-on temptation). What decode, paging and the engine still need SURVIVED by extraction first: `chunk_facts_tables`, `atom_bitmap_kernel`, `block_bitmap_kernel`, `LevelPlan`, `check_arch_table` and the `(C, BC, D, B)` dispatch vocabulary all live under `csrc/rola/src/facts/`, bit-identical (the post-build gate proved "the source digest moved but NO ratified body did"). Revival = git (this row's commit, parent) or the tag. |
| 2026-08-20 | 675d6b6 | **THE TWO-SCAN BACKWARD, whole.** `csrc/rola/src/chunk/backward.cu`/`backward.cuh` — Scan A (the read side's digit gradient over a forward rebuild), Scan B (`dv`, `d_gain`, `d_write` on the descending walk), the `Descent` mirror, `reg_floor_scan`, the walk probe and both scan censuses; `scan_arms.inc` and `SCAN_ARMS`; the pybind entries `chunk_scan_a_probe`, `chunk_scan_b_probe`, `chunk_walk_probe`, `chunk_backward_seeds`, `chunk_scan_a_census`, `chunk_scan_b_census`; `docs/internals/chunk/backward.md`. Python: `rola/ops/chunk.py` (the executor: `execute`, `backward_seeds`, `scan_a`, `scan_b`, `unpack_grads`), `rola/interface.ChunkConsumer` (the autograd node), `rola/engine/dags/backward_dag.py` + `BackwardPlan` + `Seeds` + `rules/retention.py` + `rules.backward_envelope` + `manifest.scan_arms`, and `docs/internals/engine/backward_dag.md`. `rola_op` is now the explicit refusal: `prefill arm deleted pending K31 R2 — see queue K31 card`. | **K31 redesign replaces; baseline = tag `baseline/pre-k31`.** The scans were the consumer's adjoint — they walk ITS schedule from ITS tables — so they cannot outlive it, and K31 R2's backward is designed against the rebuilt forward (P65's two-scan LAW carries as design, not as code). Training meanwhile runs from the baseline tag (pending v1 rental items noted on the K31 card). Revival = git (this row's commit, parent) or the tag. |
| 2026-08-20 | 675d6b6 | **EVERY TEST WHOSE SUBJECT WAS THE DELETED EXECUTION.** Oracle tier: `test_chunk_op.py`, `test_chunk_final_state.py`, `test_chunk_ragged_tail.py`, `test_families.py` + `test_conformance_census.py` (the tier-1 conformance battery over the consumer; `families.py`/`generators.py`/`instantiation.py` survive as fixture vocabulary, the kernel leg removed), `test_bf16_family_gate.py`, `test_backward_family.py`, `test_backward_gradcheck.py`, `test_backward_seeds.py`, `test_scan_a.py`, `test_scan_b_partition.py`, `test_scan_b_probe.py`. Integration: `test_backward_ctx.py`, `test_chunk_dense_inplace.py`, `test_chunk_paging_equivalence.py`, `test_liveness_exit.py`, `test_execution_identity_gates.py`, `test_layer_dispatch.py`, `test_paging_stream_races.py`, `test_decode_continuation.py`, `test_conv_continuation.py`; the facade suite `test_state_facade.py` reduced to its surviving surface (bind-and-continue, chained halves, clone, backing agreement and the BC-flip cells were prefill-binding claims); `test_decode_family.py`'s layer-walk and decay-prefill-refusal rows; `test_interface.py`'s `ChunkConsumer` rows. Unit: `test_chunk_bench_harness.py`, `test_chunk_latency_regression.py`. | **K31 redesign replaces; baseline = tag `baseline/pre-k31`.** A test whose subject is deleted gates nothing; what still HAS a subject was repointed rather than deleted — the decode gates now seed their carried state through the facade's own `_kernel_entry` + the fp64 oracle's `output_final_state` (the state's definition), the G7 seam and the decode-family handoff grade decode against the oracle from an oracle prefill, and the dispatch table's prefill rows assert the deletion refusal itself. Revival = git (this row's commit, parent) or the tag. |
| 2026-08-20 | 675d6b6 | **THE CHUNK-ARM BENCH HARNESS'S CONSUMER HALF.** `benchmarks/bench_chunk.py` (the front door: baseline/gate over consumer latency), `bench/chunk_baseline.py`, `cells/chunk_latency_baseline.json`, the unit benches `bench_chunk_forward.py`, `bench_backward.py`, `bench_seeds.py`, `bench_divide.py`, the integration benches `bench_prefill_decode.py`, `bench_layer_step.py`, and the consumer subjects in `bench/subjects.py` (`chunk_forward`, `divide_pass`, `backward_seeds`, `scan_a`, `scan_b`, `layer_step`, `prefill_decode`); `tools/chunk_identity_capture.py` (the consumer's device-identity capture). | **K31 redesign replaces; baseline = tag `baseline/pre-k31`.** The R1 bring-up worktree predates the deletion, so its M5 baseline measurement is self-contained (K31 card); the surviving subjects — `chunk_facts`, `chunk_block_bits`, `decode_step`, `entmax_solve` — keep the harness (`bench/driver.py`, `chunk_cells.py`, `discipline.py`), whose cell registry builds plans through the KEPT facts engine and launches nothing consumer-shaped. Revival = git (this row's commit, parent) or the tag. |
| 2026-08-18 | (a commit cannot name itself; stamped at merge) | **THE MATERIALIZED LEVEL-0 LEAF BITMAP, and every pass that existed to sweep it.** `expand_sides<N_SIDES>` (the level-0 broadcast half of it; the recursion over levels `1..D-1` survives as `expand_inner<D>`); the `bits_r`/`bits_w` shared-memory arrays and their `4 * n_words` footprint; `coarse_index`'s block-scanned popcount over the whole bitmap; `unmapped_write_atoms`' and the atom condensation's sweeps over `n_words` words; `locate_row`'s serial word walk. `decode_step_smem_bytes` and `decode_work_words_per_cta` lose their `n_words` parameter and take `(n_groups, outer_width, inner_words)`. | **IT WAS A BROADCAST STORED AS DATA, AND THE STORING WAS THE FLOOR.** `supp = prod_l supp_l` makes the level-0 expansion one inner block of `S_1 = N/b_0` bits repeated into every live outer digit, so a bitmap word is a pure function of the inner block and level 0's staged amplitude row -- `SideView`/`view_word_at` serve it, and every caller already holds the `(digit, offset)` pair the broadcast needs, so it costs no integer division. Nothing that read the map lost information: the coarse index computes the SAME VALUES from digit classes and inner-block popcounts, the row partition and the walk's group skip are untouched, and the atom side enumerates the write support instead of the atom space. GATED ON BIT IDENTITY: `y` AND `state` `torch.equal` against the baseline binary across 80 ABI-level cells (`D` in {1,2,3} x sparse/dense routing x dense/paged backing x `n_split` in {1,2,4,7}), plus the named decode batteries. The removal is what takes the `(256,256)` step from 101.4 to 36.9 us and its shared memory from 39.5 KiB to 9.0 KiB. Revival = git (this row's commit, parent). |
| 2026-08-18 | (a commit cannot name itself; stamped at merge) | **THE GROWTH PROBE, whole: a launch, a translation unit and a host order.** `csrc/rola/src/decode/decode_expand.cu` -- `decode_growth_probe_kernel`, `decode_probe_smem_bytes`, `launch_decode_growth_probe` and its 2 instantiations. The python binding `rola_decode_growth_probe`; `rola.ops.decode.growth_probe`; the DAG nodes `growth_probe` and `request_verdict`; `DecodeScratch.probe_bits` and `.probe_ctr` (the counter survives, renamed `growth_ctr` -- it is now the step's own reduction counter, not a second launch's); `DecodeScratch.verdict` (the CUDA `Event` that ordered the probe ahead of the step); `DecodeParams::probe_bits`, `::gate`; `DecodePlan.gate`; and the `[1]` int32 GATE mechanism end to end -- the tensor a caller could pass to make a step a blind no-op, and the `gate is None` / `gate.zero_()` vocabulary that went with it. `kProbeBufWords` is deleted with the TU that sized itself by it. | **A LAUNCH AND A HOST ORDER, KEPT ALIVE BY A BOUNDARY THAT MOVED.** The probe existed to learn a residency answer BEFORE the step that needed it, because the fold and the expansion used to be separate launches and the step could not yet ask its own question. K18 folded fold+expand+walk into one kernel; K23 finishes the same move for the admission question: the step's prologue already builds the write bitmap the residency test needs (`unmapped_write_atoms`), so the test is a pass over shared memory the CTA holds anyway, not a second kernel and not a `gate` tensor threaded in from a launch that ran first. The verdict is now PER BATCH-HEAD (`growth`/`done`, reduced into `growth_any` by the last of `growth_ctr`'s arrivals) rather than the probe's all-or-nothing `[1]` gate, which is what makes a mixed batch advance the batch-heads it can address without discarding the whole step. `atom_bits` -- the write-atom condensation a growth admission plans from -- survives verbatim as a step output; only its publisher changed. Revival = git (this row's commit, parent). |
| 2026-08-18 | (a commit cannot name itself; stamped at merge) | **THE PROBE'S OWN TEST CELL, `tests/integration/test_decode_growth_trigger.py::test_the_probe_expands_the_same_write_support_the_preamble_derives`.** It held the probe's published `probe_bits` (`[BH, n_words]`, the ONE leaf-granular map anything on this path used to publish) against `_leaf_support`, `schedule.py`'s write mask taken to leaf granularity in torch. | **THE OBJECT IT CHECKED NO LONGER EXISTS TO PUBLISH.** The step's two expansion maps live and die inside the CTA that builds them -- that is the whole of the absorbed verdict -- so there is no leaf-granular buffer left to compare. The leaf-granular reference does not drop: `test_the_published_condensation_is_what_a_growth_step_admits` now checks `atom_bits` against BOTH `_write_atom_bitmap` (the Cartesian-product definition) and `_leaf_support` ORed 16-deep to atom granularity, so `_leaf_support`'s definition still reaches the assertion, through the atom condensation rather than through a leaf-level publication. `test_decode_graph_step.py`'s gate-tensor cells (`gate=torch.ones(1, ...)` / `gate.zero_()`) and their PLANE-WIDE no-op assertions (`torch.equal(arena.state, before)` over an arena with no `gate` argument to flip) were restated to the per-batch-head contract the verdict actually makes: every batch-head held needy (`_absent_one_atom_per_bh`), so the plane-wide equality is now a claim about EVERY batch-head declining rather than about a global flag, and the growth-under-capture cell (`test_a_growth_step_lands_under_the_graph_by_replay`) compares two independently-run sequences pairwise instead of restoring a snapshot plane, because a restore stopped being a common starting point once the step deposits for every batch-head it CAN serve rather than none of them. Revival = git (this row's commit, parent). |
| 2026-08-18 | `cfb9d77` | **THE DECODE OPERAND PLANE, whole**: `csrc/rola/src/decode/decode_fold.cu` and its `rola_decode_fold` binding; `DecodeScratch.amp_read`, `.amp_write`, `.g_write`, `.v`; `DecodePlan.amp_read`/`.amp_write`; `rola.ops.decode.fold_operands`; the `fold_operands` DAG node; and the `heads` argument to `rola_decode_forward`. | **THE PLANE EXISTED ONLY TO CROSS A LAUNCH BOUNDARY THAT NO LONGER EXISTS.** K14 made the fold one kernel because it was eight; K18 makes it no kernel, because the fold's consumer is one CTA and the operands are its own business (`decode/decode_fold.md`). The gather, the widening and the DECLARED `__shfl_down_sync` sum order all survive verbatim as `decode_fold.cuh`'s device functions, called by the step for both sides and by the growth probe for its write side -- so the probe and the step still agree about the write support by the CALL GRAPH, which was the one property the separate node was protecting. `heads` went with it: `v` arrives as `[B, 1, H, d_v]` now, so `H` is a shape rather than an argument that could disagree with one. MEASURED, captured replay, RTX 3080 Ti, clocks settled, against the K14-merged parent built from the same tree: `(16,16)` **29.70 -> 24.58 us** paged / **26.54 -> 20.48 us** dense at `B=2`, **31.74 -> 26.62** / **28.05 -> 22.53** at `B=32`; `(64,64)` **36.35 -> 28.67** / **31.74 -> 25.47** at `B=2`, and **91.14 -> 91.14** / **84.99 -> 86.02** at `B=32`, where the walk dominates and the per-CTA prologue exactly offsets the launches. RoLA launches per step: dense 3 -> 1, paged 4 -> 2. `y` and the state plane are BIT-IDENTICAL pre/post across 52 tensors (8 topologies x 2 dtypes x both backings). Revival = git (this row's commit, parent). |
| 2026-08-18 | `cfb9d77` | **THE `amp_clock` ABI AXIS**: the `c10::optional<std::vector<at::Tensor>> amp_clock` argument to `rola_decode_forward`, `DecodeParams::amp_clock`, `DecodePlan.amp_clock`, and the GEMV's separate `sa_c` shared-memory staging. | **IT WAS NEVER A SECOND STREAM.** The detached clock is `c[t, s] = prod_l p_write[t, d_l(s)]` -- the STORED write allocation itself, carrying no side gain -- and `fold_operands` passed literally the same tensor for both, so `amp_clock[l] == amp_write[l]` held by identity at every caller that ever existed. With the fold inside the kernel there is no second tensor to point at, and staging a bitwise copy of `sa_w` under another name would be a second derivation of one fact. The decay arm now reads `sa_w` where it read `sa_c`, which is the SAME VALUE IN THE SAME ORDER -- `y` is bit-identical, and the no-sweep argument (`stored != 0 => live != 0`) is now an identity in the code rather than a contract between two arguments. Revival = git (this row's commit, parent). |
| 2026-08-18 | `cfb9d77` | **THE PUBLISHED STEP BITMAPS**: `DecodeParams::bits_r`, `::bits_w`, `::scratch`, `::pc_coarse`, `DecodeScratch.bits` and `.pc_coarse`, and with them `decode_expand_kernel` -- the step's expansion as a separate launch. | **A GLOBAL ROUND TRIP ON THE WALK'S HOTTEST INDEX, kept alive by a launch boundary.** Both side maps, the recursion's ping-pong and the coarse index are `4 * ceil(N/32) + n_groups + 1` words -- SHARED MEMORY at every shipped topology, built by the CTA about to walk them and never written to DRAM at all (`rola_decode_work_words` returns `0` at `(16,16)` and `(64,64)`; the `work` buffer is the arm for a leaf space too wide for the budget, and it is allocated with zero length otherwise). What replaced their observability is stated rather than dropped: the growth probe still PUBLISHES its one map (`probe_bits`), which `test_decode_growth_trigger` holds to `_leaf_support`; the step's expansion is gated off the STATE DIFF at any topology (`test_decode_vs_oracle::test_g3_expansion_is_the_exact_leaf_support`, both sides, since the expansion does not know which side it serves); and the coarse index is gated by the fp64 oracle at a sparse multi-group cell whose empty groups and multi-CTA row range the fixture asserts. Revival = git (this row's commit, parent). |
| 2026-08-17 | (a commit cannot name itself; stamped at merge) | **THE ORACLE'S RAW VALIDATION ARM, and the last raw-selecting branches around it.** `rola/ops/naive.py`: the `normalized` predicate and all three branches it chose (`g_read64`'s ones-vs-supplied fork, `deposit_value`'s ones-column-vs-`v64` fork, and the readout's ratio-vs-`readout` fork), with `g_read` no longer read at all -- `R` is the read leaf product. `rola/ops/_oracle_contract.py`: `validate_inputs`' `normalization` parameter and its `else` branch (the three `g_read` shape/domain checks and the sentence `normalization='raw' requires an explicit g_read`), and `initial_cache`'s `norm` parameter (`width = d_v + int(norm == "global")` becomes `d_v + 1`). `rola/routing/factors.py`: `tensors()`' `g_read is None` fork. `rola/layer.py`: `get_stats`' `int(self.normalization != "raw")`. `tests/unit/test_oracle_contract.py`: `_case`'s raw `g_read` branch. | **THIS ROW SUPERSEDES THE CARVE-OUT IN THE P71-B ROW OF 2026-08-14 (`c9c4b2e`)**, which kept this arm on the argument that the oracle's statement of BOTH normalization arms is not dead code in the ordinary sense. The argument does not survive contact with what the arm was doing: `naive_rola`'s FIRST statement is `validate_normalization(normalization)` and `NORMALIZATION_KINDS = ("global",)`, so every branch below it was unreachable, and a spec arm that cannot be EXECUTED is prose in the wrong file -- the spec's own two-arm history is `docs/testing.md` and this ledger's row of 2026-08-06 (`52a67d8`). Reachability was proven rather than asserted: all 38 `naive_rola(` call sites in the tree were enumerated and exactly ONE passed `"raw"` (`tests/oracle/test_oracle_vs_fla.py`, repointed onto the global form in this batch's parent commit); `validate_inputs`, `initial_cache` and `validate_simplex` have no caller outside `naive.py` and the one `validate_simplex` test; and the production surface refuses `raw` BY NAME in four independent places that are all untouched here (`routing/types.py::validate_normalization`, `RouteProducer.__init__` through it, `engine/rules/envelope.py::arm_envelope`, `ops/decode.py`). THE REFUSAL IS THE SURFACE AND IT STAYS: `naive_rola` keeps its `normalization` parameter and keeps calling the validator, so `raw` is still refused by name at the oracle's door rather than becoming a `TypeError` about an argument count. **THE ARM WAS LOAD-BEARING FOR A GATE THAT COULD NOT RUN**, which is the finding this batch exists for: the FLA transcription check -- the reduction theorem's one external anchor -- passed `"raw"` and had therefore raised `ValueError` on its first cell since the raw ruling of 2026-08-06, hidden behind its `ROLA_FLA_CROSSCHECK` env gate. Verified by the dual-run protocol (`tools/oracle_dual_run.py --surface oracle --against HEAD~1`), which is what this file class requires. Revival = git (this row's commit, parent). |
| 2026-08-17 | (a commit cannot name itself; stamped at merge) | **`benchmarks/bench_layer.py`'S TWO DEAD ARMS** -- the `for norm in ("raw", "global")` loop and the `for decay_on in (False, True)` loop, with the `LearnedDecay` import and `decay_source` construction behind the second, and `_addressing`'s `norm` parameter (`state_floats_per_head` becomes `N * (d_v + 1)`). | **THE SAME PAIR P67 S5 RETIRED FROM `bench_scaling.py` (row of 2026-08-14, `2a87552`), in the file that sweep never reached.** Both arms RAISED on their first cell rather than reporting anything: `normalization='raw'` is refused by `validate_normalization`, and a decay-configured layer is refused by the chunk arm (`engine/rules/envelope.py::arm_envelope`, `decay is not None`) so it dispatches to the reference and this file's own `last_execution_backend == 'cuda'` assert then fails -- precisely so an oracle timing can never be reported as a kernel one. The `norm` and `decay` COLUMNS stay in the emitted row as the constants they now are, so a reader comparing an old JSON against a new one is comparing the same arm. Revival = git (this row's commit, parent). |
| 2026-08-17 | `97be9d9` | **THE SECOND DEVICE PASS PER GRADIENT OPERAND in `rola/ops/chunk.py::unpack_grads`** -- the `.to(dtype).contiguous()` chain on all six permuted operand views (`dv`, each read level, each write level, the write gain), replaced by `_to_operand`, which allocates the destination and copies the permuted view into it. | **A RE-LAYOUT OF DATA ALREADY IN THE TARGET DTYPE, buying nothing.** `to` preserves the source's physical order, so on a permuted view it produced a tensor that was contiguous in the OLD order and `contiguous()` then copied it again -- six bf16-to-bf16 gathers whose input and output held identical values. `torch.empty(...).copy_(view)` fuses the gather and the cast into one TensorIterator kernel. Thirteen copies become seven, MEASURED per kernel with `torch.profiler` (`launches=13.0 -> 7.0` at both cells), and the results are BIT-IDENTICAL: `torch.equal` over every returned gradient, both the pinned `(64, 64)` and the small-N `(16, 16)` `T=2048` cells. Event-timed on a settled-clock quiet GPU, `bench.pairing.measure_arm` 8x11: the unpack arm is **181.31 -> 100.35 us** pinned (-44.7%) and **98.30 -> 57.34 us** at small-N (-41.7%). Two alternatives were priced and REJECTED at the same seven launches: `torch._foreach_copy_` (no multi-tensor fusion survives the mixed dtypes and non-contiguous sources) and `torch.compile` (equal launch count, and inductor's runtime Triton codegen is what the closed-world build exists to refuse). Revival = git (this row's commit, parent). |
| 2026-08-17 | `66057b0` | **THE THREE SEPARATE OUTPUT FILLS in `csrc/rola/src/chunk/backward.cu::scan_b_probe`** -- four `torch::zeros` calls (`dv`, `dwsum`, `dgw`, `dpw`) become one `torch::zeros` over an fp32 arena that the four narrow and view. | **THE BYTES WERE IRREDUCIBLE AND THE LAUNCHES WERE NOT.** Every one of these outputs is reached through `fan_in_add` (= `atomicAdd`) from blocks whose order is not determined, so the K11 pattern does not apply to any of them: there is no output the scan writes WHOLE, none whose zeroing a store could absorb, and no grid-wide barrier that would let an in-kernel zero-init precede the first accumulate -- the identity must be in memory before the first block contributes, and it still is. What went is four launches of a quarter of the bytes each in place of one launch of all of them. MEASURED with `torch.profiler` over 20 steps: the five `FillFunctor` launches cost 52.69 us pinned / 22.22 us at small-N, and the same total bytes zeroed as one buffer instead of four is **60.38 -> 31.45 us** pinned / **25.05 -> 16.82 us** at small-N. Scan A's `d pr` is untouched: it is that kernel's only output and was already one fill. CODEGEN-NEUTRAL BY CONSTRUCTION and it is checked rather than argued -- the change is host allocation only, and the re-ratified manifests move exactly ONE field per arch, `source/csrc_sha256`, with all 444 instantiations per arch identical in registers, spills and `sass_sha256`; the scan census is unchanged at 255 regs / 0 spills / 49,696 B (pinned arm) and 219 / 0 (small-N arm). Revival = git (this row's commit, parent). |
| 2026-08-17 | K14 (a commit cannot name itself; stamped at merge) | **THE DECODE STEP'S TORCH OPERAND CHAIN.** `rola/ops/decode.py::_fold_scalar` (the `[B, 1, H] -> [BH]` gain fold); `fold_operands`' eight torch launches -- the two `read_simplex` reductions, their two divides and the four widening casts -- and, with them, the fresh per-step tensors they allocated. `fold_levels` SURVIVES as the gates' enumerated reference and nothing on the shipped path calls it. | REPLACED by ONE kernel, `csrc/rola/src/decode/decode_fold.cu`, writing into `DecodeScratch` buffers the sequence already owns (docs/internals/decode/decode_fold.md). The gain fold had no caller left: the kernel reads `g_write` at its own stride, so no `[B, 1, H]` intermediate exists to name. The replacement is bit-identical end to end on both backings -- `y` unchanged pre/post, dense and paged still `torch.equal` -- because the only reassociable step, the normalizing sum, has a DECLARED order gated bitwise in `tests/integration/test_decode_fold.py`, and it coincides with the torch order it replaced wherever the addends are bf16. |
| 2026-08-17 | `99512b1` | **THE DENSE CONTINUATION'S SECOND STATE PLANE, and the commit swap over it.** `rola/engine/facts/nodes.py::backing` no longer calls `rola.engine.facts.planes.state_plane` for a call that already carries one -- the exit plane IS the entry plane -- and `RoLAState._kernel_commit` loses the reference swap that took the new plane, keeping only the FRESH bind's take. The always-run dense `s_out` row goes with it: `rola/engine/rules/run_table.py`'s store term reads `W if (paged or carries_in) else True`, and `docs/internals/chunk/chunk.md#run-table`'s single dense row becomes two. | **A TRANSIENT WHOSE ONLY JOB WAS TO BE SWAPPED IN, and it scaled with the growth axis:** a fresh whole-written `[BH, N, d_v + 1]` fp32 plane per stateful call is hundreds of megabytes at the parity curve's `N`, allocated and written for a sequence that already owned one. In-place is safe by the CTA structure rather than by convention -- each atom has one owner group in one CTA, the entry fold reads its atoms ONCE in the prologue and the store epilogue writes the same ones ONCE after the last drain, so no CTA can read an atom another may already have stored (`chunk_kernel.cuh`'s `atom_first()`, shared by both blocks). Nothing in `csrc/` moved: the paged backing has ALWAYS aliased the two pointers, and this is the dense backing joining it. The skip row follows from the aliasing rather than being an independent claim -- a skipped block leaves its own atoms carrying, which is the paged unchanged-slot semantics -- and the FRESH dense row still cannot skip, because that plane is uninitialised. What replaces the mechanism is a claim: `tests/integration/test_chunk_dense_inplace.py` holds the in-place chain against a reconstructed double-buffer chain (aliasing isolated from the skip row, ragged and aligned lengths, four arms including the deep and two-group ones), `tests/integration/test_chunk_paging_equivalence.py` gates a three-call chain dense-vs-paged at every step, and `tests/integration/test_liveness_exit.py` holds the carry-through directly. The COST is recorded rather than hidden: a dense continuation is no longer transactional, exactly as the paged one already was not (`docs/internals/state.md#the-dense-continuation`). Revival = git (this row's commit, parent). |
| 2026-08-17 | `2fd459e` | **THE CHUNK ARM'S TOKEN QUANTUM, as a refusal.** `rola/engine/rules/envelope.py::arm_envelope` loses its `T % C` clause and its `tokens` parameter (with the fact at `rola/engine/facts/call.py::envelope_clauses`); `csrc/rola/src/chunk/chunk.cu` loses `check_inputs`' `L % chunk` check and, with it, the `chunk` parameter that only that check read; `csrc/rola/src/chunk/backward.cu` loses the same check on both scans and narrows `walk_probe`'s to `L > 0`. The test surface loses `tests/unit/test_facts_rules.py`'s `T=48` clause row, `tests/integration/test_layer_dispatch.py::test_a_token_count_off_the_chunk_refuses`, `tests/integration/test_decode_dispatch.py`'s `refuses-chunk` row, `tests/oracle/instantiation.py`'s `"T"` refusal clause and mirror branch, and `tests/oracle/test_conformance_census.py`'s three `tail = ragged` expected-empty extras. | **A CAPABILITY WIDENING, and the guards were the whole of what refused.** The consumer's schedule is a COMPACTION over live tokens, not a tiling of the token axis: `Ascent`/`Descent` ballot one 32-token word at a time, mask the final word's non-existent tokens, and close a super-chunk at the first token that would overrun a side's buffer OR at the end of the axis — so a short final super-chunk is what every sparsely-routed chunk already produced, and the facts pass has voted past-the-end tokens dead since it was written. The guards therefore refused a case the kernels already computed; nothing in `csrc/` moved to make ragged `T` work, which the SASS-body gate records (source digest moved, zero ratified bodies moved). What replaces the refusal is a claim: `tests/oracle/test_chunk_ragged_tail.py` holds `T in (1, 5, 31, 33, 63, 100)` at both routing depths against the fp64 oracle — stateless, stateful with the folded state read back, a ragged-into-ragged CONTINUATION chain, and both gradients over every topology the scan manifest carries. The composed defect this removes is the one that mattered: with decode contracted to continuations only, a short or ragged prompt could not START a sequence at all. |
| 2026-08-17 | `b967282` | **`PlanOverrides`'S HOME IN THE FACADE, and the orphaned paragraph at the top of its docstring.** The class definition is gone from `rola/expert.py` — the body, the `dataclass`/`Any` imports that fed it, and the docstring's opening paragraph, which described `state_block`'s refusal and was left behind when that field died (`1244aa1`): a sentence beginning "resident state tile." under a heading about pins the op decides. `rola/engine/rules/paging.py`'s function-body `from rola.expert import PlanOverrides` goes with it. | **A RELOCATION, and the row exists so the ledger and `git log` say the same thing about where the name lives.** `PlanOverrides` is now `rola/engine/types.py`, re-exported by `rola.engine` and by `rola.expert`, so `docs/api.md` §6's address is unchanged and `PlanOverrides.of`'s TypeError still names `rola.expert.PlanOverrides` — the surface a caller sees. It is ENGINE VOCABULARY: a pin is a VALUE the rule layer folds (`paging_preference` reads it and returns a bool), and the engine consumes it on every stateful call whether or not the marked door is ever imported. The alternative fix — resolving the preference to a bool at context construction — was REJECTED on blast radius: `expert` is a declared SEED of both the chunk and the decode contexts and `decode_forward`/`decode_step_gated` take it by that name, so the fold would have moved from the rule into `decode_dag.py` (the same upward import, one file over) or forced an API change across `rola/layer.py`, four test modules, the benchmarks and `docs/api.md`. `BackwardNotImplemented` (`2582a50`) is the precedent, in the same file. The proof is device-side of the import graph: the whole engine re-imports with `rola.interface` and `rola.expert` replaced by objects that raise on any attribute read, and no engine module defers the import into a body, which is the only way the previous shape passed. Revival = git (this row's commit, parent). |
| 2026-08-18 | bwd/deep-arms | **THE BACKWARD'S "OUTER LEVEL" AS LEVEL ZERO**, at four sites: the `spw0`/`spr0` staging column `col_base(0) + off_at(0, blk) + o` and `emit_u0`'s fan-in column `col_base(0) + off_at(0, blk) + od`, in both scans. With them, the hand-rolled copies of the composite outer index's radix -- `oBase % kFastSpan`, `(oBase / kFastSpan) % span_at(l)` and `(oBase / (kFastSpan * span_at(D-3))) % span_at(l)` -- in BOTH of `backward.cuh`'s producer column chains. The forward's copy in `chunk_kernel.cuh` STAYS. | **THEY WERE `D == 2` WRITTEN AS IF IT WERE GENERAL.** B1b's Wall C made every outer-level site `if constexpr (kDepth >= 2)`, which is depth-ONE safety, not depth generality: above two levels the composite outer index spans levels `0..D-2`, so naming level zero fanned the outer digit gradient into the WRONG COLUMN and emitted no gradient at all for the levels between -- the same class as B1b's dropped `off_at`, one level up, and invisible to a `D <= 2` battery. REPLACED by the law rather than dropped: `LevelPlan::outer_digit<Lv>` is the radix, stated ONCE and read by the producer chain and by the reverse pass alike, and every level `0..D-2` takes its digit gradient off the one composite sum weighted by its own leave-one-out product ([leave-one-out](chunk/backward.md#leave-one-out)). The deleted copies were that map spelled out; two residues in them are provably redundant (a single-digit level has one digit; the most significant outer level cannot wrap) and the plan's form does not emit them, which is why `(32,64,2,8)` and `(32,64,2,16)` census one to two registers CHEAPER than before at identical shared bytes, stages and CTA target. The FORWARD keeps its copy: repointing it moves twenty `compacted_kernel` bodies, several upward in registers, which is a measured change of its own rather than a consequence of this one. Revival = git (this row's commit, parent). |
| 2026-08-17 | `2582a50` (a commit cannot name itself; stamped at merge) | **THE ENVELOPE PAIR'S HOME IN THE FACADE.** `envelope_refusal` and `require_envelope` are gone from `rola/interface.py` — the bodies, the two `__all__` entries and the `arm_refusal`/`backward_refusal` imports that fed them — together with the `BackwardNotImplemented` class DEFINITION (`rola/interface.py` re-exports it and its `__all__` entry stays, so the public address does not move). | **NOTHING WAS REMOVED, THE LAYERING WAS: this is a relocation, and the row exists so the ledger and `git log` say the same thing about where these names live.** They now sit in `rola/engine/facts/call.py` beside `arm_refusal`, `backward_refusal` and `envelope_clauses` — the adapters they compose — and `BackwardNotImplemented` in `rola/engine/types.py`. The bodies, the composition order and every refusal SENTENCE are byte-identical; `rola.expert` re-exports both, so `rola.expert.envelope_refusal` (`docs/api.md` §4.1) is unchanged. What this buys: `rola/engine/dags/chunk_dag.py` no longer defer-imports UPWARD from the facade, and no module under `rola/engine/` imports `rola.interface` at import time — proven by re-importing the whole engine with `rola.interface` and `rola.expert` replaced by objects that raise on any attribute read. They could not land in `rola/engine/rules/envelope.py` beside the sentence folds: `tests/unit/test_facts_rules.py::test_every_function_under_rules_is_a_declared_rule` refuses any unmarked function there, and these two read objects and raise, so they are adapters and not `rule`s. Revival = git (this row's commit, parent). |
| 2026-08-17 | `a714c36` | **THE PER-ARM DISPATCH TALLY.** `RoLA.__init__`'s `_execution_backend_counts` dict and the `execution_backend_counts` property over it (`rola/layer.py`), with the `docs/api.md` §4 paragraph describing them. `last_execution_backend` — the SCALAR — stays. | **TWO WITNESSES TO ONE FACT, AND ONLY ONE OF THEM COULD NOT BE REPLACED** (Blake ruling 2026-08-17). Every reader's claim was "this arm ran": eleven sites across six test files and four benchmarks, and the scalar answers all but one of them PER CALL, which is stricter than an aggregate tally — a row that drives one forward now pins that forward's arm rather than a total two calls could also satisfy. The scalar survives because the BENCHMARKS read it (`bench_layer`, `bench_paging`, `bench_scaling`, `bench/subjects`) as the guard that stops a row reporting the wrong kernel's latency, and a process measuring the layer cannot monkeypatch the call it is timing — that constraint is now stated at the assignment site, since it is the whole reason a diagnostic is public. The one site whose claim was genuinely a COUNT (`test_layer_dispatch.py::test_checkpointing_a_training_mode_layer_now_trains_under_both_modes`, where recomputation running the forward twice IS the mechanism) converts to a test-side spy on `rola.layer.rola_op`, observing invocations directly instead of inferring them from module telemetry. NO ASSERTION WEAKENED, NONE DROPPED: every converted row still says WHICH ARM RAN, positively, and every refusal row still says no arm ran at all — the `layer-dispatch-autograd-gap` class stays closed and only the witness moved. `grep -rn 'execution_backend_counts'` over the tree is empty. The same commit repairs `docs/api.md` §4's independent staleness: it advertised an `'oracle'` key deleted at `578656e`. Revival = git (this row's commit, parent). |
| 2026-08-17 | `a826df2` | **THE NON-CUDA PROJECTION ARM in `RoLA.forward`** — the `bf16_proj = x.is_cuda` predicate and both `else` branches it guarded (`v = self.v_proj(x)` and `projected = self.o_proj(out)`). The bf16 GEMM pair is unconditional; the `value_conv` dtype return is untouched. | **A FALLBACK WHOSE VALUE NO CALL CAN EVER OBSERVE, PROVEN BY RUNNING IT.** There is no CPU lowering (row of 2026-08-14: a CPU operand has no arm), so every non-CUDA forward refuses. Measured, not argued: a CPU layer forward on the `(16, 16)` `d_v = 64` fixture raises `NotImplementedError` from `rola/interface.py:161` `require_envelope`, reached through `rola_op` at the dispatch site — AFTER the `v` projection and BEFORE `o_proj`, so the `v_proj` else-branch ran and its value was discarded while the `o_proj` else-branch was never reached at all. The two green paths that execute it are both refusal cells (`tests/integration/test_layer_dispatch.py::test_cpu_operands_refuse_because_there_is_no_cpu_lowering`, which runs in the CPU lane, and `tests/integration/test_decode_dispatch.py::test_a_cpu_decode_shaped_call_is_refused_at_the_prefill`); nothing in `tests/`, `benchmarks/` or `tools/` consumes a CPU forward's OUTPUT, and no CPU layer forward exists at any other dtype. The refusal is byte-identical across the change (the device clause reads the operand's device, not its dtype), and bf16 `linear` lowers on CPU, so the collapsed path still reaches the same sentence rather than raising a different one on the way. `docs/api.md` §4.1 drops its "On CUDA" qualifier and states the envelope reason instead. Revival = git (this row's commit, parent). |
| 2026-08-17 | `ed5ae3c` | **`RoLA._decode` (`rola/layer.py`)** — the private method and its docstring; the call moves to the one dispatch site inside `RoLA.forward`. | **A ONE-LINE WRAPPER AROUND ONE CALL, AND IT DISCARDED HALF THE RETURN.** The whole body was `decode_forward(routes, v, state, decay=decay, expert=self.expert)` with the returned state dropped on the floor; `decode_forward` returns the SAME state object, updated in place (`docs/internals/decode/decode.md`), so the inline binds `out, state` exactly as the prefill branch binds `rola_op`'s pair and the two arms of the dispatch now read as one shape. Nothing outside the method referenced it: `grep -rn 'RoLA\._decode\|self\._decode'` over `rola/`, `tests/`, `benchmarks/`, `tools/` and `docs/` is empty. The docstring carried no invariant the code cannot express — it narrated where the carrier, the scratch and the backing live, which is `docs/internals/decode/decode.md`'s subject — so nothing moved to the call site. The dispatch-site comment (decode's own envelope and its gradient clause) and `validate_decode_envelope` are untouched. Revival = git (this row's commit, parent). |
| 2026-08-16 | `b461ebd` | **THE UNCALLED BASELINE FILE TRIPLE in `benchmarks/bench/regression.py`:** the `BASELINE` path constant, `load_baseline()` and `write_baseline()`, with the `json` and `pathlib.Path` imports that had no other reader. | **NO CALLER, AND A REFUSAL THAT NAMED A DELETED FILE.** `load_baseline`'s `SystemExit` told the operator to record one with `benchmarks/bench_regression.py --write` -- a file P67 D2-c deleted with the tiled measurement apparatus (row of 2026-08-14) -- so the one instruction the triple existed to give could not be followed. Nothing in `rola/`, `tests/`, `benchmarks/` or `tools/` reads any of the three: `bench/chunk_baseline.py` is the LIVE per-cell baseline reader and says in its own header that it is not a replacement for the parked file. The DATA is untouched and stays parked: `benchmarks/cells/latency_baseline.json` is the tiled consumer's historical record, still read by `tools/ledger_import.py` and still described by `benchmarks/cells/README.md` and `docs/measurement.md` as a number no chunk result may be compared against. What went is the dead accessor pair over it. Revival = git (this row's commit, parent). |
| 2026-08-16 | `8174366` | **THE `run_table` DERIVATION IN C++.** `csrc/rola/src/chunk/chunk.cu`'s `static uint32_t run_table(bool, bool, bool)` and its call site inside `CHUNK_DISPATCH_X`, which folded the three residency presences at every launch. | **THE DECISION MOVES TO THE BAND THAT OWNS DECISIONS, and the `uint32` becomes a launch argument** (design.md §H.2, RULED 2026-08-16). It was a pure host fold living behind the extension boundary, which the doc had already called 'a host decision' — so the table a call PLANNED and the table it RAN were two derivations that agreed by hand, and the five rows of `chunk.md#run-table` were reachable only through a launch. `rola/engine/rules/run_table.py` is now the ONE derivation: its verdict is a `ChunkPlan` field and both consumer entries (`chunk_forward_raw`, `chunk_forward`) take it beside the bitmap it predicates over. The rule's body is the deleted one clause for clause, and the fold was already gated against the doc over all eight liveness bytes x all six rows before it carried anything (`tests/unit/test_facts_rules.py`, device-free). **THE CONSUMER KERNELS ARE UNTOUCHED AND IT IS MEASURED, not argued**: they read `ChunkParams::run_table` exactly as before, and over the two binaries `tools/sass_bodies.py --compare` reports **902 entries, 0 with a different body, 0 absent from either side**; the re-ratified manifests move ONE field, the source digest, with all 444 instantiations per arch identical in registers, spills and `sass_sha256`. Bit-identity against the parent commit's binary: `torch.equal` on all 180 tensors over 36 cells. Revival = git (this row's commit, parent). |
| 2026-08-16 | `acb2178` | **THE THREE TOP-LEVEL ENGINE PACKAGES, and the mirror tree that named one of them.** The import paths `rola.facts`, `rola.rules` and `rola.dags` cease to exist, with NO compatibility shim and no re-export stub of any of them; `rola/facts/__init__.py`'s re-export of the core (`Node`/`node`/`rule`/`run`/`validate_dag`, the plans, the value types) is deleted rather than moved, because the root it was standing in for now exists. `docs/internals/facts/` goes with them, its root page renamed `facts.md` -> `engine.md`. | **A RE-HOMING, and the paths are the only thing that ends.** Every body, every declaration and every doc section moves byte for byte into `rola/engine/`: the shared core to its ROOT (`runner.py`, `types.py`, `plan.py`) and the three domains beneath it (`engine/facts/`, `engine/rules/`, `engine/dags/`), mirrored by `docs/internals/engine/`. What ends is the mis-homing the layout existed to hide -- `from rola.facts.runner import rule` reached the SHARED framework through ONE domain's package, and `rola/facts/types.py` and `rola/rules/mask.py` cited each other -- so a domain now reaches the core through `rola.engine` and never through a sibling. A shim would have preserved exactly the ambiguity being removed (CODE IS STATE), so the ~80 import sites in `rola/`, `tests/`, `benchmarks/` and `tools/` are repointed in this commit and `grep -r 'rola\.facts\|rola\.rules\|rola\.dags'` is empty outside this ledger. STRUCTURE ONLY: no body edited, no launch moved, no argument changed -- `tools/chunk_identity_capture.py` holds `y`, the returned state plane, the union table, the block bitmap and the atom bitmap `torch.equal`-identical against the parent over 36 cells (180 tensors). The stale `(this commit)` on the row above is stamped here, which is the stamping convention this ledger already declares. Revival = git (this row's commit, parent). |
| 2026-08-16 | `59fd85d` | **THE ATOM-GRAIN TABLE PASS, folded into the union table's own launch.** `atom_table_kernel<D, B>` (`chunk_kernel.cuh`), `launch_chunk_atom_table_arm` (`chunk_launch.cuh`) and its eight instantiations; the `CHUNK_ATOM_ARMS_X` X-macro and the `chunk_arms_inc` block of `tools/gen_shards.py` that emitted it; `build_atom_bits` and the `stats` torch entry with its `chunk_stats` binding; `union_table_kernel<D, B, BC>`, `launch_chunk_table_arm` and the `union_table` / `chunk_union_table` entry; `rola/facts/nodes.py`'s `atom_bits` and `union_table` nodes; the import-time absent-`arm`-edge assertion in `rola/dags/chunk_dag.py` and its twin in `tests/unit/test_facts_memo.py`; the bench subject `chunk_stats` and `benchmarks/unit/bench_chunk_stats.py`. | **ONE PASS OVER THE AMPLITUDE PLANE, because the plane IS the cost and it grows with `T`.** The two kernels were the same expression at two grains over the same bytes -- identical grid (`ceil(tok_words(L)/4), BH`), 128 threads, one lane per token, one ballot per entry -- and they read the write plane TWICE (`BH · T · sumW · 2` bytes; 8.4 MB at the pinned cell). `chunk_facts_tables<D, B, BC, MASK>` votes both row sets out of one loaded row. `MASK` is the CALL CLASS and never a bool per fact: the stateless class compiles the atom-grain emission out entirely, so a stateless call pays exactly what it paid; within the stateful class a call with no arena hands a null atom table, which is the runtime-null OUTPUT skip. **The published bitmap stays BC-free by construction** -- the transient atom TABLE acquires the pass' `BC`, `atom_bitmap_kernel` does not (its arm list is still `(D, B)` alone, which is why `chunk_atom_arms()` survives the deletion of the macro it also fed). The import-time assertion could not survive the fusion in its old form, because the fused node DOES read the arm; what replaced it is a claim about the FACT rather than about an edge -- `tests/oracle/test_atom_bitmap.py::test_the_bitmap_is_bc_free` requires `torch.equal` bitmaps at every built `BC` of a topology -- plus an import-time check that the two facts still come from ONE node. `chunk_stats(pw, widths)` had no `BC` to give, so it dies rather than acquiring one; `rola/facts/planes.py`'s `atom_bits` keeps the bitmap-alone convenience for the gates and the paging bench by naming the arm and discarding the table, and says so. Instantiation accounting: the union table's 12 `(D, B, BC)` arms x 2 call classes = 24, replacing 12 + 8 = 20 (net +4 kernels; the design's estimate of "unchanged" counted the 22-row arm macro rather than its 12-row projection). ORDERING: the pass now runs BEFORE `plan_exact` rather than straddling it, so it fills the host-synchronization window instead of the admission's side-stream window. MEASURED, both binaries, `landing` tier, 8x11 reps, `perf/ledger.jsonl` probe `chunk:facts_pass`: at the pinned constructor (`B=2, H=8, widths=(64,64)`, arm `(C=32, BC=128)`) the pass is **49.15 -> 38.91 us at T=2048** (-20.8%, plane 8.4 MB) and **160.77 -> 135.28 us at T=8192** (-15.9%, plane 33.6 MB) -- the SAVING grows with `T` (10.2 -> 25.5 us) as the byte argument says it must, and the two points are the second-point rule. This also retires `docs/open-work.md`'s P67-d row, whose earlier not-worth-scheduling verdict priced two separable launches; as ONE pass with the grains shared it is worth it. Revival = git (this row's commit, parent). |
| 2026-08-16 | `9dd495f` | **THE DECODE PATH'S BESPOKE HOST MACHINERY, replaced by the decode DAG.** `rola/ops/decode.py`'s `_prepare` (the carrier + scratch + operand dict the two entries shared); the two module-level maps `_GEOMETRIES` and `_WORKSPACES` and the `_carrier` lookup over the first; `DecodeScratch.key` and `DecodeScratch.matches` (the second map's shape check); `_TopologyShim` (the three attributes `leaf_rate_dials` reads); `_growth_probe_launch`'s own fold of the write levels, and `_decode_step`'s second and third folds of the same tensors. `decode_forward`, `decode_step_gated`, `growth_pending` and `admit_growth` are no longer defined in `rola/ops/decode.py`: the family's facade moved to `rola/dags/decode_dag.py` beside the DAG it walks, and `DecodeGeometry` moved to `rola/facts/plan.py` as `DecodePlan`'s config half. | REPLACED, each by the framework mechanism it was a per-site instance of. `_GEOMETRIES` is `DeriveGeometry` declared `pure_of` the same five key fields; `_WORKSPACES` is `BuildScratch` declared `per_state`, whose table IS a `WeakKeyDictionary` — the weakness that map existed for is now the framework's, and `matches()` is the memo key rather than a hand-written comparison. `_prepare` is the walk. `_TopologyShim` existed because `leaf_rate_dials` wants a topology and `DecodeGeometry` already carries the `widths`/`d_v`/`D` it reads, so the config is handed in directly. The duplicate folds were three derivations of one fact: `FoldOperands` produces the amplitude streams once and the probe, the clock and the step read the same tensors (the clock IS the write stream, `decode.md#detached-clock`). The four facade names moved rather than died — `rola.dags.decode_dag` is their one home, and `rola/layer.py` imports `decode_forward` from it. |
| 2026-08-16 | `0d4405a` | **THE AD-HOC DECISION SITES, replaced by the rule layer; and the chunk arm's PRIMITIVES, moved out of the launch module.** `rola/ops/chunk.py`'s `_arm_for`, `arm`, `_PIN_C`/`_PIN_BC`, `CHUNK_TOKENS`, `unsupported`, `backward_unsupported`, `scan_arms`, `packed_copy`, `_side_plane`, `pack_side`, `state_plane` and `atom_bits`; `rola/interface.py`'s `_paging_preference`, `operands_of`, `requires_backward` and `_any_requires_grad` bodies, and the inline `any(ctx.needs_input_grad)` retention test; `rola/facts/types.py`'s `CallClass.mask` property; `rola/facts/nodes.py`'s `paging_preference` and `select_arm` node wrappers; `ExtentAllocator.take`'s alignment-and-ceiling body. | MOVED, not withdrawn, and every one of them to the band it belongs to (design.md §B2, Blake's rulings of 2026-08-16). The DECISIONS are pure host folds and are now `rola/rules/` — `arm`, `arm_envelope`/`envelope`/`backward_envelope`/`training_envelope`, `mask`, `run_table`, `retention`, `paging_preference`, `admission` — each declared with the `rule` marker, each fed values and gated with no device present (`tests/unit/test_facts_rules.py`, 44 cells). The FACT PRIMITIVES are `rola/facts/{planes,manifest,operands,call}.py`, bodies and all, so no fact primitive defers its logic back to `rola/ops/` or `rola/interface.py`; the per-family DAGs are `rola/dags/`. What `rola/ops/chunk.py` keeps is what crosses the extension boundary. Nothing computed differently: `chunk.unsupported`'s six clauses are `arm_envelope`'s six, in order and byte for byte, and the bit-identity capture over the 18 cells is `torch.equal` against the pre-stage tree. `CallClass.mask` became `rules.mask` because a decision written as a method on a fact is a rule no listing of the layer would show; the walk reads a `mask` FACT the rule produces. `MMA_K_QUANTUM` moved from `rola/ops/paging.py` to `rola/facts/types.py` (one definition, imported back) because the packing primitives are below the arena, not above it. |
| 2026-08-16 | `2368013` | **`SavedForward`, THE SECOND OBJECT THAT DESCRIBED ONE CALL.** `rola.ops.chunk.SavedForward` whole — its thirteen fields, `of()`, `retained_bytes()` and `kernel_v_is_the_callers()` (which had no caller); `chunk.backward` and the ten-argument tuple it hand-built for the two scans, spelled once and passed twice; `ChunkConsumer.backward`'s `_levels` closure and the un-packing body around it; `ctx.saved_routes`. | **The ctx became a REFERENCE to the forward's plan, so the copy it was a copy OF is the only thing left.** Every `SavedForward` field was a `ChunkPlan` field under a shorter name (`pr`/`pw`/`gw`/`rm`/`tbl`/`vk`, `c`/`bc`/`widths`/`eps`/`v_row_bytes`), re-read out of the plan by `of()` at retention time; two objects describing one call is exactly the hand-agreement the facts engine exists to remove, and what it permitted was a launch argument the forward and the reverse pass could come to disagree about silently. `ctx.saved_plan` is now `BackwardPlan(forward=plan, den=den)` — the plan by reference plus the one thing the forward PRODUCED rather than configured, `den` being `execute`'s output and therefore un-representable as a frozen plan field. `retained_bytes()` moves onto it over `replayed_from()` and returns **the same number for the same call**, measured before and after the change on five cells (T=64/128/256 at widths `(64,64)`; `(8,8)` and `(16,16)` at T=128): 67328, 134656, 269312, 17920, 34560 — unchanged. `chunk.backward`'s three launches become the four declared nodes of `rola/facts/backward_dag.py` over bodies in `chunk.py` (`backward_seeds`, `scan_a`, `scan_b`, `unpack_grads`), and the scans' shared ABI is `_scan_operands(plan, seeds)`, written once and read off the plan; an import-time check refuses an edge between the two scans, whose independence a later stage would stream. `kernel_v_is_the_callers` had no caller in the package, the tests, the tools or the benchmarks. The `"never filled"` refusal goes with the class: a half-filled ctx cannot be constructed once the ctx is a plan that ran, and the state that IS reachable — a backward reached from an inference forward — is refused at the node by name (`"no saved ctx"`; `tests/unit/test_interface.py`, `tests/integration/test_backward_ctx.py`). What the plan reference newly pins is `block_bits`, `[BH, N / BC]` bytes with no `T` in them (64 B at the pinned arm), which is NAMED in `test_backward_ctx.py`'s per-field gate rather than left to an aggregate. Revival = git (this row's commit, parent). |
| 2026-08-16 | `899e78e` | **THE CHUNK ARM'S AD-HOC HOST PATH, replaced by the facts engine's DAG.** `PackedRoutes` and `chunk.pack`; `chunk.run` whole, with its `saved` OUT PARAMETER contract and its re-derivation of the arm it was already handed; `chunk.stats(PackedRoutes)`; `interface._ChunkCall`; `interface._validate_call`; the four un-folded HOST-ORDER lines in `rola_op` and the duplicate `_chunk.arm(routes)` call among them. | **The conventions are replaced by declarations, and the bit-identity gate is the proof that nothing moved.** `PackedRoutes` was the plan's tensor half and `chunk.run`'s argument threading was its host half, both hand-maintained; both are now `ChunkPlan` fields built by one terminal DAG node, and `execute(plan)` decides nothing. The `saved` out-parameter existed so a grad-bearing call could reach the byproducts a launch built -- the plan IS those byproducts, so `SavedForward.of(plan, den)` reads them off it and the out-parameter has no subject. The four un-folded lines existed because the host order was load-bearing and un-declarable at a function boundary; as a DAG the order is the tuple's, the side stream and its one event are node attributes, and the Join's placement upstream of the block bitmap -- which reads the page table the admission writes -- is asserted rather than commented. `_validate_call` moves whole into `facts.chunk_dag.validate_context`, the ONE host call per kernel family that can refuse: consolidating it there makes a refusal after a launch unreachable by construction, and every message text is unchanged by one character (`tests/unit/test_api_contract.py`). `chunk.stats` becomes `chunk.atom_bits(write_plane, widths)` -- the same launch, taking the fact it actually reads instead of a bundle object. `rola_op`'s signature, `RoLAState`'s surface and every refusal message are unchanged; `tools/chunk_identity_capture.py` holds `y`, the returned state plane, the union table, the block bitmap and the atom bitmap `torch.equal`-identical across the refactor over 18 cells. Revival = git (this row's commit, parent). |
| 2026-08-15 | B1b | **THE FOUR CLAUSES THAT MADE THE BACKWARD ONE TUPLE**, and the hand-written arm list with them: `backward.cuh`'s `kDepth == 2` (both scans), `kInnerSpan == 2 * (BCg / 4)` (the digit rectangle), `BCg == kGroupThreads` (the mass column), the `kScanBCtaTarget == kCtaTarget` EQUALITY, and `backward.cu`'s hand-listed `SCAN_B_ARMS_X`. With them: `GeomB`'s `pw0_bytes = 2 * C * 4` and `DsRows = BCg / 4 + kMmaN`, and the `(char*)(spw0 + 2 * C)` layout literal. | None of the four was a decision -- B1a swept the whole legal space and found the clauses admit exactly `(*, 128, 2, 64)`, so the comment calling the single arm deliberate was wrong about its own kernel. Each is REPLACED by its general form rather than dropped: the digit rectangle by [the pass law](chunk/backward.md#pass-law) (`CLS * ODU == P`, which reproduces the quarter carve at the pinned arm), the mass column by `kGroupThreads % BCg == 0` (a slot's threads are a whole lane group), the depth clause by `if constexpr (kDepth >= 2)` on the outer level, and the equality by `<=` plus a censused floor/budget pair ([reg-floor-scan](chunk/backward.md#reg-floor-scan)) -- the parity clause could not hold as an equality once the two kernels' live sets were measured to fall at different rates in `BC`. The two `GeomB` literals and the layout one were the same hard-coded `kOuterSpan = 2`, and the layout copy was a live memory overrun at every arm with a different one. Revival = git (this row's commit, parent). |
| 2026-08-15 | `af462c2` | **The layer's dead surface, swept in one pass (flagged by Blake):** `PlanOverrides.mma_precision` (the dead axis — one legal value, two guards, zero readers) + the layer's `_bf16_projection_active` (reduced to `x.is_cuda` once the axis died; inlined) + `validate_execution_envelope` (a validator whose own docstring opened "BOTH OF THEM ARE GONE" — its clauses died when P65 wired the backward; a method kept as a place to explain why it does nothing) + the decode-plan dead axis threading and its two tests. RENAMES with it: `DecodePlan` -> `DecodeGeometry` and `freeze_decode_config` -> `derive_decode_geometry` (the object is shape-derived geometry — the chunk arm's `LevelPlan` analog at runtime binding — not a stats-fed plan; the plan taxonomy is geometry / residency / data-level skips, recorded in docs); `_CARRIERS` -> `_GEOMETRIES`. | The `state_block` pattern, generalized: after the planner era's deletions, several fields existed only as refusals of their non-default values, and one validator validated nothing. Every reader was a guard; every guard guarded a constant. |
| 2026-08-15 | `1244aa1` | **`PlanOverrides.state_block`, its refusal in `rola_op`, and the test asserting the refusal.** | The tiled consumer's `BC` pin, vestigial since D2-c deleted its only consumer: every surviving reference was the refusal explaining the field does nothing. The refusal was correct while the tiled path lived (a silent-inert pin is the forbidden thing); once nothing accepted the pin anywhere, the honest state is the field not existing. Flagged by Blake. |
| 2026-08-15 | `44296e2` | **THE `PERSIST` KERNEL AXIS AND THE MACHINE-SIZED PERSISTENT GRID, and the `sparse_grid` plan flag with them.** `compacted_kernel`'s `PERSIST` template parameter and the whole work loop it compiled (the grid-stride `do`/`while`, the per-item address re-derivation, the `stage_wait<0>()` re-use fence); `chunk_launch.cuh`'s span machinery (`want`, `span`, the `PERSIST != (block_bits != nullptr)` agreement check) and `ChunkParams::sm_count` with `chunk.cu`'s `sm_count()` query; `gen_shards.py`'s `CHUNK_PERSIST`, `CHUNK_NO_PERSIST`, `chunk_persist()` and the emitted `CHUNK_ARM_PERSIST_BUILT` macro, with the by-name refusal in `launch_variant`; `chunk_sass_gate.py`'s work-loop exemption and its `PERSISTENT` symbol regex; `PlanOverrides.sparse_grid`, `_sparse_grid_preference`, `_ChunkCall.sparse_grid` and `chunk.run`'s `sparse_grid` parameter; `SavedForward.block_bits`. Chunk instantiations 74 -> 42. | **THE CAPABILITY IS KEPT AND IS NOW UNCONDITIONAL: what went is the LAUNCH SHAPE, not the skip.** P68b measured the two apart on one binary with only the grid moving: the work loop cost +0.36..+4.43 % at `rho = 1` and the machine-sized grid cost +13..+47 %, so the whole dense wall was launch-shape quantization (`ceil(kCtaTarget * sm_count / BH)` put 256 CTAs on a 240-CTA machine at `BC=64`; `span=5` did not divide `blocks=32` at `BC=128`). The block-grain liveness bitmap, the three-bit predicate, the `run_table` truth table and `block_bitmap_kernel` ALL SURVIVE unchanged -- the byte is now read as the CTA's first statement on the ordinary `(blocks, BH)` grid, which is cheaper at density (+0.0..+1.1 % at `rho = 1`) and wins at every sparsity the loop won at. `CHUNK_NO_PERSIST` dies because there is no loop to spill: the five arms it excluded now carry the exit, and `(64, 128, 2, 64)` -- `reg_floor` 255, the arm the loop could never fit -- censuses at 250/247 registers with `localSizeBytes == 0` and is gated at `s_out` bit-identity in `tests/integration/test_liveness_exit.py`. `sparse_grid` goes because the reduction is unconditional: there is no crossover left to choose against, so a flag would be a knob whose only value is the default. `SavedForward.block_bits` was retained-but-unread -- the reverse scans derive the same zero from the same membership tables -- and `test_backward_ctx.py`'s closed form is the gate that says so. Revival = git (`91fe9d0`). |
| 2026-08-15 | `37c1af6` | **P74: the side gain's SECOND PROJECTION.** `rola/routing/side_gain.py`'s `gain_W` parameter, `project_side_gains`, `validate_gain_projection`, and `SideGain`'s `hidden_size` argument; `tests/unit/test_gain_surface.py`'s `test_the_side_gain_module_owns_the_layout_and_the_projection`; `tests/integration/test_layer_dispatch.py`'s `produce_routing` detour in `_sparsity`. | **THE GAIN IS `f(h)` AND SO ARE THE ROUTING LOGITS**, so a separate `[hidden -> H]` projection was a second GEMM, a second cast of the whole source stream, and two weight casts for one column per head. The weight moves into `route_W`'s trailing span -- one parameter, one init (the same `kaiming_uniform_(a=sqrt 5)` bound, which does not depend on the column count), one cast -- and `SideGain` keeps the half that is not a projection: the bias, the layout and the positive parameterization, now taking LOGITS. The `gain=False` ablation is unchanged in meaning and stronger in fact: the column is absent from the parameter rather than absent from a second one. The module row goes because its subject was that the module and the free function computed the same thing, and there is no free function; what replaced it is `test_the_gain_rides_the_router_gemm_and_is_not_a_second_projection`, which asserts the module's parameter set is exactly `{route_W, side_gain.gain_bias}` and that perturbing the trailing column moves the gain and NO routing level. MEASURED, and the number is not the one the plan predicted. Deleted: 3 launches (the gain sgemm, its duplicate `x -> fp32` cast of the whole source stream, and two weight casts) worth 0.042 ms and ~6.5 MB per step. Added: the router GEMM's N goes 256 -> 257, and `ampere_sgemm_128x64_nn` charges a whole TILE COLUMN for the one real one -- +0.031 ms, isolated in an N sweep where 257 and 258 cost the same (0.4663 / 0.4680 against 256's 0.4299) and the step is ABSENT at 512 -> 513. So the lever is worth 0.009 ms rather than the 0.045 the plan predicted: the tile eats 70% of it at THIS `packed_router_width`, and none of it at the next one. Net, interleaved over four reps: kernel-sum step 1.819 -> 1.810 ms at the union anchor and 2.275 -> 2.272 ms at the alternating config, DRAM -6.5 MB. Revival = git. |
| 2026-08-15 | `2b34616` | **P74 S3 (R1): `naive_rola`'s and `_decode_step`'s `write_levels_stored` parameter, `_oracle_contract.validate_inputs`' stored-levels block, decode's stored-vs-live zero-set precondition check, and the PACK's copy on the shipped path.** Test surface: `tests/unit/test_oracle_contract.py`'s `test_the_stored_clock_receives_no_gradient_not_a_zero_one` and its `stored` fixture operand; `tests/oracle/test_backward_family.py`'s stored-grad-is-None assertion and its clock-freezing gradcheck argument; `tests/oracle/test_decode_vs_oracle.py`'s `quantize_stored` axis. | **ONE REPRESENTATION, so the emulation parameter has nothing left to emulate** (Blake's R1: the bundle's canonical form is bf16). The solve WRITES the stored form -- `OutT = __nv_bfloat16`, already built and ratified on both arms -- so the levels handed to the oracle ARE the stored representation, and a parameter for passing a separately quantized clock was a second spelling of the same tensor. Decode's `stored != 0 => live != 0` check goes with it: the two are one tensor, so the precondition holds by identity rather than by a device reduction per level per step. GATED by the DUAL-RUN protocol against `d443180` -- oracle 640 quantities bit-identical, producer 162 at 0.000e+00, and all four mutants rejected on their own surface INCLUDING `clock`, the one that perturbs the very quantity this parameter fed. The tool calls the oracle through `_call`, so the old side is handed `stored` and detaches it while the new side never sees it, and BOTH record `None` for its gradient -- which is why no exception was declared. **WHAT REPLACED THE TEST ROWS**, all in the same commit: the clock's stop-gradient is now asserted as a DIFFERENCE (`test_the_clock_is_detached_so_autograd_is_deliberately_not_the_forwards_derivative` -- a central difference on the forward must disagree with autograd under decay and agree without it; measured separation 7.7e-4 against 1.3e-10), the gradcheck insurance splits into two cells so the write side is checked with no clock and everything else with the clock live, and the decode row becomes `test_g4_decay_on_the_stored_write_representation`, which rounds the write levels before either side is called -- the regime the shipped producer actually hands decode. The storage boundary gained a gate it never had: `test_the_stored_form_preserves_the_support_exactly`, bitwise, no tolerance, over 4 stress fixtures x 4 routing shapes x 2 alphas -- the self-masking theorem's storage precondition, checked. MEASURED at the pinned cell, same harness before and after: launches 29->25 (A) and 28->24 (B), kernel-sum step 1.8962->1.8145 ms (-4.3%) and 2.3597->2.2670 ms (-3.9%), DRAM 343.3->279.9 MB (-18.5%) and 303.0->247.9 MB (-18.2%). Revival = git. |
| 2026-08-15 | `d443180` | **P74 S1, TEST SURFACE: the gateway-assembly rows.** `tests/unit/test_api_contract.py`'s `test_a_foreign_producers_mass_is_still_folded_because_it_declares_no_simplex`, `test_a_producer_whose_output_is_malformed_is_named_with_its_level` and `test_the_stored_levels_are_simplices_and_the_mass_went_to_the_gain`; `tests/integration/test_layer.py`'s `test_a_producer_broadcasts_one_routing_tag_and_validates_a_lists_length`; three parametrize rows of `tests/unit/test_routing_types.py::test_invalid_routing_values_are_rejected`. | Each subject is an assembly step that no longer occurs. A foreign producer returns the FINISHED bundle, so nothing above it folds its mass, re-validates its per-level shapes or attributes a malformed level to a slot -- the bundle's own structural invariants are what refuse it, and `test_a_malformed_hand_built_bundle_is_refused_by_its_structure` holds those. The write-side mass-into-gain row needed a producer declaring `simplex_output=False` on a WRITE duty, which only the opaque row does and only a foreign producer can carry. The broadcast row's subject is deleted sugar. The `branches=` rows compared a constructor argument that is now a derived property, so there is nothing left to disagree. WHAT REPLACED THEM, in the same commit: `test_a_foreign_producer_runs_with_no_registration_and_no_tier` (rewritten against the bundle duck, and now also asserting the LAYER's structural slot accepts it unwrapped) and `test_a_slot_that_declares_none_of_what_the_layer_reads_is_refused_at_construction`. The skip's tooth is unmoved: `test_the_simplex_output_column_is_read_per_duty_not_per_activation` still pins union's read cell at `False` against its own activation's `True`. |
| 2026-08-15 | `d443180` | **P74 S1: THE PRODUCER TIER AND THE GATEWAY, and the `LevelConfig` wrapper.** `rola/routing/producer.py`'s `LevelProducer`, `SimplexProducer`, `RouteSource` and `ProducedRoutes`; `rola/routing/factors.py`'s `RouteGateway` whole, with `_slots`, `_scattered_widths`, `_scattered_tags`, `_validate_produced` and `_fold_bth`; `rola/routing/types.py`'s `LevelConfig`; `ResolvedRouting`'s stored `branches` field; the `rola` exports of all six names; the routing tag's broadcast-across-widths sugar. | **THREE BOUNDARIES WHERE ONE IS LOAD-BEARING.** The only duck the library ever checked is `RouteFactors` itself -- support bits are derived from the returned tensors and nothing reads a producer's claims about itself -- so a per-level producer interface and an assembly gateway above it were two contracts with no enforcement behind them and one negotiation protocol (who computes the gain, who packs which columns) as their only content. The one public class is `RouteProducer(levels, *, hidden_size, num_heads, ...)`, which owns projection, solves, fold, mass carry and side gain and returns the bundle; hand construction of a bundle is as legal and as checked as it was. `LevelConfig` goes because the WIDTH MOVED ONTO THE LEVEL CONFIG: `IndependentRouting(width, read, write, tied=False)` and `UnionRouting(width, alpha)` are the level's whole spec, so a wrapper pairing a width with a routing form that already carries one is a second place the same number lives, and `ResolvedRouting.branches` is a property over the levels for the same reason. `.at(width)` re-widths an operator choice; `uniform(depth, level)` builds the uniform stack a broadcast used to imply. ONE MEASURED CONSEQUENCE beyond the surface: the producer used to return `[B,T,H,width]` views the gateway immediately permuted back, and the solve's own `[BH,T,width]` output now reaches the fold directly. GATED by the DUAL-RUN protocol on both surfaces against `91fe9d0` -- oracle 640 quantities bit-identical, producer 162 quantities at 0.000e+00, P5 mutant rejected -- because retiring `LevelConfig` changes how the oracle's `Topology` input is BUILT even though its arithmetic is untouched. Revival = git. |
| 2026-08-15 | `a75248e` | **THE LAST FOUR TRITON KERNELS, and the `triton` dependency with them.** `rola/routing/entmax/production.py`'s `_production_fwd_kernel`, `_production_bwd_kernel`, `_production_softmax_fwd_kernel` and `_production_softmax_bwd_kernel` (the `@triton.jit` bodies, the `tl.constexpr` module constants, `_value_strides`, and the ~17 host uses of `triton.cdiv`/`triton.next_power_of_2`); `pyproject.toml`'s `dependencies = [..., "triton"]` and the E1(b) note that apologized for it. | **PORTED, not dropped: a capability moved, it did not leave.** `csrc/rola/src/entmax/factor.cu` carries all four (independent entmax forward/VJP + the softmax pair), reached through the same `_launch_*` helpers at the same call sites, and `dense_routing()` -- the shipped, paper-relevant full-support level -- runs on the softmax pair exactly as before. NO torch fallback was added anywhere, per the no-fallback rule. The port is gated against the fp64 REFERENCE and never against the Triton it replaces (the matching-the-naive rule): `test_cuda_lowering_matches_the_fp64_oracle` asserts the SUPPORT SET identical and the values and VJP inside the ratified production tolerances, across `shared_solve`/`mixed_per_level`/`dense_only` x 2 alphas x 4 stress fixtures, and the masked `production_factor` surface keeps its six unit rows. The reasons this was a deletion and not a preference: the closed-world policy (a runtime JIT against pinned-ptxas AOT + the runtime refusal), the hard dependency `docs/build.md` already denied having, and decisively P65 -- a Triton backward in the training path is what CUDA-engine-first forbids. Revival = git. |
| 2026-08-15 | `222e6e7` | **`csrc/rola/src/arch_table.cuh` + `docs/internals/arch_table.md` — the legacy V3 capability table.** Found INERT by the P70-C batch: no TU included it after the summaries surface retired (P71-A), so its `static_assert(sm_arch_tabulated(...))` — presented by setup.py's R4 row and two docs as R4's compile-time half — had stopped compiling; its last live role was as `tools/supported.py`'s parse input, a SECOND table that could drift from the live one. Consolidation: `supported.py` now parses `chunk/arch_caps.cuh::tabulated()` (the compiled, asserted, runtime-checked authority — `static_assert(tabulated(kArch))` + `chunk.cu::check_arch_table()`), and setup.py/bringup.md/paging.py repointed. The regenerated README gained the `sm_90` row the live table tabulates and the dead one lacked — the drift the two-table hazard predicts, caught at deletion time. | One table, one authority: R4's compile-time half is `arch_caps.cuh`'s assert, its runtime half `check_arch_table()`; a tool-only table is a claim the build never checks. |
| 2026-08-14 | `56e77b7` | **P70-C: the routing telemetry sink, whole.** `rola/diagnostics/telemetry.py` (`RolaTelemetry`, the throttled JSONL sink, the descriptor recorder, the per-BH leaf-support and never-written accumulators) with its package `rola/diagnostics/` and its gate `tests/unit/test_telemetry.py` (14 tests). | **It had no call site and could not acquire one by accident.** Nothing under `rola/` imported it, it was not exported from `rola/__init__.py`, and the only importer in the tree was its own test. This is NOT the dormant-by-plan case the register below exists for: dormant machinery is machinery a planner declines to SELECT, and selection presupposes a call. The test file said as much itself -- "not yet wired to telemetry, so there is nothing equivalent to re-point it at; closing it requires a call site, not a test" -- so what the 14 tests gated was a sink's behaviour when the sink is invoked by the tests that gate it. The measurements it would take (per-level support, exposed bits, collapse) are derivable from a producer's distributions whenever a measurement wants them, which is where `benchmarks/` already gets them. Revival = git (`56e77b7^`). |
| 2026-08-14 | `f2d9261` | **P70-C: `RoLA.__init__`'s `hidden_size` and `num_heads` parameters, and the refusal that compared them.** | A SECOND DECLARATION, not a capability. `RouteGateway` stores both, and the constructor's only use of the two parameters was a `ValueError` when they disagreed with the gateway's -- so by construction every reachable call site passed values equal to the gateway's, or it was already raising. That refusal is what makes the removal provably behaviour-preserving at every call site rather than merely plausible, and it goes with the parameters because with nothing to compare there is nothing to refuse. The layer now reads `routes.hidden_size` / `routes.num_heads`. Signature is `RoLA(routes, *, d_v=64, decay=None, value_conv=None, route_conv=None, layer_idx=None, expert=None)`, gated by `tests/unit/test_api_contract.py::test_the_layer_takes_the_slot_and_not_the_slot_spelled_out`. `d_v` STAYS: it sizes `v_proj`/`o_proj` and the routing carries no value geometry, so it is the layer's own fact and not a second declaration of anyone's. |
| 2026-08-14 | `4d032fd` | **P70-C: the `tier1`/`tier2` pytest markers and `tests/conformance/__init__.py`.** | The markers were declared in `pyproject.toml` and applied by NOTHING -- a grep for `mark.tier1`/`mark.tier2` over the whole tree returns nothing -- so they were a vocabulary the suite had stopped speaking; the tier names themselves retired with the re-tree onto `oracle`/`integration`/`unit`. The `__init__.py` went because `tests/oracle` is FLAT by the ruling and the cross-file imports resolve as an implicit namespace package, which the green battery on the new paths demonstrates. |
| 2026-08-14 | `c9c4b2e` | **P71-B: THE DEAD `raw` BRANCHES, wherever `validate_normalization` had already made them unreachable.** `GainConfig.read_gain` and everything built on it (`.validate`, the two-boolean `from_public`, the read column of `.layout`, `READ_GAIN`, `READ_GAIN_UNDER_GLOBAL`, and the construction refusal that existed only to reject `read_gain=True` under `global`); `project_side_gains`' / `gain_layout`'s / `validate_gain_projection`' / `SideGain`'s `normalization` and `read_gain` parameters and every branch selecting on them (`g_read` is no longer a key of the returned dict); `RouteProducer.forward`'s raw read-mass fold; the `(g_read is None) != (normalization == 'global')` biconditional in `RouteFactors.__post_init__` and `rola_op._validate_call`, both now `g_read must be None`. | `raw` has been refused package-wide by `rola.routing.types.validate_normalization` since P26, so every one of these was UNREACHABLE code selecting on a value no bundle can carry -- and a `normalization` parameter that selects nothing is exactly the dead knob the gain surface's own gate claims not to have. Architecture §4.2 is the standing statement that survives them: the readout is a ratio, so `g_read := 1` identically and a read gain is a parameter with an identically zero gradient. Conformance verified: no family expects `raw` anything -- the surviving `-raw-` name tokens are historical family ids, already documented as no longer an axis (`families.py`). NOT deleted, deliberately: `rola/ops/_oracle_contract.py`'s raw validation arm. It is the ORACLE's own statement of the spec's two normalization arms, the oracle is the executable spec under the dual-run protocol, and its defensive completeness is not dead code in the same sense. Revival = git. |
| 2026-08-14 | `a8c9082` | **P71-B: EVERYTHING ON THE STATE THAT A PAGED TENSOR WOULD NOT KNOW, and the dense-contents bridge.** `RoLAState._widths`/`._normalization` and the `_require_compatible` interview built on them; `._batch`/`._heads` as fields (they are the shape tuple); `._BC` as a field (it arrives from the call); `.conv_state`; `._decode_carrier_cache`/`._decode_workspace` and the `_decode_carrier` method; `._dense_entry`, `.adopt_dense_contents`, `._readmit`; the `keying` property. `freeze_decode_config`'s unused `normalization` parameter went with them. | **THE MEMBERSHIP TEST** (would a paged tensor know this?) is the rule, and each removal answers it. The FINGERPRINT: shape mismatches now fail where the bytes are addressed, as they do for a tensor, and the named accepted hazard -- same `N`/`cols` from different widths interchange silently -- is the caller's risk, stated in `state.md` §2. The BRIDGE: with the oracle off the facade there is no arm to cross to, so a state never changes backing mid-life. The DECODE MACHINERY: the carrier is a pure function of its key (a module dict), and the workspace belongs to one live state (a `WeakKeyDictionary` keyed by the state -- concurrency-correct, and it drops with the sequence). `conv_state`: continuation is LAYERED -- the layer returns `(y, (state, conv_rings))`, holding exactly the recurrence its own level introduced (api.md §1.2). This SUPERSEDES the `8fabf0e` row's move of the carrier onto the state: that move was right about per-SEQUENCE and wrong about the object that should hold it. |
| 2026-08-14 | `578656e` | **P71-B: THE ORACLE THROUGH THE FACADE, and every dispatch that reached it.** `rola.interface`'s `_reference_arm`, the `_arm` parameter of `rola_op`, `Dispatch`, `dispatch_for`, `select_backend`, `_backend_for`, `_tensors`; the layer's `select_execution_backend` and its `'oracle'` counter key; `rola.expert`'s re-exports of the dispatch surface. Replaced by ONE authority -- `envelope_refusal`/`require_envelope` -- and, at the layer, `validate_execution_envelope` (the module's two clauses). | **A CAPABILITY REMOVAL, and the consequence is named: there is NO TRAINING PATH through the production surface until the native backward lands (P65 / open-work M-2), and no inference route for a decay-configured prefill, a jagged topology, `d_v != 64`, an unbuilt `(D, B)`, a ragged `T`, an unratified activation or a CPU operand.** The reversed ruling is R-13's route-to-reference: a dispatch to the fp64 textbook recurrence at inference is a silent ~1000x-slower route shipping unnoticed, which is what the no-fallback rule forbids. The oracle did not move and did not change -- it is the executable spec, and tests, ratification and the dual-run call `rola.ops.naive.naive_rola` DIRECTLY (that repoint is the bulk of this commit's test diff). Every refusal names the clause that has no arm, and `rola.expert.envelope_refusal(routes, v, decay)` answers the same question before the call. |
| 2026-08-14 | `b952192` | **P71-A: THE ROUTING-SUMMARY SURFACE, whole.** `rola/routing/summaries.py` (586), `rola/routing/geometry.py` (115), `rola/routing/exact_summaries.py` (221), `csrc/rola/src/summaries.{cu,cuh}`, the `rola_routing_summaries` pybind entry and its `#include`, the `setup.py` source row, the `tools/sass_bodies.py` and `docs/build.md` gate-roster entries, `docs/internals/summaries.md`, and `tests/unit/test_routing_summaries.py` (244 rows). | **It priced a planner that no longer exists.** `summarize_routing` measured tile-union occupancy for the tiled consumer's `BC`-aligned owner rectangles -- `geometry.py`'s entire premise -- and P67 D2-c deleted that executor; the chunk arm's `BC` is not host-selectable at all. Production callers: ZERO (`grep` over `rola/` found `summarize_routing` only in its own module and two doc cross-references; `owner_geometry` only in the test). NOT crown-jewel-protected: the rule covers the fp64 naive and the kernel-vs-oracle diff, and this is neither the oracle nor a kernel the oracle grades. **The protected surface was measured, not argued:** `.so` stashed before the csrc deletion, both binaries dumped at SASS hash version 2 -- **310 entry points before, 306 after, 0 differing and 0 extra over the 306 survivors**; the four that left are `summaries_kernel` and `cub::EmptyKernel` per arch (`summaries.cu` was the only CUB emitter, which is why `docs/build.md`'s full-scan arithmetic lost its CUB clause). Device-side afterwards, the check a stale binary cannot pass: the bound entry list is `chunk_* (7), entmax_* (2), rola_decode_* (4), rola_vmm_* (2), RoLAVmmOwner`. **REOPEN CONDITION, nameable:** if a future card needs per-owner occupancy pricing (a widened chunk matrix choosing among arms, a scheduling study), it revives from this commit WITH its consumer in the same batch. An addressing-entropy instrument is NOT that condition -- it wants statistics over the PRODUCER distributions, not over a retired schedule's owner rectangles. Revival: `git show b952192^:<path>`. |
| 2026-08-14 | `dfc3ff8` | **P71-A: the bundle's PYTHON-SIDE STATISTICS and its DECLARED DENSITY, and the seal that existed to protect them.** `RouteFactors.support_counts()`/`touched_bits()`/`exposed_counts`/`exposed_bits`/`resident`/`density`/`origin`, `_DERIVED` and the hand-construction refusal, `_declared_density`, `ProducedRoutes.support_counts`/`touched_bits`, `SimplexProducer(expose_stats=)` and `_unfold_count`, the `dense_sides` producer protocol, `rola/routing/budget.py` whole (`exact_support_count`, `touched_bitset_update`), `rola/routing/density.py` whole (`StaticDensity`), the `rola.expert` re-export, `tests/unit/test_budget.py`, and `tests/conftest.py`'s `RawRoutes` shadow bundle. | **Nothing in the library ever read any of it.** The stats accessors had zero production callers (only tests); `density` had exactly one reader -- the gateway, which stored it and never consulted it -- because its pricing role died with the tiled planner at D2-c. The device-side stats pass (`chunk.stats`, the decode growth probe) is the one stats system that executes anything, and an instrument that wants producer distributions rebuilds from them. **THE SEAL FOLLOWS THE DECLARATIONS.** `origin=_DERIVED` refused hand construction on one argument: a caller could pair statistics with tensors those statistics did not describe. With no statistics and no declaration on the record, that argument has no subject, so `RouteFactors` unseals and `__post_init__` asserts STRUCTURE instead (normalization re-validated, one level per topology level at that level's width, one shared `[B, T, H]` prefix, one device, one dtype, `g_read` present iff `raw`, `g_write` shaped `[B, T, H]`) -- stronger than the seal for exactly the fixtures the seal forbade. Unit-sum-ness is deliberately NOT asserted: an O(BTH*width) device reduction per level per call, the gateway's own output contract, and conformance fixtures plant non-canonical amplitudes on purpose. `rola_op` still type-gates on the class. **THE MEASUREMENT OUTLIVES ITS CONSUMER** (`StaticDensity`'s Gate-2 provenance, 2026-08-01 item 3e, preserved here verbatim in substance): a parity-shaped `RoLA` layer (`hidden=256`, `H=4`, `d_v=64`, `branches=(64,64)`, `BC=64`) at `L=2048`, seed 11, with `route_W` scaled by a `logit_gain` lever, holds a declared-dense read side through `logit_gain in {1.0, 8.0, 16.0}` (0.0 realized zeros at every level; the bench default is 8.0) and goes FALSE at `logit_gain=32.0` -- realized read-side zero fractions 0.002056 and 0.001883 across the two levels. At that cell the substituted vs measured tile statistics differed by ~2e-4 relative on `mean_KI_candidate` (0.4067078 vs 0.4066162) and `mean_kisteps_candidate` (0.2754822 vs 0.2754517), and `cost_per_token` was UNCHANGED -- i.e. the mispricing moved no decision at that cell, consistent with cost-model-only. There is no known upper bound on the gain needed to move one; this is one measured point, not a proof. A future cost model that revives a declared-density column inherits that caveat. Revival: `git show dfc3ff8^:<path>`. |
| 2026-08-14 | `b077b91`, `dfc3ff8` | **P71-A, TEST SURFACE: the `raw`-arm producer cells and the statistics rows.** `test_union_level_mass_is_folded_not_dropped`, the `[raw-1.5]`/`[raw-2.0]` cells of `test_producer_production_path_matches_its_oracle_path`, `test_producer_supplied_statistics_equal_the_measured_ones_exactly`, `test_the_statistics_are_derived_on_demand_and_not_before`, `test_producer_exact_support_counts_match_a_manual_nonzero_count`, `test_a_hand_built_routing_bundle_is_refused`, and `tests/unit/test_budget.py`'s two rows. | The `raw` cells gated a configuration NO BUNDLE CAN CARRY: `validate_normalization` is `global`-only, so `RouteProducer` refuses `raw`, and those rows only ever ran because `tests/conftest.py` reassembled the gateway's derivation outside `rola/` and bypassed it. That reassembly is gone (`produce_routing` now drives the real gateway through `torch.func.functional_call`), and with it the back door. The fold's exactness is still gated on the live arm by `test_union_mass_fold_is_an_exact_identity_at_fp64`. The statistics rows lost their subject with the accessors. The hand-built-refusal row INVERTED rather than vanished: `test_a_well_formed_hand_built_bundle_is_legal` plus six planted structural violations refused by name. |
| 2026-08-14 | `d45f92f` (+ `a6245b9`, `8046999`, `8e66647`, `9ef4258`) | **P67 D2-a: the UNSUPPORTED-TOPOLOGY arm, and the second dispatch authority behind it.** Every configuration outside the built chunk matrix now dispatches to the fp64 reference: `d_v = 32`, `raw` / per-state normalization, a jagged (non-uniform-width) topology INCLUDING the flagship `widths = (8, 16)`, an unbuilt `(D, B)`, a `T` that is not a multiple of `CHUNK_TOKENS = 32`, decay, and an unratified activation. `rola/interface.py::dispatch_for` reads `rola.ops.chunk.unsupported` and is the ONE authority (`Dispatch.no_kernel_arm` carries the refusing clause; `Dispatch.not_kerneled` is unchanged, since overloading it would have conflated an activation fact with a shape fact). `RoLA._tiled` and `RoLA._dense_carry` (the layer's last raw-tensor state crossing) are DELETED, and the layer's two prefill arms collapse to one `rola_op` call. `rola_op`'s `NotImplementedError` is reachable only through the `_arm="kernel"` override. `PlanOverrides(state_block=...)` is REFUSED on the kernel arm. | **A CAPABILITY REMOVAL on rule 2, ruled by Blake as R-13** (the inventory's flagged question, vetoable before the mass deletion): `d_v = 32` and `raw` normalization were not topology-shape refusals but whole FEATURES with a working kernel, and they lose it. Basis: the chunk envelope (`d_v = 64`, global) is paper 1's scope; `raw` was already declared a dead axis; P28 is the precedent (capability removal, revival = git). The narrowing is STATED rather than softened -- `docs/api.md` §1.1 is the envelope table, the refusal is named before the call, and an override raises rather than silently taking the oracle -- and `docs/open-work.md`'s P67-f is the card that widens the matrix back, one axis at a time, each justified by a measurement rather than by the shape having once worked. The refused configurations still RUN, on the textbook fp64 reference, which is also the gate every kernel arm is compared against; what they lose is throughput, and that is the whole of the loss. `state_block`'s refusal was added in the same commit rather than left: after the repoint an expert `BC` pin would have reached an arm whose `BC` is a manifest fact, so the pin would have been silently inert -- the forbidden thing. Battery unmoved at the repoint (1557 passed, 137 skipped, 838 xfailed); csrc byte-untouched. |
| 2026-08-14 | `ae4ed29`, `c954aa2`, `62c4fef`, `c7eaeb9` | **P67 D2-b: the tiled consumer's TEST SURFACE, ~6,900 LOC.** Whole files: `tests/tier1_semantics/test_consumer_vs_oracle.py` (1374), `test_algorithm_vs_oracle.py`, `tests/tier2_geometry/{matrix,test_meta,test_matrix_rows,test_endgame_executor,test_paging_equivalence}.py`, `tests/unit/test_planner.py` (2047), `test_derive.py`, `test_dispatch_table.py`, `test_selector_interface.py`'s successor rows. Re-anchored rather than dropped: the conformance engine (`generators.py` now drives `rola.ops.chunk` instead of `_consumer_forward`), `test_paging_stream_races.py` (onto the `rola_op` facade), `test_shard_partition.py` (R-9 rewrite onto the chunk arm list, and it ARRIVES as M1's manifest pin), the output-charge mutant and the `sqrt(T)` walk (onto `test_bf16_family_gate.py`). The tier-2 GeometryPair matrix and its three meta-tests retire with their axes (R-8): four of five axes were gone, and a one-axis table does not earn the machinery written to police one. | Every deletion is one of two classes, and the class was declared per file. **Class (a): the subject is deleted code** -- the tiled consumer, `Plan`/`Schedule`, the planner's derivation, the `GT` window, `intra_panel.cuh`'s lowering, `rola_consumer_occupancy` -- so the row gates nothing that exists. **Class (b): the CLAIM survives its carrier**, and each was walked to a named successor before its file was touched (the tables are in `workflows/p67-paging/d2b_build.md`, row by row, with the twin that carries each). The census moved and was RE-DERIVED rather than adjusted: the instantiation axes are now projected from the built arm list (`BC`, `DV`, `DECAY` gone; `B` NEW, invisible under the tiled axes), giving 7 off-mid values x 10 regime values = **70** cells against the old 60, with `EXPECTED_EMPTY` **69** against the old 56 -- a LARGER declared-empty set because `tail = ragged` is now empty at every instantiation, which is an envelope fact carried in three `_EXTRAS` rows rather than a coverage loss. Two conformance families lost their subject and say so through a CLAUSE NAME (`instantiation.REFUSAL_CLAUSES`) that CUDA asserts against production's own refusal, so a declaration cannot lie. Net battery: 1557 -> **1147 passed**, and the 838 xfails (837 of them `test_planner.py`'s) go to **zero**; every delta was measured against a `git stash` run of the same files at the previous tip. |
| 2026-08-14 | `b19e855`, `a121fc7`, `aa2fb75` | **P67 D2-c: THE TILED CONSUMER ITSELF, and everything whose subject it was.** 53 files deleted whole, 11,615 lines; net across the phase 98 files changed, +656 / −13,397. **csrc** (4,152 lines): `consumer.{cu,cuh}`, `consumer_kernel.cuh`, `consumer_launch.cuh`, `intra_panel.cuh`, `mma_tile.cuh`, `smem_layout.cuh`, `readout_mma.cuh`, `mma.cuh`, `cp_async.cuh`, `host_api.cuh`, the four `consumer_gt*` instantiation shards and `instantiations/dispatch_table.inc`. **python**: `rola/planner/` whole (`schedule.py` 1575, `derive.py` 369, `dispatch_table.py` 171, `calibration/{union_stats,census}.py` 944) and `rola/ops/consumer.py` 387; shrinks to `interface.py` (the private seam, `RoLAConsumer`), `expert.py` (the planner re-exports), `layers/rola.py` (`feasible_BC`, `state_block`, `last_selected_plan`), `ops/paging.py` (`plan(schedule)`, `adopt_dense`, `atom_bitmap_from_schedule`), `routing/factors.py` (`views()`, `summaries()`). **the tiled MEASUREMENT apparatus, as ONE capability**: `benchmarks/probe/` whole (1,144 lines), `rola_probe.py`, `bench_regression.py`, `test_latency_regression.py`, `atscale_probe.py`, `bench/{cells,drivers,instruments,pairing}.py` (1,318), `bench_consumer.py`, `bench_api_stats.py`, and the harness's own gate `tests/unit/test_probe_harness.py` (331 lines, −17 tests). `tools/chunk_ladder_parity.py`. | **THE PROTECTED SURFACE IS THE PROOF, and it was measured rather than argued.** `rola/_C*.so` was stashed before the csrc deletion and both binaries dumped with `tools/sass_bodies.py` (hash version 2 asserted equal on both sides): **378 device entry points before, 310 after; the 68 that left are exactly the consumer's; over the 310 protected bodies 0 missing, 0 extra, 0 differing** -- chunk 138, entmax 128, decode 40, summaries 2, byte for byte. The include graph agreed with the inventory before a file was touched (no chunk/decode/entmax/summaries/paging source includes any member of the doomed set; the only cross-edge, `summaries.cuh -> arch_table.cuh`, survives). Device-side afterwards, which is the only check a stale binary cannot pass: the bound entry list is exactly `chunk_* (7), entmax_* (2), rola_decode_* (4), rola_routing_summaries, rola_vmm_*, RoLAVmmOwner` -- no `rola_consumer_*`, no `rola_dispatch_table`. The MEASUREMENT APPARATUS could not be checked-and-kept: every entry in `probe/registry.py::PROBES` named `consumer_forward_kernel` and a `bench.cells` cell, and `probe/{duration,repro}.py` imported `CELLS`/`_timed`/`_paired_one_kernel` directly, so with its fixtures gone the harness had no subject and could not import. It went as one capability, and what SURVIVES is named: `bench/{stats,regression,ledger}.py` (the paired signed-rank verdict, the per-cell IQR thresholds, the append-only record), still gated by `tests/unit/test_perf_ledger.py`, with `settle_clocks` and the instrument-identity rule moved into `regression.py` because live callers need them. What is LOST is named too, and is what the chunk-arm harness card OWES: the interleaved paired-ratio engine (`_paired_one_kernel`, the ~7x bias-reduction rule), the discipline gates (lock, dirty-tree refusal, clock-settle refusal), per-cell latency baselines, and a locking statement of its own -- the standing `rola-probe` exception to the flock rule is retired with the harness (`docs/measurement.md`, `docs/testing.md`). `benchmarks/cells/*.json` are PARKED, never silently dropped: their README states they are the tiled consumer's historical record and that no chunk number may be compared against them. Regenerated build world: 5 generated files (was 10), 42 instantiations over 3 shards, 147 ratified entries per arch (was 179), build EXIT=0, `chunk_sass_gate` PASS, ratify PASS over 294 bodies. Battery 1147 -> **1118 passed, 13 skipped, 0 xfailed**; the −11 in the csrc commit is parametrization only, MEASURED (`test_docs_mirror` + `test_shard_partition` collect 72 rows before and 61 after, one row per source file and per shard file). Revival for every line above: `git show c7eaeb9:<path>`. |
| 2026-08-14 | `aa2fb75` | **Two zero-caller files outside the charter's lists**, both keyed on symbols D2 deleted: `tools/verify_rename_transform.py` (148 lines -- the one-shot gate for the completed `fla_rola::v3 -> rola` rename) and `benchmarks/microbench/p14_emission_probe.cu` (a standalone probe reproducing `consumer_kernel.cuh:951-961`'s emission pattern at an old SHA). | Flagged rather than assumed, which is why they are their own row. The rename gate's DECLARED TRANSFORM names `v3_consumer_forward_kernel` and its pre-rename manifest inputs no longer exist, so it could not run and could not be repaired without inventing a subject; its result is banked in `docs/open-work.md`'s W1 row (every register and spill byte identical under the declared rename), and that citation now says the gate is gone. The microbench's subject is a deleted source's deleted lines. Neither had a caller anywhere in the tree. Revival: `git show c7eaeb9:<path>`. |
| 2026-08-14 | `35224d8` | **P67 D2-d: the ten ORPHANED mirror docs, and `docs/architecture/planner.md`'s derivation.** `docs/internals/{consumer,consumer_kernel,consumer_launch,cp_async,host_api,intra_panel,mma,mma_tile,readout_mma,smem_layout}.md`. `planner.md` keeps its file and its opening rule; what leaves it is the mechanism -- `derive_bc`'s three ceilings and their falsifiers, `build_plan`/`plan_and_schedule`, the expert `BC` pin, `select_window`'s four refusals and the `GT` residency rule -- rewritten down to the rule, what decides a launch in its place, and the `GT` verdict a measurement outlives its axis by. | Rule 1 of `tests/unit/test_docs_mirror.py` runs SOURCE -> DOC, so a deleted source leaves an orphan doc that nothing catches; deleting the doc with the source is therefore part of deleting the source, exactly as `segment_mask.md` was handled at P26. Every citation INTO the ten was repointed or made explicitly historical before they went (`fp32-accumulators.md`, `self-masking-theorem.md`, `guarantees-choices-preferences.md`, `no-delta-rule.md`, `factored-representation.md`, `decode.md`, `dispatch_switch.md`, `arch_table.md`, `README.md`), so the tree has no dangling reference and `test_docs_mirror` is green on all five rules. `planner.md` was rewritten rather than deleted because the RULE outlived the machine that enforced it and is cited from `architecture/README.md` and `factored-representation.md`: a tree with no cost model should say why it has none. Revival: `git show c7eaeb9:docs/internals/<name>.md`, `git show c7eaeb9:docs/architecture/planner.md`. |
| 2026-08-14 | `2a87552` | **The benchmarks' two dead arms.** `benchmarks/bench_scaling.py`: the `--norm` option's `raw` choice and the whole `--decay` option, with the `decay_on` axis behind them (`run_v3_cell`'s two parameters, the `LearnedDecay` import and construction, `decay_config`, and the `decay=` threading into `derive_plan` / `plan_and_schedule` / `build_plan` / `_consumer_forward`), plus `_addressing`'s `norm` parameter. `benchmarks/bench_paging.py`: the unreachable `16,16,16,16` default. | PRE-EXISTING ROT, and both arms RAISED on their first cell rather than reporting anything. `normalization='raw'` is refused outright by `validate_normalization` (the mass column is unconditional -- raw died before these benchmarks were repointed), and a decay-configured layer dispatches every arm to the fp64 oracle, which each cell's own `last_execution_backend == 'cuda'` assert then fails -- precisely so an oracle timing can never be reported as a kernel one. The `norm` and `decay` COLUMNS stay in the emitted row as the constants they now are, so a reader comparing an old JSON against a new one is comparing the same arm. `bench_paging`'s default topology had no built chunk arm, so the benchmark's default invocation asserted instead of measuring; the default is now the deepest BUILT cell (`8,8,8,8`). S3b's D6 flagged all of this to S5; the surviving arms of both files were run to prove they execute. |
| 2026-08-14 | `de81e3d` | **`VmmOwner`'s SECOND PLANE, and the reporting name that lied.** `csrc/rola/src/paging/vmm_owner.{cuh,cu}`: the `mass_` plane, the `global_mass_` axis that selected it, `mass_base()` and its binding, the `Plane` and `GrowthMark` structs with `grow_plane` / `rollback_growth` / `release_planes` / `reserve_planes` / `validate_boundary`'s two-plane machinery, `checked_add`, and the `bp`/`dqk`/`dv` constructor parameters (`value_base()` -> `base()`). `rola/ops/paging.py`: `PageArena.resident_pages`'s old meaning and `PageArena.resident_bytes()`; `PagingResult.resident_pages`, `.resident_bytes` and `.resident_fraction`. | The mass plane was V2 GEOMETRY that no caller had ever constructed: the arena's page is `[MMA_K_QUANTUM, d_v + 1]` fp32 with the mass column FUSED into `cols` (raw is dead), so a separate `[pages, bp, dqk]` plane had no producer, no consumer, and no shape the kernel could address. `mass_base` was reachable only from pybind and called nowhere; `grep` for `VmmOwner` outside `csrc` found no Python caller at all before this commit, which is what made the whole class safe to re-geometrize rather than adapt. With ONE plane the `GrowthMark` journal is degenerate -- `grow`'s own inner unwind already leaves the plane untouched on any failure -- and `checked_add` had no remaining caller. The reporting rename is the S4 adjudication item 3: `resident_pages` returned `ExtentAllocator.allocated`, the allocator CURSOR, which includes the alignment gap a batch opens and is therefore an upper bound on residency rather than residency; it is now `allocated_pages` (the truth, host-side and free) and `resident_pages` is the page table's popcount (exact, one sync, reporting only). `resident_fraction` went with it because the fraction that matters is `committed_bytes / dense_bytes` -- what the driver actually holds against the unpaged counterfactual -- which is `committed_fraction`. Every quoting site moved in the same commit: `tests/unit/test_paging_atoms.py`, `tests/tier2_geometry/test_chunk_paging_equivalence.py`, `tests/tier2_geometry/test_decode_paging.py`. |
| 2026-08-14 | `50a2a14` | **The per-STACK stateful surface, and the adapters onto it.** `rola/state.py`'s `LayerRecurrentState`, the layer-indexed `RoLAState` (`update`, `get_seq_length`, `reset`, `detach`, the container protocol) and `as_layer_state`; `rola/utils/` whole (`get_layer_cache`, `require_cache_layer_idx`, `update_layer_cache`); `rola/integrations/` whole (`RoLAHFCache`); `RoLA.forward`'s `past_key_values` and `use_cache` parameters and its third return value; `tests/unit/test_state.py` and `tests/layers/test_decode_cache_isolation.py` | REPLACED, not withdrawn: the P67 paging integration makes the carried state ONE object per sequence (`rola.state()` -> `RoLAState`, docs/internals/state.md), owning a paged or dense backing that a per-layer dict of raw tensors cannot express. The predicate `state is not None` replaces `use_cache` exactly, at the op and the layer alike, and the layer is a pass-through -- so the layer-indexing container had no remaining job, and `as_layer_state`'s adaptation point had nothing to adapt to. `test_decode_cache_isolation.py` gated `use_cache=False` not mutating a caller's cache; there is no such mode to get wrong (a call either carries a state or does not), and the decode arm's clone-before-mutate survives in `RoLA._decode`. The `transformers` shim is a wrapper at a model boundary if it is ever wanted again; nothing in the tree imported it, and no checkpoint or trained model exists to strand. |
| 2026-08-14 | `8397b51` | **`max_physical_pages`, the paging allocator's ceiling, end to end.** `PageArena.__init__`'s parameter and attribute and the storage sizing that read it; `ExtentAllocator`'s refusal text; `adopt_dense`'s cap guard; `PlanOverrides.max_physical_pages`; `RoLA.paging` (the `cap-implies-paging` property) and the layer's arena branch (`_resolve_page_arena`, the `adopt_dense` continuation bridge, the reference-arm paging refusal); the C++ ceiling name (`VmmOwner::max_physical_pages` -> `dense_limit_pages`, the virtual reservation it sizes) and its binding; `tests/layers/test_layer_paging.py` whole | A CAPABILITY REMOVAL on rule 2. The footprint is PLANNED-EXACT: `plan_exact` commits the routing's exact write set, so a ceiling over it is either never-binding (the default) or a self-inflicted refusal of a launch the machine could serve. What sized storage now is the dense limit, reserved VIRTUALLY under the VMM owner and committed per plan, so the parameter did not even bound the allocation it appeared to. Layer-level paging went with it because paging is not a layer configuration any more: the backing is the STATE's, chosen when it binds, default ON, with `PlanOverrides(paging=False)` the surviving pin (`paging=True` is refused -- there is nothing to turn on). `test_layer_paging.py` gated the arena through the layer's expert flags and `use_cache`, both of which are gone; the invariants it carried live in `tests/tier2_geometry/test_chunk_paging_equivalence.py` (bit identity, zero-page inertness, absent-atom skip, `BC`-invariance) and `tests/tier1_semantics/test_state_facade.py` (the two backings through the public surface). The two cells that tested the CAP itself are retired in place with a note: `test_an_exhausted_arena_REFUSES_and_never_grants_a_prefix` now gates `ExtentAllocator` directly, and `test_a_near_full_but_fitting_arena_survives_a_real_launch` keeps its planned-exact half. |
| 2026-08-14 | `8fabf0e` | **The decode path's PAGING REFUSAL, and the caller-side launch machinery behind it.** `decode_forward`'s `paging=` parameter and its `NotImplementedError` ("the decode path is unpaged only"), the matching `TORCH_CHECK` prose in `decode.cuh`; `FrozenDecodeConfig.page_states` and `.atoms_per_bh`; `RoLA._decode_config`, `RoLA._decode_workspace`, `RoLA._resolve_decode_config` and the `_DecodeConfigCache` dataclass; `docs/internals/decode.md` §4 "Unpaged only, deliberately" | REPLACED, not withdrawn, and each piece by the thing it was a placeholder for. The refusal existed because a paged decode address "would today have to know `BC`"; §4.5's atom re-key is what makes it `BC`-free, and the walk now resolves a slot per touched atom under both backings (`decode.md` §4) with the dense form byte-identical to the refused-era one on five topologies. `page_states`/`atoms_per_bh` were never read by anything; the atom count is `config.N // MMA_K_QUANTUM`. The layer's carrier cache moves ONTO THE STATE (`RoLAState._decode_carrier_cache`, `._decode_workspace`) because a frozen-at-the-boundary carrier is per-SEQUENCE, not per-module -- a layer-held one was already wrong for two concurrent sequences, and the `conv_state` beside it is the precedent. **Not a deletion-only commit**, against this ledger's own rule: none of these five is a standalone removal -- each is one half of a replacement, and a commit holding only the deletions would not build. Recorded here rather than split. |
| 2026-08-14 | `e4e9ed6` | **Hygiene batch B bench/perf surface.** `benchmarks/bench.py` (Tri Dao vendor helpers, zero importers); `benchmarks/bench_consumer.py` (the old F6-reproduction script against the legacy tiled consumer) and `benchmarks/p39_window_probe.py` (a one-shot window-acceptance probe on the same legacy tree), both copied to the development journal before removal; `perf/p22_remedy_recompute.py` + `perf/p23_revert_recompute.py` (one-shot ledger recomputes whose outputs are ledger rows), journaled likewise. `benchmarks/bench_consumer_new.py` renamed to `bench_consumer.py` (drift-pair collapse: the dispatcher is the only consumer CLI). | Unreferenced vendor code and one-shot scripts whose subjects are either the doomed legacy consumer or numbers already banked in `perf/ledger.jsonl`; the journal keeps the scripts, git keeps everything. `tools/ledger_import.py` was RESTORED in the same commit -- its input file remains in-tree as provenance of the six banked probe_version-0 ledger rows. |
| 2026-08-14 | `60836ff` | **Hygiene batch A dead files.** `benchmarks/bench_occupancy.py` (broken: reads plan fields deleted with the old planner surface); `tools/ledger_import.py` (removed as unreferenced -- REVERSED in batch B, see the row above). | The occupancy bench was unrunnable, not dormant; the import tool's removal was corrected once its provenance role was recognized. |
| 2026-08-07 | `59e3f8b` | **`mma_tile_fragA`'s generic `TB`/`B_T` form** (`csrc/rola/src/mma_tile.cuh`) — the templated `d += Af . B` against a caller-chosen storage type and orientation | No caller left: it existed for exactly one B, the fp32 state, whose narrowing to bf16 happened at the fragment because the fp32 state had no fragment-ordered or `ldmatrix`-fetchable form. The state's numerator is now two `StageLayout` bf16 planes (`Shi`/`Slo`, [`smem_layout.md#state-planes`](smem_layout.md#state-planes)) fetched by the same `ldmatrix` atom `Vw` already used (`stage_frag_b`, generalised from `vw_frag_b`), and `mma_tile_fragA_stateB` replaces the generic template at its one call site. Nothing else ever instantiated the generic form — a `TB`/`B_T` axis with one caller is a second spelling of that caller's own type, not genericity. |
| 2026-08-07 | `be7004e` | **The `int K` runtime-bound argument of `mma_tile_fragA_stateB` and `mma_tile_fragA_massB`** (`csrc/rola/src/mma_tile.cuh`, `consumer_kernel.cuh`) | Dead since `589b383` deleted the COMPACT executor arm: `K` was the realized (possibly partial) contraction extent under a gather that could stop short of `KMAX`; with COMPACT gone, every surviving call site passes `K == KMAX`, so the loop's `if (ks * kMmaKQuantum >= K) break` was a branch that could not be taken and the compiler was already folding the compare away. Deleting the parameter makes the trip count purely the template's (`KMAX`), which is also what the two-stage software pipeline this commit added needs: a pipelined loop stages `i + 1`'s fetch before `i`'s multiply, and an early-exit branch mid-pipeline has no well-defined "next stage" to have fetched. |
| 2026-08-07 | `59e3f8b` | **`SP = DV + 4`, the state's row pitch, and its scalar bank-conflict derivation** (`csrc/rola/src/consumer_kernel.cuh`, `smem_layout.cuh`) | The pad existed to decorrelate GEMM 1's B-operand banks for a SCALAR fragment map (`k = k0 + 2r` collided up to four ways at a row extent of `DV`, a multiple of 32) that packed the state out of SMEM by four loads, four clamps and two packs per `mma.sync` per lane. That scalar map is gone: the state's numerator now lives as two `StageLayout` bf16 planes (`Shi`/`Slo`) at the SAME swizzled layout `Vw` uses, addressed by `state_index<DV>` and fetched by `ldmatrix`, which needs no pad to stay conflict-free — the swizzle decorrelates banks by permuting within the tile instead. The carve shrinks from `BC*(DV+4)*4` to `BC*(DV+1)*4` (-768 B at BC=DV=64) as a direct consequence, not a separate optimization. Five `constexpr` proofs (`stage_offset_matches_cute`, `stage_layout_is_bijection`, `stage_ldsm_banks_ok`, `stage_pair_banks_ok`, `stage_store_banks_ok`, all `<BC, DV>`) replace the single `banks_distinct(1, 2 * SP)` assert, enumerated in [`smem_layout.md#state-planes`](smem_layout.md#state-planes). |
| 2026-08-07 | `a8f2e3c` | **P30: the SELECTION seam and the cost model, whole.** `rola/planner/select.py` (921 lines: `Selector`, `CostModelSelector`, `FixedChoiceSelector`, `DEFAULT_SELECTOR`, `select_with`, `SELECTOR_INTERFACE_VERSION`, `StatisticsBundle`, `ConfigChoice`, `ConfigEvidence`, `select_config`, `select_config_with_evidence`, `config_for_step`, `cost_per_token`, `waves`, `atomic_work_per_token`, `mean_quantum`, the four `RULE_*` constants and `RULE_TABLE_PROVENANCE`, `C_SATURATION`, `C_ATOMIC_COEFF`, `C_COLLISION_GAMMA`, `ShadowRecord`, `shadow_step`, `StaticPlanInputs`, `HardwareFacts`); `rola/planner/calibration/auditor.py` whole; `cost_model.py`'s model half (`plan_cost_per_token`, `plan_waves`, `TILE_FIXED_WORK`/`READOUT_FOLD_KSTEP_WORK`/`INTRA_GRAM_KSTEP_WORK`/`DECAY_KSTEP_WORK`, `COST_MODEL_PROVENANCE`, `PlanAxes`, `select_plan_axes`, `select_plan`) — its surviving half, `TileUnionStats`/`tile_union_stats`, moves to `calibration/union_stats.py` as a bench MEASUREMENT instrument; `rola/planner/schedule.py`'s PEP-562 relocation proxies, `OccupancyOracle`, `resolve_occupancy`, `_SUPPORTED_BC`/`_SUPPORTED_DV` | A CAPABILITY REMOVAL on rule 2, and the capability had no alternatives left to select among: one executor, one token quantum, and `BT`/`PREC`/packing/`P` all deleted ahead of it, so the only planned quantity is `BC`. `BC` is now a CLOSED FORM over launch-static inputs (`rola/planner/derive.py`: the largest compiled block clearing `subtree_ceiling`, `box_admitted` and `fill_ceiling`), which is why the machinery GOES rather than shrinks — a formula of launch-static inputs cannot go stale, so capture invalidation, the shadow ledger, the regret metric and the agreement auditor are all answers to a question that no longer exists. The occupancy oracle goes with them: the box term prices the per-CTA shared-memory budget from the binary's OWN `constexpr` carve (the dispatch table's `smem_bytes`, which since change-2 already includes the cooked schedule ring), so residency is not an input to anything — and the executor hides latency inside one CTA, so co-residency was never what the shared memory was for. What replaces the coefficients is three derivations each carrying its own falsifier at its definition, gated term by term by the new `tests/unit/test_derive.py` (19 tests, one joining the box arithmetic against the built binary's real carve). `rola.expert` loses `ConfigChoice`, `ConfigEvidence`, `Selector`, `CostModelSelector`, `FixedChoiceSelector`, `StatisticsBundle`, `select_with`, `select_plan`, `PlanAxes`, `StaticPlanInputs`, `HardwareFacts` and gains `derive_bc`, `MachineFacts`, `build_plan` — an API change on a marked door, taken deliberately. `csrc/` is byte-untouched (ratify: source freshness OK, SASS-body gate 224/224 unchanged): this is a python-plane deletion. |
| 2026-08-07 | `a8f2e3c` | **P30: `FeederFlags` and the §4.4 per-level density word, end to end.** `rola/planner/feeder.py` whole (`FeederFlags`, `MAX_FEEDER_LEVELS`, `BITS_PER_LEVEL`, the two-trigger `derive`, `packed`/`unpack`, `side_dense`), `ConfigChoice.feeder_flags`, `FrozenDecodeConfig.feeder_flags`, and `freeze_decode_config`'s `frozen=`/`static_density=` kwargs with the packing-equality plumbing between them | A CAPABILITY REMOVAL, and the capability was ALREADY UNREACHABLE: `grep -rn 'feeder\|dense_read\|dense_write' csrc/rola/` returns zero hits — the endgame consumer has no dense feeder variant to select. The one surviving host consumer was decode's `n_split`, whose own code sizes the split from `m_ceiling = N` unconditionally, so the word could not move that either. The declaration the flags carried is not lost: `StaticDensity` stands and is read by the routing (`rola/routing/factors.py`, `summaries.py`), where it is a fact about the producer rather than a kernel dial. |
| 2026-08-07 | `a8f2e3c` | **P30: the §6b decode-freeze carrier.** `FrozenConfig`, `freeze_for_decode`, `is_decode_step`, and `rola/layers/rola.py`'s construction of one at the prefill → decode boundary | A CAPABILITY REMOVAL whose object dissolved: the carrier existed so a decode step could inherit a SELECTED config without re-running selection on a one-sample statistic. With selection deleted there is nothing to bypass, and both its fields are independently gone — `feeder_flags` is dead in the kernel (row above) and `BC` is not a decode input at all (`FrozenDecodeConfig` has never carried it; decode is BC-blind by construction, asserted byte-identically across prefill `BC` by `tests/tier2_geometry/test_decode_bc_blindness.py`). The state-layout latch it also stood for is not weakened: `BC` fixes the state layout, and the derivation reads only launch-static facts, so two steps of one sequence derive the same `BC` by construction rather than by being frozen. |
| 2026-08-07 | `a8f2e3c` | **P30: the plan's selection provenance.** `Plan.occupancy_blocks_per_sm`, `Plan.selector_name`, `Plan.selector_status`, `PlanOverrides.selector`, and `rola/interface.py`'s `_HARDWARE`/`_hardware_facts`/`_static_inputs`/`_static_tier0` | A CAPABILITY REMOVAL on rule 2. The three `Plan` fields answered "who decided, at what residency, and was that decider audited" — three questions with one answer now, and a field whose value is constant is not provenance. `PlanOverrides.selector` was the swap point of an interface with one implementation left and nothing to swap to; the pin that survives is `state_block`, which REPLACES the derivation and is still alignment-validated (and, since `557c0ab`, refused host-side by name when it names an arm the binary does not carry). `Plan.sm_shared_bytes` is KEPT and re-scoped in place: still provenance-only, read by no rule, because the box term prices the per-CTA block limit rather than the per-SM budget. |
| 2026-08-07 | `a8f2e3c` + `557c0ab` | **P30: the test and bench surface of selection.** `tests/unit/test_selector_interface.py` whole (13 tests: the seam's version refusal, the fixed/default selector pair, `select_with`, `select_config`'s frozen signature, the bundle's `expose`, the per-`BT` statistic, the decode bypass's precedence, the plan's selector stamp, the table's refusal of an armless choice — the last re-anchored in `test_dispatch_table.py`, which owns it); `test_planner.py` −9 (the cost model's calibration reproduction, the absolute-instruction recording, the two auditor-ledger tests, `select_plan_axes`, the BC-generic selector probe, the decode path's zero-statistics claim, the feeder-flag packing, the lazy-call-proxy resolution); `test_decode_capacity.py` −2 (both feeder-flag packings); `test_api_contract.py` −1 (the selector swap); `test_decode_family.py`'s density-declaration drift test, REPLACED by `test_the_decode_split_is_performance_only_under_drift`, which varies `n_split` directly instead of through a deleted proxy; `test_consumer_vs_oracle.py`'s `select_plan` gate re-pointed to `build_plan`; `test_dispatch_table.py`'s tuple pin narrowed to `derive.SUPPORTED_BC`; and the bench harness's `run_btab`, `arm_time(bt=)`, `Cell.plan(bt=)` and `bench_consumer_new.py --mode btab` / `--bt` | Every deleted test's SUBJECT is deleted code; none carried a claim about a surviving mechanism. The two that did — decode's `n_split` invariance and the plan-built path's oracle exactness — are re-anchored rather than dropped, and are named above. The `bt` cascade is P26 debt this batch surfaced: those instruments read `plan.BT`, a field that has not existed since the `BT` axis died, so they were unrunnable rather than merely dormant. Replacing them: `tests/unit/test_derive.py`, 19 tests, one per term (the structural ceiling and its saturation below the ladder, the box's arithmetic against the binary's own carve and its two distinct refusals, the fill guard at small `N` and its saturation, the composition, the downward clamp, the alignment defect, the expert pin, and the purity of the whole). |
| 2026-08-05 | `505bb05` | **`graph_zero`** (`rola/ops/naive.py`): the `sum(x) * 0` term added over every input to both of `naive_rola`'s outputs | A CAPABILITY REMOVAL on rule 2, and the capability was a defect: it manufactured autograd edges so that inputs the result does not depend on came back with a ZERO gradient instead of `None`. Nothing in the mathematics has that term. It mattered because the card's backward conformance makes the reference backward *autograd through this oracle*, so a manufactured graph edge becomes a manufactured gradient in every backward gate anchored to it — and the one input it concealed is the one the spec detaches on purpose (`c[t,s] = stop_gradient(...)`, the stored clock). What replaces it is a stated contract rather than a silence: `naive_rola`'s docstring says an unused input gets `None`, and `tests/unit/test_oracle_contract.py` asserts it, asserts that every operand the result DOES depend on still gets a real nonzero gradient, and asserts that the stored clock moves the forward — so the first claim cannot be vacuous. A caller that wanted the old zeros writes `torch.zeros_like` at its own call site, where the expectation belongs. Measured: the dual run reports exactly 256 quantities changing, all of them `*/dstored*` going from a materialized zero to `None`, and nothing else. |
| 2026-08-05 | `474f43d` | **`_fold_bth`** (`rola/ops/naive.py`), the `[B,T,H,*] -> [B*H,T,*]` fold; the `view/permute/contiguous/cat` epilogue that undid it; and the SEPARATE `mass_state` recurrence and its `einsum("bs,bs->b")` denominator | Not a capability removal — the function computed by this file is unchanged, and the dual run says so: of 1328 quantities only the `global` cells move, none of them a `state`, worst `1.155e-14` against a declared reassociation bound of `1e-12` relative. It IS a deletion mixed with a rewrite, against this ledger's deletion-only-commit rule, and stated plainly rather than split artificially: each of these three things ceased to have a caller BECAUSE the recurrence changed shape, so a deletion-only commit ahead of the rewrite would not have compiled and one after it would have deleted nothing. The fold existed to vectorize `BH` into one batch dimension — a layout argument with a silent copy in it that the textbook form has no step for; the epilogue existed to undo it; and the mass recurrence existed because normalization was modelled as a second state instead of as the ones column it is. The rewrite is the audit's decomposition step 3 and the reason it was ordered last: it buys literalness, not new independence. Everything the deleted code did is done by the surviving loop, and `tests/unit/test_oracle_contract.py::test_the_ones_column_is_the_mass_state_and_nothing_else` proves the substitution against a construction the oracle does not share. |
| 2026-08-05 | `94a6537` | **The layer's flat configuration surface and the translate layer under it.** `rola/layers/rola.py`: `LevelRoutingConfig`, `resolve_level_routing_configs`, `_resolve_level_routing`, and the constructor parameters `head_v_dim`, `levels_per_head`, `states_per_level`, `level_routing_configs`, `decay`(str), `decay_target_leaf_rate`, `state_norm`, `router_bias`, `gain`, `gain_bias_init`, `gain_config`, `temp_bias_init`, `use_short_conv`, `conv_size`, `conv_bias`, `progressive_memory`, `paging`, `state_block`, `max_physical_pages`, `mma_precision`, plus `forward`'s `output_attentions` and its unpoliced `**kwargs`. `rola/routing/producer.py`: `produce_routing`, `RoutingOutput`, `resolved_routing_from_topology`, `_project_logits`, `embed_budget`, `_unconditioned_pass`, `_conditioned_pass`, `_split_level_mass`, `_split_and_fold_levels`. `rola/interface.py`: `rola_func`, `rola_forward` | A CAPABILITY REMOVAL on rule 2, and the removal IS the API change, which is why it is not a deletion-only commit: the reduction theorem says the public surface is linear attention's contract plus ONE slot, and each name above was either that slot spelled out flat (topology, widths, activations, normalization, gains, decay rate, conditioning), a launch decision the planner makes (`state_block`, `mma_precision`, `paging`, `max_physical_pages`), or tissue (`progressive_memory` was set, type-checked, stored and read NOWHERE; `output_attentions` was accepted and unconditionally ignored, and RoLA has no attention matrix to return). Nothing modelled is unreachable: every MATH knob moved onto the objects that own it (`RouteProducer`, `SimplexProducer`, the decay sources), every launch decision moved to `rola.expert.PlanOverrides` where pinning one is a named act, and the tissue has no replacement because it did nothing. The string routing vocabulary (`read`/`write`/`tied`/`alpha`) was STRICTLY LESS expressive than the tag vocabulary it translated into -- it could not express a level whose read is `entmax(2.0)` and whose write is `entmax(1.5)` -- so retiring it widens the configurable set rather than narrowing it; `IndependentRouting`/`UnionRouting` are the surface now, with `dense_routing()`/`sparse_routing()` as the two common spellings. Migration is in `docs/api.md`; no trained checkpoint exists anywhere, so no state_dict was stranded. |
| 2026-08-05 | `94a6537` | **The per-side short-convolution stream split** (`read_route_conv1d` / `write_route_conv1d` and the tie-structure predicate that chose between them) | A capability removal. The layer's three conv scalars became two MODULE slots (`value_conv`, `route_conv`), and the read/write route-stream split went with them: under the producer slot the layer no longer knows a level's tie structure -- that is the producer's own business -- so a layer-side predicate over `UnionRouting`/`tied` was the layer reasoning about semantics it had given away. A producer that wants two source streams takes two: `RouteProducer.forward(read_stream, write_stream)` carries the split, and a producer sees whether the streams are the same object. What is gone is the LAYER allocating a second convolution on the strength of a routing tag. |
| 2026-08-05 | `94a6537` | **The eager auxiliary statistics** -- `exact_support_counts` and `touched_bits` computed on every production call | Nothing in the library reads them: they exist for the budget losses and for diagnostics, and a grep over `rola/` finds no consumer. They are now derived on demand (`RouteFactors.support_counts()` / `.touched_bits()`) with the same values by the same expressions, so no caller loses a number -- only the launches on the steps that never asked. Priced at 3.00% of the step at `L = 4096` (evidence: `workflows/refactor/p12_lane3.md`, whose `audit_replay.py` re-derives every number from the raw probe JSON). |
| 2026-08-05 | `94a6537` | **The inverse copy pair between the producer and the planner's view** -- the producer's `contiguous()` unfold to `[B,T,H,w]` and the fold back to `[BH,T,w]` in `RoutingViews` | Not a capability: two copies of the same values, in opposite directions, per level per side per step. The solve already emits the layout the kernel's view wants, so the gateway normalizes in that layout and hands back a permuted VIEW for the public one; the public tensors carry identical values and shapes, and every consumer that reads them is unchanged. Priced at 2.82% of the step at `L = 4096` (same evidence document). |
| 2026-08-05 | `f3ea01a` | **164 lines of hand-rolled fp64 entmax/sparsemax reference numerics**, from `rola/routing/entmax/reference.py`: `entmax_tau_bisection` (an 80-step bisection with a hand-proved bracket), `sparsemax_forward_closed_form`, `_support_mask`, `EntmaxFn` (the hand-derived analytic VJP autograd wrapper), and the `n_iter` parameter that configured the bisection's depth, plus the four call sites that named it | A CAPABILITY REMOVAL, not only code, on this ledger's rule 2: `sparsemax_forward_closed_form` was **a second, independent sort-based algorithm existing only to cross-check the first** (the module's own words) — an in-tree second opinion, now gone. It was safe to remove because it was superseded by a stronger one, not because it became unreachable: the matching-the-naive rule says a gate comparing an implementation against code we also wrote proves self-consistency and nothing else, and this 189-line reference was entirely ours. `entmax` (PyPI, DeepSPIN) — the paper authors' own forward and backward — replaces it and is MORE exact besides: `entmax15`/`sparsemax` are closed forms (sort/topk), not iterations, so the threshold is solved rather than bisected, and the dual-run protocol measured the swap at 1.421e-14 worst continuous difference against the reference this commit's PARENT (`f3ea01a^` = `7e98ac6`) still carried, with 0 support-set mismatches. The independent second opinion this deletion removes is not reproduced by the swap — it is what the swap is FOR (matching-the-naive) — so if a future reviewer wants a second sort-based cross-check restored rather than a single third-party path, it revives from `f3ea01a^` (`7e98ac6`), the last commit carrying it. |
| 2026-08-05 | `589b383` | **The COMPACT executor arm, whole.** `csrc/`: the consumer's support-driven gather (`c_idx`, `c_slot`, `c_count` and the two-pass ballot in `stage_tile`), every extent it made runtime (`K`, `KI`, `Kpad`, `KIpad`, `KpadM`, `wt_rows`, `n_mt`, `qmax`), `mma_tile_fragA`'s `B_GATHER` indirection and its `b_row` operand, `consumer_smem_bytes_at`'s gathered-list term, the `COMPACT` template axis of the kernel / `launch_consumer_arm` / `query_consumer_occupancy_arm` / `consumer_smem_bytes` / `consumer_ring_depth` / `ShardRow`, and the `compact` argument of `rola_consumer_forward`, `rola_consumer_occupancy` and `rola_consumer_smem_bytes` — the matrix falls from 512 arms to 256. `tools/`: the axis in `gen_shards.py`'s matrix definition. `rola/`: `Plan.compact`, `ConfigChoice.packing`, the `compact` argument of the `OccupancyOracle` contract, `select_packing`, `pack_breakeven`, `derived_pack_breakeven`, `C_PACK_BREAKEVEN`, `compact_is_structurally_inert` (rung P) and `FeederFlags.any_side_dense`, the gather coefficients `RULE_GATHER_FIXED_WORK`, `RULE_GATHER_QUANTUM_WORK`, `GATHER_CANDIDATE_FIXED_WORK` and `GATHER_CANDIDATE_QUANTUM_WORK`, the packed branch of `cost_per_token`, and the compact plumbing in `ops/consumer.py`, `ops/decode.py`, `layers/rola.py` and `routing/sequence_parallel.py`. `tests/`: `tier2_geometry/test_compact_equivalence.py` (a whole file — the tree's dense-degeneracy bit-identity gate, whose subject was the two compact arms' equality at `K == BC`), the `COMPACT` axis declaration and geometry pair in `tier2_geometry/matrix.py`, `test_a_saturated_dense_cell_never_argmins_onto_a_compacted_arm`, and every compact cell of every surviving parametrization. `benchmarks/`: the compact arms of `bench_consumer.py`, `bench_regression.py`, `bench_scaling.py` and `bench_sequence_parallel.py`, and their rows in `cells/latency_baseline.json` | A CAPABILITY REMOVAL, ruled by the user on 2026-08-05: the arm doubled every gate's cost and the planner selected it nowhere (`C_PACK_BREAKEVEN` shipped EMPTY, so `select_packing` declined every cell, and rung P excluded the arm structurally on the dense-flagged side). The surviving dense arm is what every shipped plan ran. The evidence that this is a specialization and not a rewrite is the binary: over the 256 surviving arms the SASS bodies are unchanged, and the only absences are the 256 COMPACT entries themselves. The design survives on the P14 big-BC card and the code in this commit's parent; a measured cell that wants a gather brings it back with `git revert`, not from memory. ONE GUARD WENT WITH THE ARM AND IS REPLACED BY A MEASUREMENT, not by an assertion: `test_a_saturated_dense_cell_never_argmins_onto_a_compacted_arm` made the cost model's known `BT` inversion unreachable by construction (rung P excluded packing on a dense-flagged side), and with no compacted arm it cannot be restated. What bounds the limitation now is that the shipped closed form reproduces the MEASURED argmin `BT = 32` on all four saturated-dense rows of the calibration table (`topo-D2-w64x64-BC64` and `topo-D2-w64x16-BC16`, decay off and on) — the two decay rows being exactly the cells the inversion was measured on — so the inversion moves no decision at any cell it was measured at. `RULE_TABLE_PROVENANCE` and `cost_model.py`'s provenance string carry that sentence, and `tests/unit/test_planner.py`'s calibration gates assert it. |
| 2026-08-05 | `589b383` | `rola::panel_clock_prefix()` (`csrc/rola/src/intra_panel.cuh`) and `rola::readout_fragment()` (`csrc/rola/src/readout_mma.cuh`) | No caller anywhere in the tree. The kernel computes the clock prefix inline in `stage_tile` and reads the state through `mma_tile_fragA`, so both were earlier lowerings kept beside the ones that ship. Found while removing the `COMPACT` axis they both took as a template parameter — deleting an unused parameter of an unused function is how a dead symbol survives an audit, so the function goes instead. Neither emitted code (`__device__ __forceinline__`, uninstantiated), so the SASS of every surviving arm is unmoved. |
| 2026-08-04 | `bcd9a16` | `rola::mma::mma_m16n8k8_tf32x2` (`csrc/rola/src/mma.cuh`) — the two-term tf32 split | No caller anywhere in the tree (`.cu`, `.cuh`, `.cpp`, `.py`). The engine's exactness arm is `tf32x3`, the three-term split, which is what `mma_precision = "tf32x3"` selects and what the fp64-oracle gate runs; the two-term variant is a rung that was measured and not taken. It is a `__device__ __forceinline__` function with no callers, so it emitted no code — the SASS gate is unmoved by its removal, which is the evidence that nothing depended on it. |
| 2026-08-05 | `5ee4cf0` | `rola::consumer::kFoldPrecDefault` and `kReadPrecDefault` (`csrc/rola/src/consumer_kernel.cuh`) — two `MmaPrec` constants naming the production point of the precision axis | No caller anywhere in the tree. Every arm takes `READ_PREC`/`FOLD_PREC` as template arguments supplied by the generated instantiation matrix from the plan's `mma_precision` field, so a DEFAULT for an argument that is always passed is a second spelling of the production point, and a second spelling is what goes stale. The axis and its contract were unchanged and stated where the choice was made, in a `consumer_kernel.md` section (`#mma-precision`) that no longer exists: the whole `PREC` axis it documented was itself deleted by the P26 endgame executor (the `PREC` axis row below), so the citation is left as prose rather than repointed at a description of something that is no longer true. Two `constexpr` values with no reader emit no code — the SASS gate is unmoved by their removal, which is the evidence that nothing read them. |
| 2026-08-05 | `5ee4cf0` | `rola::consumer::staging_slot_bytes()` (`csrc/rola/src/smem_layout.cuh`) — the per-ring-slot footprint of the staging set | No caller anywhere in the tree, here or on master. `consumer_smem_bytes_at` sizes the ring from the two carves it actually rings (`vw_union_bytes` and the two row scales) and `consumer_ring_depth` prices residency off those same carves; this function was a THIRD description of the same bytes, read by nothing and checked by nothing. What the staging set IS was stated at a `smem_layout.md` section (`#staging-set`) about the ring's membership; that section is gone along with the ring itself, deleted by the P26 endgame executor (the staging-ring row below) — the citation is left as prose rather than repointed at a description of something that no longer exists. A `constexpr` function template with no instantiation emits no code — the SASS gate is unmoved. |
| 2026-08-04 | `bcd9a16` | A commented-out profiler line at `benchmarks/bench.py:252` | A debug variant of the line immediately below it (`sort_by="self_cuda_time_total"`). Commented-out code is a claim that someone might want it back, made in a place where nothing checks it; the live line is one edit away from carrying the sort. |
| 2026-08-06 | `74524f1` | **The P10 deterministic split-K emission** (`csrc/rola/src/consumer.cu`): `EmitReduceParams`, `emit_reduce_kernel`, `emit_reduce_pass`; `consumer.cuh`'s `emit_num`/`emit_den`/`emit_rowmask` `ConsumerParams` fields (num/den return to atomic accumulators); `consumer_kernel.cuh`'s row-mask publish and single-writer slot stores (readout emission returns to `atomicAdd` guarded by an elementwise `!= 0.0f` skip); `host_api.cuh`/`rola/ops/consumer.py`'s `emit_works`/`emit_off` arguments; `rola/planner/schedule.py`'s `emit_reduce_order`, `Schedule.emit_reduce_works`/`emit_reduce_off`, and the ABI v3 bump (reverts to v2 -- the bump carried nothing else, confirmed by grepping every ABI-version consumer before reverting it); `docs/internals/consumer.md`'s `#emit-workspace` (A11b) section whole, including the ~137GB nc-scaling-cliff paragraph it was the only home for | A CAPABILITY REMOVAL, ruled by Blake on 2026-08-06 (`queue/completed/user/U08_determinism-price.md`): the mechanism made `y` bit-identical across reruns (CUTLASS parallel-split-K shape -- single-writer workspace, one host-fixed-order reduce kernel) but priced at **+23.0%** step time at the canonical at-scale cell (P22-remedy ledger, `perf/ledger.jsonl` + `workflows/perf/p10_splitk.md` -- over 3x the +7.1% figure it was first accepted against, since that figure was never ledgered and did not reconcile against either binary), and Blake ruled the price not worth carrying: "if determinism costs that much, i dont see a reason to keep it vs atomics." The revert is SASS-bit-identical to `8ce8797` (the last commit before P10) across all 352/arch instantiations, 0 diffs -- so register/spill counts, the latency baseline, and every downstream number return to their pre-P10 values exactly, not approximately. What is NOT removed: the honest nondeterminism it was built to fix survives as a rerun-floor test (`test_split_k_determinism_is_not_assumed`, reworked onto the SAME multi-owner cells P10 added, floor `5e-3` measured with margin over the actual `9.8e-4` worst rerun spread) rather than the vacuous single-owner-cell version that predated P10. | A hard EXTERNAL bitwise-reproducibility requirement (not an internal preference) AND a banded workspace reduction that fixes the price and the mechanism's nc-scaling cliff TOGETHER -- the design note for banding is in the deleted `consumer.md#emit-workspace` section, recoverable from this commit's parent (`70f607b`, `perf/deterministic-splitk`). |
| 2026-08-06 | `52a67d8` | **The aligned walk, tile candidacy and segment tile-ranges**: the candidate-word/`cand`/`next_cand` machinery, the tile-level `stage_tile`, `segment_mask.cuh` whole (`segment_word_mask`, the §6.4 tile-range mask) and every "which tile does this owner's segment own" computation keyed on tile indices | A CAPABILITY REMOVAL: the P26 endgame executor replaces tile candidacy with a STREAMED FINE SCAN over 32-token machine words (AND of the owner's fixed-level token rows, AND the token-granular slice-union word) feeding a group assembler that ballots members straight into `kMmaKQuantum`-token groups — there is no tile-candidacy QUESTION left to answer, coarse or otherwise, because intersections do not survive coarsening (AND-consumers must read fine). §6.4 segments are now WORD RANGES over that same scan stream (`w_lo`/`w_hi`, derived inline in `consumer_kernel.cuh` from `p.n_twords`), so the tile-range partition a segment previously owned has no successor object to own. Design and measurement: `workflows/perf/p26_endgame.md`. Revival = git (`52a67d8^`). |
| 2026-08-06 | `52a67d8` | **`docs/internals/segment_mask.md`**, this doc's own mirror | The source it mirrored (`segment_mask.cuh`, the row directly above) is gone, and rule 1 of `tests/unit/test_docs_mirror.py` requires a mirror doc to have a source to mirror. The mask arithmetic and its agreement contract it once described are superseded by the word-range segments documented at [`consumer.md`](consumer.md#all-or-nothing) and [`host_api.md`](host_api.md#sequence-parallel-block). Revival = git (`52a67d8^`, plus the P26 docs sweep that removed the doc itself). |
| 2026-08-06 | `52a67d8` | **The `BT` axis** (token-tile extent): the template parameter itself, every `BT`-keyed pitch (`BTP` as a function of a runtime-chosen `BT`, the old `WT`/`Gm` row formulas), the `bt16`/`bt32` shard split, and `BT`'s appearances in `tools/gen_shards.py`'s matrix, the dispatch key, and the residency/register-budget tables | A CAPABILITY REMOVAL, ruled with the endgame executor: the work unit is now always a GROUP of exactly `kGroupTokens == kMmaKQuantum` (16) gathered tokens — the MMA quantum — never a runtime-sized tile, so there is no extent left for `BT` to name. `consumer_reg_budget`'s former `BT`-keyed lookup collapses to a `{BC, DECAY}` key (the register working set does not measurably vary with the group size once it is fixed). Matrix: `consumer_forward_kernel<BC, DV, D, DECAY>`, 32 arms (was 256 over `BT x BC x DV x D x PREC x DECAY x GLOBAL`). Revival = git (`52a67d8^`). |
| 2026-08-06 | `52a67d8` | **The `PREC` axis**: the `MmaPrec` enum, the `tf32x3` three-term-split exactness arm and its `m16n8k8` MMA path, every `READ_PREC`/`FOLD_PREC` template parameter and the paired kernel/shard/dispatch-table entries they multiplied the matrix by, `mma.cuh`'s tf32 wrappers, and `mma_precision` as a plan/host-API argument | A CAPABILITY REMOVAL, ruled with the endgame executor: bf16 storage + fp32 accumulation is now the ONE precision story, with the fp64 torch reference as the oracle standing in for the exactness arm the tf32x3 split used to provide — a caller needing more precision than bf16 delivers runs the oracle path, which is also the correctness truth every gate already compares against, so the in-kernel exactness arm was redundant with the thing that validates it. `readout_mma.cuh`'s finite-clamp format guard is what remains of §12 rule 3 in this precision story. Revival = git (`52a67d8^`). |
| 2026-08-06 | `52a67d8` (consumer) + `4e7710a` (decode) | **Raw normalization / the `GLOBAL` template axis**, end to end: the consumer's `GLOBAL` template parameter and its raw-mode kernel arms (mass column and `num`/`den` divide made unconditional, `cols = DV + 1` always), `g_read` as a field (fixed at `1`: "`global` fixes `g_read := 1`"), and the decode GEMV's mirrored `GLOBAL` axis (`g_read` deleted end to end through the kernel, `DecodeParams`, the pybind entry, `rola/ops/decode.py`, and `FrozenDecodeConfig.is_global`) | A CAPABILITY REMOVAL: raw (unnormalized) output was a second, less-used arm of every instantiation, doubling the consumer matrix and the decode matrix for a mode nothing in the shipped plan selected. Decode's matrix fell `32 -> 16` (`DV(2) x D(4) x DECAY(2)`) on the `4e7710a` half of this deletion. Revival = git (`52a67d8^` for the consumer half, `4e7710a^` for decode). |
| 2026-08-06 | `52a67d8` | **The staging ring** (`ROLA_C_RING_DEPTH`, its run-ahead prefetch, and every ring-slot-keyed SMEM address) | A CAPABILITY REMOVAL: the ring existed to hide staging latency behind the ALIGNED WALK's tile loop, which this same commit deleted; with the streamed membership scan and its single-visit staging (`stage_group`), there is no second in-flight tile for a ring to buffer against. `tests/unit/test_ring_depth_agreement.py`, the three-party (`Plan.ring_slots`, the occupancy-oracle `ring_slots` argument, `ROLA_C_RING_DEPTH`) cross-check, was deleted with it in `9478267` once none of the three parties it checked still existed. Revival = git (`52a67d8^`). |
| 2026-08-06 | `52a67d8` | **The write-panel window** (`ROLA_C_WT_WINDOW` and the per-window fold cursors, `ctl_words(BC) == 3`) | A CAPABILITY REMOVAL: the window sub-divided a tile's write panel into planes so the fold could claim work before the whole panel was staged; the endgame executor's write panel is one plane sized to the group (`kGroupTokens` tokens), so there is nothing left to sub-divide. `pipeline.cuh`'s `ctl_words(int)` returns `kCtlFold0 + 1` now — ONE fold cursor, not one per window. Revival = git (`52a67d8^`). |
| 2026-08-06 | `52a67d8` | **`token_gather`** — the `ConsumerParams::token_gather` field and every branch keyed on it (the host-side per-plan choice between the token-gather walk and the aligned walk it was an alternative to) | A CAPABILITY REMOVAL: `token_gather` named a CHOICE between two walks (the P14 gathered walk and the pre-P14 aligned walk); the endgame executor has only one walk — the streamed membership scan described above — so there is no second arm for a flag to select between. Revival = git (`52a67d8^`). |
| 2026-08-06 | `e474da9` | **The fp16 decay-panel application** (U07-d: `factor` computed once in fp32 and narrowed to fp16, applied via a paired `hmul2` against the bf16-stored panel) | BUILT, THEN CARVED ON A MEASURED BREACH — recorded here rather than left as an unreachable arm because it shipped for one commit and its removal is a capability change a reader could otherwise mistake for a typo. Adjudication (`workflows/perf/p26_evidence/m2_fp16_breach.txt`): the M2-shaped visible-elapsed-exponent fixture PASSED on the fp16 path, but an ADMITTED hot routing (panel bound 23.3 of 40) put the per-group survival factor at ~e^15+, past fp16's 65504 max — measured NaN on `y` and `state`, a wrong answer rather than a rounding one. Per the standing STOP-and-carve rule the decay arms revert to fp32 panel application; bf16 amplitude STORAGE survives the carve (the memory win stays, the register cost is the ratchet's to adjudicate). Post-carve: gate 44/44, hot cell `3.4e-3`, M2-shaped cell `3.6e-3`, both inside the `1e-2` budget. Revival = git (`e474da9^`). |
| 2026-08-06 | `69a135f`/`111619e`/`c318b20`/`3660cf0`/`1d076b0` | **The P26 test-surface deletions**, whole: `tests/tier1_semantics/test_consumer_smem_precision_gate.py` (whole file), `tests/tier2_geometry/test_token_gather.py` (whole file, replaced by `test_endgame_executor.py`), and the ~13 individual dead-axis test functions across `tests/tier1_semantics/`, `tests/tier2_geometry/`, `tests/unit/` and `tests/layers/` (the `mma_precision`/tf32x3 arms, the `raw`-normalization cells, the `BT`-parametrized rows, and the amp-dtype diff mechanism) | Every one of these gated a capability the P26 endgame executor deleted (`BT`, `PREC`, `GLOBAL`/raw, the aligned walk — the axis rows above), so each deletion is the test-surface half of an axis kill already ledgered here. The per-test accounting — what each deleted test gated, why the surviving cell covers or supersedes it, and every re-anchored tolerance's re-derivation — is recorded row by row in `workflows/perf/p26_endgame.md` ("Test-surface DELETIONS", phases 1-5) and `workflows/perf/p26_test_migration.md`; this row exists so the ledger's own completeness rule (rule 1 covers `tests/`) holds without duplicating that accounting. `tests/unit/test_ring_depth_agreement.py` (`9478267`) is ledgered on the staging-ring row above. Revival = git (each file's pre-migration tip). |
| 2026-08-07 | `993b7b4` | **P28: sequence-parallelism (the `P` axis), whole.** `rola/routing/sequence_parallel.py` (whole file: `SequenceParallelPlan`, `SequenceParallelPolicy`, `owner_slot_counts`, `select_sequence_parallelism`, `build_sequence_parallel_tensors`, `MEASURED_HBM_BYTES_PER_S`, `MEASURED_STATE_ROUNDTRIP_BYTES_PER_S`, `GATES`, `consumer_blocks_per_sm`). `rola/planner/select.py`'s `select_p` (§4.3's second entry point) and its docstring citations; `FrozenConfig.sequence_parallel`, the decode-boundary property that always returned `None`. `rola/ops/consumer.py`'s `sequence_parallel`/`sequence_parallel_tensors` parameters and the plan-splitting branch of `_consumer_forward` (every launch now unconditionally builds `owners_arg = schedule.owners` and passes the extension's fourteen trailing sequence-parallel pointer arguments null -- the ABI itself is UNCHANGED, csrc did not move). `rola/interface.py`'s `_execute`'s `sequence_parallel` kwarg and its passthrough into `RoLAConsumer.apply`'s kwargs dict. `tests/`: `tests/unit/test_sequence_parallel_policy.py` (whole file, 479 lines -- the §6.4 policy's three admission gates), `tests/tier2_geometry/test_p_composition.py` (whole file, 991 lines -- the kernel-side `P`-composition gate; its shared, non-`P`-specific fixture infrastructure -- `_Fixture`, `_masked_simplex`, `_simplex`, `_topology`, `_SPARSE_WRITE_ROUTING`, `_relative` -- survives as `tests/tier2_geometry/fixtures.py`, `_Fixture.run()`/`.kernel()` stripped of their `P`/`sequence_parallel` arguments since `test_matrix_rows.py`'s BC/paging rows still build fixtures through it). `tests/tier2_geometry/matrix.py`'s `P` `AxisDeclaration` and `_p_rows()` (12 `GeometryPair` rows: the `P=2` zero-init engineered-identity row, and per `P` in `{2,3,4}` the carry-fold/split-readout reassociation rows plus the never-written-leaves carve-out row). `tests/tier2_geometry/test_matrix_rows.py`'s now-orphaned `_split_zero_init`/`_split_written`/`_read_only_owner` fixtures, `_never_written_leaves` selector, `_carry_fold_T_eps` bound, and the `P`-axis branch of `assert_precondition` (kept: `_multi_owner_dense`/`_paged_dense`, `_split_readout_n2_eps`, the BC/paging precondition branches -- all still exercised). `tests/tier2_geometry/test_meta.py`'s `test_M3_fails_when_a_split_silently_degrades_to_P_equals_one` (its fixture row is gone) and `test_M2_fails_on_a_fixture_a_broken_kernel_could_not_move` (its only non-default-probe row, `restrict="never_written_leaves"`, is gone with the row that carried it -- M2's general vacuity behavior is still exercised by `test_M2_an_engineered_identity_row_is_not_vacuous`, just not by a second row that deliberately mis-probes). `tests/tier2_geometry/test_paging_equivalence.py`'s `test_a_near_full_but_fitting_arena_survives_a_real_launch_at_P` RE-ANCHORED to `test_a_near_full_but_fitting_arena_survives_a_real_launch` (its non-`P` claim -- the allocator's ceiling arithmetic at a real launch -- kept at `P = 1`; its `P`-composition claim, never composed with paging anywhere else, dropped); `_run_sp` deleted, callers moved to the surviving `_run`. `tests/tier1_semantics/test_consumer_item_stream_gate.py`'s starvation axis (e) (`test_item_stream_is_live_on_the_segmented_path`, `P in {2,3}`) deleted whole -- its claim was the summary pass's forced `is_read=false`, a `P`-specific code path with no surviving mechanism to produce it; axes (a)-(d) (`test_item_stream_is_live_and_exact_under_role_imbalance`) kept, `P=1` dropped from its now-constant call. `tests/layers/test_poison_guard_vacuity_probe.py` re-pointed its arbitrary "real rola module" frame probe from `rola.routing.sequence_parallel` to `rola.ops.consumer` (the probe's claim -- the NaN-poison guard engages from inside a rola frame -- does not depend on which module). `tests/unit/test_planner.py`'s `test_select_p_routes_the_hardware_record_into_the_unchanged_6_4_policy` and the `select_p` import. `tests/conformance/families.py`'s `P` config axis (dropped from `_cfg`, `Family.__post_init__`'s required keys, `instantiation_values()`), the `corner/k0-fold` and `corner/ragged-tail` adversarial families (both were forced-`P=3` fixtures; `k0-fold`'s claim -- a summarized segment's `K=0` degeneracy -- has no surviving mechanism since a "summarized segment" only exists under a `P`-split; `ragged-tail`'s residual non-`P` claim -- `T=83` under the fixed scan word -- is already covered by the surviving `stratified/ragged`), and the `sweep/P{2,3,4}` stratified rows. `tests/conformance/test_family_meta.py`'s `FROZEN_CORNERS` lost `corner/k0-fold`/`corner/ragged-tail` (the "frozen permanent" rule protects a corner's DEMONSTRATED bug class, not a deleted capability's fixture). `tests/conformance/generators.py`'s `Case._k0_fold`, `Case.forced`, `self.P`, the `sequence_parallel` imports, and the `case.P` branches of `_check_divisible`/`_check_ragged` (both checkers now assert only the scan-word divisibility they were named for). `tests/conformance/test_conformance_census.py`'s `("P", 2/3/4)` off-mid-instantiation cells and the `("P", 3, "tail", "ragged")` exception (re-derived join size: 60 off-mid-x-non-mid cells, `EXPECTED_EMPTY` 58). `tests/conformance/test_producer_families.py`'s `_view`'s `P=c["P"]` field (unread by any checker). `benchmarks/bench_sequence_parallel.py` (whole file, 373 lines). `benchmarks/bench/drivers.py`'s whole "§6.4 SEQUENCE PARALLELISM" section (`_forced_split`, `run_split`, `run_split_gate`, `run_p1_identity`). `benchmarks/bench_consumer_new.py`'s `split`/`p1identity` CLI modes | A CAPABILITY REMOVAL, ruled by Blake 2026-08-06 (queue P28, the post-endgame roadmap): the endgame executor's schedule owns work division natively (P26, merged `93ca094`), which is what made sequence-parallelism -- the pre-executor host-side answer to dividing one owner's work across several launches -- a problem the token schedule already solves. `P` was already unreachable from the public API (`rola_op` never threads a `sequence_parallel` argument into `_execute`; only tests and benchmarks called the private path directly with one), so this is a python-plane deletion with the extension ABI held fixed: `rola_consumer_forward`'s fourteen trailing sequence-parallel arguments are still declared and still accepted, every launch now passes them null, and `docs/internals/consumer.md`'s dormant-machinery register (§B2) is updated to say so plainly rather than citing tests that no longer exist. Revival = git (this row's commit, parent). |
| 2026-08-06 | `be4a01b` | **P29: routing-coherence residual losses + injected routing factors, whole.** `rola/ops/losses.py` (whole file: `DualAscentLambda`, `tsallis_participation_surrogate`, `tier1_support_loss`, `TouchedPrefixState`, `tier2_prefix_novelty_tax`). `rola/routing/producer.py`: `BudgetConditioning` wiring, `SimplexProducer._allocate_conditioning`/`_apply`'s `temp_b`-precision handling, `_conditioned_solve` (the chunk-recurrent temperature-actuator + usage-bias-head recurrence), `init_temp_bias`, `_embed_budget`, `_normalized`, the `temp_W`/`temp_b`/`usage_bias_W`/`usage_bias_b`/`budget_embed_W`/`budget` constructor args and parameters, `_TEMPERATURE_FLOOR`, `RouteSource.budget`, `ProducedRoutes.slack_history`. `rola/routing/budget.py`: `BudgetConditioning`, `chunk_target_count`, `chunk_slack`, `normalized_slack`, `update_soft_usage`, `chunk_boundaries`, `DEFAULT_CHUNK_SIZE`. `rola/routing/factors.py`: `RouteFactors.slack_history`, `RouteProducer.forward`'s `budget` kwarg and its slack-collection loop. `rola/layers/rola.py`: `RoLA.forward`'s `budget` kwarg. `rola/__init__.py`: the `BudgetConditioning` export. `tests/`: `tests/unit/test_budget.py`'s conditioning-arithmetic tests (kept: the two general statistics-primitive tests), `tests/unit/test_producer.py`'s `test_embed_budget_*`/`test_fully_inert_budget_conditioning_warns_loudly`/`test_budget_conditioning_rejects_softmax_level`/`test_conditioned_path_*`/`test_usage_bias_fires_even_when_no_tier1_budget_is_supplied`/`test_conditioned_r_input_preserves_fp64_precision`, `tests/unit/test_production_levels.py`'s `conditioned` parametrize axis and `test_producer_conditioned_path_backward_matches_its_oracle_path` (plus its `_CONDITIONED_VJP_RTOL`/`_ATOL`/`_SEEDS` derivation block), `tests/conftest.py`'s `embed_budget` adapter and `produce_routing`'s conditioning arm | A CAPABILITY REMOVAL, ruled by Blake 2026-08-06 (queue P29, the post-endgame roadmap; the paper-1 scope ruling on budgeted routing amended the same day from "deferred" to "descoped entirely"): the paper-2 budgeted/coherence family -- a compute-budget target actuated into the router through a learned temperature head, a budget embedding and a usage-conditioned per-digit bias, trained against by a GECO-style dual-ascent support loss and a first-touch locality tax -- leaves the tree whole. The endgame's data-adaptive executor (P26, merged `93ca094`) obsoletes the premise: a fixed compute budget no longer needs a router-side lever to hit it, because the executor is already data-adaptive. Paper 1's thesis is unchanged and does not need this machinery: FA parity plus wins on INCOHERENT entmax routing. What survives, because it is general-purpose and not budget-specific: `rola.routing.budget.exact_support_count`/`touched_bitset_update`, the two primitives `RouteFactors.support_counts()`/`touched_bits()` derive from on demand (`docs/internals/DELETIONS.md`'s "eager auxiliary statistics" row, still true) -- neither computes a target, a slack or an actuator, they are exact nonzero-count and touched-digit facts about a routing allocation with no relationship to a budget. Revival = git (`be4a01b^`). |
| 2026-08-06 | `6cb2dfd` | **`LearnedDecay`'s `'level'` scope**: the `'level'` entry of `_SCOPES`, its `shapes` branch in `__init__`, and the parametrized test cell in `test_every_learned_decay_scope_emits_the_same_dial_shape` | A CAPABILITY REMOVAL, ruled 2026-08-06 (docket, identity argument): under the write-conditioned per-leaf clock `keep = (1 - prod_l delta_l)**c` (one clock per leaf, never per level), a per-level rate enters the recurrence only through that product. The recurrence has no way to observe which level contributed how much of it, so `'level'` was an UNIDENTIFIABLE reparameterization of `'global'` -- same forward, same backward, extra parameters with no distinguishing gradient. `'state'` (per-digit, the finest resolution) and `'global'` (one clock) are the two scopes that remain and they are the two the recurrence can actually tell apart. `docs/api.md`'s scope enumeration updated in the same commit. Revival = git (`6cb2dfd^`). |
| 2026-08-06 | `8675f95` | **The per-token atom-byte union** (`read_atom_bytes`/`write_atom_bytes`, `[BH, n_slices, T]` uint8, one byte per token) and its warp-0 consumption path: the warp-0-only byte gather, the paired `__reduce_or_sync`, and the `g_scan` mask slots it published through (`g_scan` shrinks from `8 + D` to `6 + D` ints) | A CAPABILITY REMOVAL, ruled with Change 1 (the atom-major fold nest + atom-major contiguous union bitlists): `read_atom_bits`/`write_atom_bits` (`[BH, n_slices, QPO, n_twords]` int32, one CONTIGUOUS token-bit row per atom — the layout the per-atom fetch era streams without a further migration) replace the per-token bytes, and because ballot consumption is now WORD-WISE (the visit's atom row word AND the taken word) rather than per-token, the visit's atom masks become uniform registers straight off that AND — there is no per-token byte left to gather, so the warp-0-only gather and its `__reduce_or_sync` reduction have no input to consume. `rola.planner.schedule._atom_union_bits` is the host-side reference for the new ABI. Revival = git (`8675f95^`). |
| 2026-08-07 | `b4c71d7` | **`pipeline.cuh`, whole, and `docs/internals/pipeline.md` with it**: the item-stream control block (`ctl`, `kCtlReadDone`/`kCtlGramDone`/`kCtlFold0`, `ctl_words`) and its two release/acquire primitives (`retire_item`, `eligible`, the `await_count` spin) | A CAPABILITY REMOVAL: the protocol existed so an early warp could take another fluid-pool work item instead of stalling on a rendezvous, and with the mass column off the critical path (the row below) the fold's item pool is a STATIC STRIDED PARTITION with nothing left for an early warp to run — so there is no rendezvous left to avoid a stall on, either. Both counter spins collapse into the ONE `__syncthreads()` `consumer_kernel.cuh` already needed after the Gram, which carries the readout-before-fold edge on `S` and the Gram-before-phase-C edge on `Gm` at once (`docs/internals/consumer_kernel.md#visit-barrier`). Revival = git (`b4c71d7^`). |
| 2026-08-07 | `b4c71d7` | **`frag_a_get`** (`mma_tile.cuh`'s hand-rolled inverse of `frag_a_slot`, ~10 integer ops per element) and its HAZARD | A CAPABILITY REMOVAL: the inverse existed to read one element of the fragment-ordered read panel back out for the mass column's denominator, which was a scalar reduction because an `n8` MMA tile "would waste 7/8 of a fragment" — a rejection that priced the fragment and not the instructions. `mma_tile_fragA_massB` puts the same matvec on the tensor cores instead (broadcast B across the tile's eight `n` slots, `docs/internals/mma_tile.md#mass-column`), which has no use for reading an element back out of a fragment at all. Revival = git (`b4c71d7^`). |
| 2026-08-07 | `b4c71d7` | **`gm_put`** (`mma_tile.cuh`'s unclamped bf16 store, distinct from `stage_put`) | A CAPABILITY REMOVAL: `gm_put` stored the Gram into `Gm` without §12 rule 3's clamp, which was safe only because every reader of `Gm` was a scalar walk that clamped on the READ side instead. GEMM 3's A operand is now `ldmatrix`-fetched (`Gm` sits at a pitch `pitched_ldsm_banks_ok` licenses), and an `ldmatrix` has no read-side clamp to put one at — so the guard MOVED TO THE STORE (`stage_put`, exact, since the readers were computing precisely that clamp), and the unclamped store had no remaining caller. Revival = git (`b4c71d7^`). |
| 2026-08-07 | `b4c71d7` | **The `den` SMEM array** (`consumer_kernel.cuh`'s `[kGroupTokens]` fp32 mass-column denominator accumulator, 64 B/visit on every arm) | A CAPABILITY REMOVAL: `den` existed to hold the scalar-reduction denominator between the readout, the Gram-derived intra term and the split-K emission. The denominator is now a register-resident MMA D fragment (`dm0..dm3`, one `[16 x 8]` accumulator on WARP 0, `docs/internals/consumer_kernel.md#mass-column`) — to the denominator exactly what `y0..y3` already was to the numerator — so there is nothing left to carve SMEM for. Revival = git (`b4c71d7^`). |
| 2026-08-07 | `b4c71d7` | **The fold's dynamic item-claim pool**: the per-visit `atomicAdd` claim cursor (`ctl[kCtlFold0]`) and its `__shfl_sync` broadcast, HAZARD `fold-claim-uniform` | A CAPABILITY REMOVAL: the claim existed to load-balance the fold's work items across warps when an item's cost could vary. Every fold item is now one `ldmatrix` pair and one `mma.sync` — uniform cost — and `n_fold_items` is CTA-uniform, so a compile-time-shaped strided partition (`for (c = warp; c < n_fold_items; c += kWarps)`) balances to within one item for any mask at zero atomic cost, and the claim cursor has no imbalance left to correct for. Revival = git (`b4c71d7^`). |
| 2026-08-07 | `47035ee` | **The P31 test-surface deletions**: `tests/unit/test_phase_c_gate_structure.py` (whole file, 658 lines — a SOURCE-STRUCTURE audit that parsed `consumer_kernel.cuh` for the phase-C counter's release/acquire/store sites and discharged a release invariant over the `kNarrowGate` fork), and `tests/tier1_semantics/test_consumer_item_stream_gate.py` RE-ANCHORED to `test_consumer_visit_barrier_gate.py` | The audit's SUBJECT is the protocol deleted in the `pipeline.cuh` row above: there is no counter to retire, no narrow-gate fork to prove safe, and no release invariant to discharge, so the file gates nothing (three of its six tests were already `xfail`ed as needing "a dedicated rewrite, not a mechanical fix" after the endgame executor moved the same structure — the rewrite is the deletion). What the audit was FOR — that the phase edge is safe — is now a property of `__syncthreads()` rather than of the source's shape, and needs no parser to check it. The re-anchored gate keeps its whole mechanism (child process, wall-clock timeout, per-cell oracle check, starvation axes (a)-(d)) and changes only its CLAIM: from "a software barrier can HANG" to "every predicate wrapping a barrier (`is_read`, `is_write`, `mixed`, `DECAY && is_write`) is CTA-uniform" — the liveness precondition the one hardware barrier now rests on. Revival = git (`47035ee^`). |
| 2026-08-07 | `2decba5` | **`kFoldStagedGroups`** (`csrc/rola/src/smem_layout.cuh`) and its values-shard tripwire `static_assert` (P25 contract item 4's premise, pinning the constant to 1 and naming the day it deepens as the day the values-shard dial must be built) | A CAPABILITY REPLACEMENT, not a removal: the constant became the `GT` template axis (`window_groups(GT)`, `{16, 64}`, [`smem_layout.md#window`](smem_layout.md#window)) — the fold now DOES hold more than one staged group per atom residency at `GT = 64`, exactly the condition the tripwire named, and the values-shard dial is STILL inert under it, because the tripwire's premise was the cursor SPREAD, not the staged depth as such: all `W` slots of a window are staged in the same barrier-delimited phase and released by the same end-of-window `__syncthreads()`, so the spread stays zero at every `GT` this batch ships ([`smem_layout.md#values-shard`](smem_layout.md#values-shard)). Revival of the pinned constant and its tripwire, if the dial's premise is ever restated: git `2decba5^`. |
| 2026-08-07 | `2decba5` | **The old shard file names**: `consumer_decay_bc16.cu`, `consumer_decay_bc64.cu`, `consumer_nodecay_bc16.cu`, `consumer_nodecay_bc64.cu` (`csrc/rola/src/instantiations/`), four shards of eight | RENAMED, not removed: `GT` joined `SHARD_AXIS_PRIORITY` ahead of `DECAY`/`BC`, so the matrix's 64 arms partition into eight shards of eight — `consumer_gt{16,64}_{decay,nodecay}_bc{16,64}.cu`. Every member the old shards carried still exists, under the new names, in the new partition; nothing here changed compute. |
| 2026-08-20 | `7c7776a` | **`test_g3_the_coarse_index_carries_a_sparse_multi_group_topology`** and its `kCOARSE_GROUP_LEAVES` constant (`tests/oracle/test_decode_vs_oracle.py`) | THE SUBJECT IS GONE: the cell gated the coarse popcount-prefix index over the leaf-space bitmap — `config.n_groups`, the 1024-leaf group, the exclusive prefix the walk binary-searched. The lattice walk enumerates `(owner, r_0 .. r_{D-2})` units from per-level factors and builds no bitmap over the leaf space, so there is no index to be off by one and no field on `DecodeGeometry` to read the group count from. What the cell established BEYOND the index — that a sparse `(64, 64)` fixture with a multi-CTA split matches the fp64 oracle — is `test_g4_matches_the_canonical_oracle[0.4-0.3-widths7]` and the `n_split` rows, which run the same topology. Revival = git (this row's commit, parent). |
| 2026-08-20 | `7c7776a` | **`test_the_global_residence_expansion_arm`** (`tests/oracle/test_decode_vs_oracle.py`), REPLACED by `test_the_widest_staged_topology` at the same `(256, 256, 4)` topology | A CAPABILITY REMOVAL, upstream of the test: the `IN_SMEM` template axis is gone with the expansion it selected residence for (32 instantiations become 16), so there is no second arm and the `n_words` SMEM-budget assertion names a quantity `DecodeGeometry` no longer carries. The topology itself is still gated — the replacement runs the same `N = 262,144` fixture, seed and density against the fp64 oracle, which is what kept the widest staged shape covered. Revival = git (this row's commit, parent). |
| 2026-08-20 | `7c7776a` | **`decode_expand.cuh`, whole: the K26 inner-block Kronecker construction.** `expand_inner<D>` (the seed over level `D-1` plus the outward broadcast down to level 1, destination-word-owned and race-free without atomics), `view_word_at`/`view_r_at`/`view_w_at`/`view_w_word` (level 0 served as a broadcast FUNCTION rather than materialized data), and the sizing constants `inner_span`, `inner_words`, `kExpandBufWords`; `docs/internals/decode/decode_expand.md` with it. | THE LEAF ORDER IS THE LATTICE NOW, SO THERE IS NO LEAF-SPACE BITMAP FOR AN INNER BLOCK TO BE PART OF. `expand_inner` existed to serve `supp(R)`/`supp(W)` as exact leaf bitmaps built by Kronecker broadcast from the outer levels down to level 0; the lattice walk never represents the leaf space at all — it composes a leaf from an owner, its fixed run offsets and one innermost digit ([`decode_lattice.md`](decode/decode_lattice.md)), so there is no bitmap for a broadcast to construct or for a view function to read a word out of. The digit masks that replace it (`decode_lattice.cuh::build_digit_mask`) are sized `sum_l ceil(B_l/32)` — one bit per DIGIT of every level, never a leaf-space bit — and are built directly from the amplitude rows with no recursion, no seed level and no ping-pong. GATED ON BIT IDENTITY, same as the removal it followed the pattern of: the K31 R3 batch's oracle and fixture battery (`tests/oracle/test_decode_vs_oracle.py`) holds the lattice walk's `y` and `state` against the fp64 oracle across the same topology space the deleted expansion was gated on. Revival = git (this row's commit, parent). |
| 2026-08-20 | `7c7776a` | **The `IN_SMEM` template axis, the expansion's GLOBAL scratch, and its sizing entry.** The `IN_SMEM` boolean template parameter on the step kernel and its effect on `decode_step_blocks_per_sm`; `DecodeParams::work`, `DecodeScratch.work`; the host functions `rola_decode_work_words` and `decode_work_words_per_cta`; the ABI argument `work` on `rola_decode_forward`. The instantiation matrix drops from `DV(2) x D(4) x DECAY(2) x IN_SMEM(2) = 32` to `DV(2) x D(4) x DECAY(2) = 16`. | THE ADDRESS SPACE THE AXIS SELECTED BETWEEN HAS NO SECOND ARM LEFT TO NAME. `IN_SMEM` existed because an inner block too wide for the shared-memory budget needed a global-memory fallback, and a runtime pointer there would have made every bitmap access on the fast arm generic; with the inner block gone (the row above), there is nothing an over-wide inner block could overflow into GLOBAL memory, so the axis has no second arm to select and the scratch it sized has nothing left to hold. The factor tables' SMEM footprint (`sum_l ceil(B_l/32)` mask bits, `sum_l g_l` owner-list entries per list) is bounded by the level-width and level-count capacities the same way the staged amplitude rows always were, and is refused loudly by the SAME `decode_step_smem_bytes` check the amplitude rows go through rather than by a second address-space arm (`decode.md#smem-refusal`). Revival = git (this row's commit, parent). |
| 2026-08-20 | `7c7776a` | **The coarse popcount index and the row-range-to-word walk it served.** `kCoarseGroupWords`, the coarse index array (`pc`/`pc_coarse`) and its block-scanned exclusive prefix; `locate_row` (the binary search over the coarse index plus the warp-wide popcount that resolved a global set-bit index to a bitmap word); the row-range-to-word boundary cut (`drop_low_set_bits`, `keep_low_set_bits`) and its load-bearing application order; the walk's per-group coarse-index consult that skipped wholly-empty 32-word groups; `digit_index`, `digit_class`, `class_word`, `class_popc` (the outer-digit row-counting index `cum`/`lw` built over class popcounts of the inner block). | THE OBJECT THESE INDEXED HAS NO ROWS TO COUNT. Every one of these existed to answer "which bitmap word holds touched row `X`, and how many touched rows precede it" over a leaf-space bitmap the CTA's row-range partition (`row_lo = seg * M / n_split`) was defined against. The lattice walk's grid partitions the UNIT RANK SPACE directly (`decode_lattice.md#the-owner-lists`, `decode.md#the-unit-partition`): a unit index decomposes into an owner rank and the fixed run offsets by SHIFTS ALONE, because each level's live count is padded to a power of two — there is no row count to locate a word within, no boundary word to cut, and no group to skip, because there is no bitmap left to skip groups of. GATED ON BIT IDENTITY against the fp64 oracle across the sparse multi-group topologies the coarse index's own test cell covered (superseded per the row above, 2026-08-20). Revival = git (this row's commit, parent). |
| 2026-08-20 | `7c7776a` | **The canonical `candidate_atom` span logic over the leaf-space bitmap.** `atoms_per_digit(S_1)` (atoms one outer digit's leaf range touches), `atom_written` (the per-atom write test over a half-word), and the `[n_live x atoms_per_digit(S_1)]` rectangle enumeration that took the lower digit on a straddling atom (`a > a_hi(k-1)`). | REPLACED, NOT REMOVED: the candidate-atom enumeration survives as `decode_step_kernel`'s `candidate_atom` lambda, which resolves a WRITE-LIST unit (`decode_lattice.md#the-unit`) and tests the window of that unit's innermost-run write slice the atom covers -- an ALIGNED window, because under the mixed-radix intra-box order a unit's leaves are a contiguous aligned run, which is the property `box.cuh`'s K3/K4 note proves rather than a half-word test over a materialized bitmap. The enumeration is still a PARTITION mapping monotonically onto ascending atom ids (`decode.md#candidate-atoms`), which the pool claim's canonical ranks still depend on; only the objects it is enumerated FROM changed, from an outer-digit list and a class popcount to a write owner-list and a sub-box's intra digit slice. Two shifts replace `atoms_per_digit`, and exactly one of them is ever non-zero: `atoms_per_unit_shift = log2(s_{D-1} / kAtomLeaves)` where a unit holds whole atoms, `lat_units_per_atom_shift = log2(kAtomLeaves / s_{D-1})` where an atom holds whole units. Both are shifts rather than per-digit divisions. Revival = git (this row's commit, parent). |

---

## Pre-extraction removals

This repository's history begins at the extraction. The rows below carry hashes of the
pre-extraction fork, whose history was retired and is not published
([`docs/provenance.md`](../provenance.md)): they record what happened and resolve nowhere.

| Commit (pre-extraction fork) | What | Invariant it leaves behind |
|---|---|---|
| `c3c2f845` (2026-08-01) | The register-resident consumer: its kernel header (also named `consumer_kernel.cuh`, a DIFFERENT kernel from this tree's file of that name), `intra_gram.cuh` and the `gram` intra lowering, `consumer_matrix.def` and its eight shard translation units, the `intra_lowering` template axis, and the analytic SMEM mirror | One consumer kernel family, so what the planner prices and what the driver launches are the same object: [`consumer.md`](consumer.md#one-consumer) |
| `c3c2f845` (2026-08-01) | The intra-kernel tracer: `v3_trace.cuh`, `v3_trace_host.{cu,cuh}` and the capture scripts, whose 27 `V3T*` emission sites were all in that consumer | No trace-emission site exists in the shipped consumer: [`consumer.md`](consumer.md#tracer-retired) |
| pre-extraction | The V2 CUDA page-plan producer and its eight pybind entries | The page table is published by the torch arena in `rola/ops/paging.py`: [`rola_api.md`](rola_api.md#deleted-page-plan) |

The ledger does not reconstruct that retired history.
| 2026-08-15 | P65 S2 | **The forcing census** -- `tools/ratify.py`'s `--force-ctas N` flag, its `-DROLA_FORCE_CTAS=N` define and `compile_and_parse`'s `defines` parameter | A DEAD INSTRUMENT: nothing in `csrc/` has read `ROLA_FORCE_CTAS` since the launch bounds became derived (`design::cta_target`), so the flag compiled the shipped bound and reported it as a candidate's. The measurement it named survives as a scratch-tree edit of the bound itself ([`../measurement.md`](../measurement.md)), which is also what keeps a forced bound unratifiable: the shipped tool no longer has the lever. Revival = git (this row's commit, parent). |
| 2026-08-07 | P39 | **The `D`-free residency/footprint query** (`kQueryD` in `consumer.cu`, and the four-argument `rola_consumer_occupancy(decay, BC, DV, GT)` / `rola_consumer_smem_bytes(decay, BC, DV, GT)` ABI) | AN ABI NARROWING, forced by measurement rather than by taste: the query answered at a fixed `D = 2` on the stated ground that "`D` moves only the `[D][BC]` digit table". The amplitude prefetch slab makes that false -- it carries one 16-byte chunk per token per side for every level beyond the first -- so a residency answer at the wrong `D` is the wrong arm's, and `select_window`'s residency rule consumes exactly that answer. Both entry points now take `D` between `DV` and `GT`; `select_window` passes `len(schedule.read_amps)`. Revival = git (this row's commit, parent). |

---

## <a id="dormant"></a>Dormant by plan — implemented, kept, NOT selected

| What | Where | Why it is kept | What would activate it |
|---|---|---|---|
*(empty)*

**The register's one row left it a THIRD way, and that is recorded rather than
quietly dropped.** The three-pass §6.4 sequence-parallel split (`P > 1`) -- pass
1's sparse per-segment summary, `scan_kernel`'s ordered fold, pass 3's entry-state
stash, the fourteen optional split arguments -- was dormant by plan in
`csrc/rola/src/consumer.cu` / `consumer.cuh`. Its host-side half was deleted at
P28 (`993b7b4`, the row above) with the extension ABI held fixed, leaving the
device machinery reachable only by an argument nothing passed; the rest of it went
with `consumer.cu` itself in P67 D2-c (`aa2fb75`). So it left WITHOUT its
activating measurement: not because a split was measured and lost, but because the
kernel that carried it was retired. Whether a schedule-native split earns its
place on the chunk arm is an open question and a new one, to be answered on that
kernel rather than inherited. Revival: `git show c7eaeb9:csrc/rola/src/consumer.cu`.

A row leaves this register in exactly two ways: it becomes plan-selected (and the
row goes), or its activating measurement is taken and comes back negative for
good (and it moves to Deletions with that measurement cited). A THIRD way is
possible and is what happened above -- the code's host is deleted for an unrelated
reason -- and it must be written out here when it happens, because a dormant row
that silently disappears is indistinguishable from one nobody checked.
